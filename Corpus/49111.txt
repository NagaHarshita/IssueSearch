 URL : "https://github.com/ros-industrial/ros_qtc_plugin/issues/215" TITLE : support partial build BODY : with the current catkin build setup, i don't see a possibility to trigger a partial build of a single package. often i only want to build the package, i'm currently editing. with a custom build step, i achieved this with: comamnd : catkin arguments : build --this --no-deps --no-status working directory : %{currentdocument:path}