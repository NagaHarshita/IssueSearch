 URL : "https://github.com/survival/donation-system-webapp/issues/2" TITLE : gift aid html validation BODY : ask if ok to submit a form with the neither gift-aid checkboxes checked. if not, can we enable a default e.g. always yes the only way to enforce tat one radio button is checked when you don't start with a default one already checked, is to use javascript. which means we need to install and setup jasmine.