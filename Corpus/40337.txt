 URL : "https://github.com/asamuzaK/withExEditor/issues/26" TITLE : trigger change event BODY : hi, great addon, i've recently switched from it's all text due to e10s support :+1: one bug i've run into is that the addon doesn't trigger the change event on a textarea. this is required for some web apps that only grab the contents of a textarea on change. the change event doesn't fire automatically if the contents of a textarea is updated via javascript. to work around this bug, please manually trigger the change event on a textarea after updating its contents. thanks!