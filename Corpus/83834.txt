 URL : "https://github.com/brandaorodrigo/moodle-format_buttons/issues/6" TITLE : color setting don't works BODY : hi, if we define in color settings a html color with letter ex: 00ff00 , this setting don't works. in script _renderer.php_, in _function get_color_config _: your test if is_numeric '0x'.$color don't allow hexadecimal color with letter. maybe you can use something like : if preg_match '/^ ? a-f0-9 {6}$/i', $color