 URL : "https://github.com/jemise111/react-native-swipe-list-view/issues/131" TITLE : hiddenrow height wrong BODY : hi: swiperow.js render { return <view style={this.props.style ? this.props.style : styles.container}> <view style={ styles.hidden, { //height: this.state.hiddenheight, // //width: this.state.hiddenwidth, // } }> {this.props.children 0 } </view> {this.renderrowcontent } </view> ; } // lines i think is wrong, when i delete row ,the height is wrong, wo just set the hidden row style = 1 , it can make the height right