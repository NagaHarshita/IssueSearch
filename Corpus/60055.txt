 URL : "https://github.com/probot/probot/issues/316" TITLE : webhook test framework BODY : i think it would be really nice to have some node-github equivalent for mocking webhooks. it might be easier than working with payload objects directly because defaults could be baked-in. in this case, only necessary fields would need to be defined for each test. perhaps something like the following: js let robot = / ... / let github = new mockgithub ; let repo = github.mocks.repo { org: 'test-org', name: 'test-repo' } ; robot.auth = => promise.resolve github ; let webhook = github.issues.edited repo.issue { body: 'new issue body' } ; await robot.recieve webhook ; / ... assertions ... /