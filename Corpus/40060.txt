 URL : "https://github.com/afropolymath/mothership/issues/6" TITLE : task payment trigger BODY : once a task is marked as done by both the service provider and the customer, payment needs to be made to the service provider by the customer. a new transaction is created in the queue and once the transaction has been validated, the service provider is paid the amount set and the customer is debited. need to ensure that transaction flags can be set programmatically to ensure that transactions that don't meet requirements are flagged as suspicious and don't get resolved until an admin has had a look over it.