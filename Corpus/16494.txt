 URL : "https://github.com/babel/babel-preset-env/issues/119" TITLE : ios version issue BODY : because of browserlist not returning version in a reliable order https://github.com/ai/browserslist/issues/99 if the version is a 'range 10-10.1 for example babel-preset-env doesn not handle correctly ios version if you use the following targets configuration javascript targets: { browsers: '> 1%', 'last 3 versions', 'ios >= 6', 'not ie < 10' } you get javascript // using targets: { chrome : 49, edge : 12, firefox : 48, ie : 9, ios : 10, safari : 9 } with ios being 10 instead of the expected 6. this happens because babel-preset-env strongly relies on the list being ordered and just takes the last returned version. javascript return browsers.reduce function all, browser { var _browser$split = browser.split , browsername = _browser$split 0 , browserversion = _browser$split 1 ; if browsername in browsernamemap { all browsernamemap browsername = parseint browserversion ; } return all; }, {} ; maybe would be a good idea to do not rely on the order and just explicitly get the smallest version : javascript return browsers.reduce function all, browser { var _browser$split = browser.split , browsername = _browser$split 0 , browserversion = _browser$split 1 ; if browsername in browsernamemap { all browsernamemap browsername = math.min all browsernamemap browsername , parseint browserversion ; } return all; }, {} ;