 URL : "https://github.com/glfw/glfw/issues/1045" TITLE : inaccurate joystick axes BODY : i found out that the axes of my joystick/gamepad are only recognized that they moved if the value is much higher than the default value. for example the right trigger returns -1. if the value is higher than -0.75 its working fine. and this is not the hardware because steam can receive smaller values. why is this?