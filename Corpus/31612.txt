 URL : "https://github.com/barissaslan/django-dersleri/issues/2" TITLE : static file hatası BODY : /etc/nginx/sites-available/blog dosyasının içeriği şu şekilde ; server { listen 80; server_name 35.188.97.4; location = /favicon.ico { access_log off; log_not_found off; } location /static/ { root /home/syny/blog; } location / { include uwsgi_params; uwsgi_pass unix:/run/uwsgi/blog.sock; } } manage.py den runserver la çalıştırdığımda static dosyaları alıyor mesela ck editor çalışıyor fakat nginx ile çalıştırdığımda ckeditor çalışmıyor hatanın buradan olabilceğini düşündüm '/static/' yerini '/static/' files da yaptım fakat hala daha çalıştıramıyorum setting dosyasında mı değişiklik yapmam gerekiyor . blog/setting.py dosyasında da şöyle: static_url = '/static/' staticfiles_dirs = os.path.join base_dir, static , static_root = os.path.join base_dir, 'static/'