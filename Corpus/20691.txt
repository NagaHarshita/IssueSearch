 URL : "https://github.com/FreeCodeCamp-Detroit/pomodoro-timer/issues/6" TITLE : create canary test BODY : add the following template to the index.js file in the spec folder: const expect = require 'chai' .expect; describe ' canary test ', function { describe 'mocha/chai test environment', function { it 'runs', function { expect false .to.equal true ; } ; } ; } ; then on the command line, run npm test to confirm the test is working properly. experiment and add a second describe statement to the test to see if you can create a failing test, then submit your pull request.