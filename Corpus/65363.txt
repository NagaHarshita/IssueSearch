 URL : "https://github.com/markfinger/python-webpack-manifest/issues/4" TITLE : inline bundle rendering BODY : that would be great to allow rendering bundles inline rather than linking to them. this will speed up page loading for smaller bundles e.g. having facebook/twitter sharing scripts combined into a single bundle . example: python a string containing pre-rendered script elements for the main entry manifest.main.js '<script src= /static/path/to/file.js ><script><script ... >' a string containing inline pre-rendered script elements for the main entry manifest.main.inline_js '<script>/ content of /static/path/to/file.js /<script>'