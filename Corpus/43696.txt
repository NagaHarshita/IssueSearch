 URL : "https://github.com/Adoria/Martlet/issues/19" TITLE : db.py dies with error BODY : when %q is called with a username that doesn't exist, the bot exits. might need something to ignore the exception.