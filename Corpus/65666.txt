 URL : "https://github.com/Greenstand/treetracker.org/issues/5" TITLE : jsonp endpoints for map BODY : we would like to split the map repository from the backend so that frontend developers can work without having to set up a database and server. to avoid cross site scripting restrictions, we need to switch loading of data into the map to use jsonp calls rather than json over ajax.