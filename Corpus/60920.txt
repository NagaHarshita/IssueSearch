 URL : "https://github.com/shanzhenren/StructMineDataInfra/issues/2" TITLE : add query suffer button BODY : for both interfaces, we should have a button to suffer a query out of a given set, to auto-fill the query fields. this save time if do not want to type in queries.