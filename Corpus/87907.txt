 URL : "https://github.com/uoguelph-mlrg/theano_alexnet/issues/30" TITLE : merge split layer weights BODY : do you know how should i merge the the layer weights that are split in the two processing channels i.e. layers 2,4,5 , in order to load them to an alexnet implementation? i donwloaded the weights, loaded them and merged them along the number-of-filters dimension but the network output is junk.