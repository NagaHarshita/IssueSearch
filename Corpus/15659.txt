 URL : "https://github.com/checkstyle/checkstyle/issues/3892" TITLE : false positive in BODY : >/var/tmp $ javac main.java success >/var/tmp $ cat main.java import static java .math .biginteger .one; import java.math.biginteger; / main class. / public final class main { private main { int i = 0; } public static void main string... args { // write your code here biginteger max = one; } } >/var/tmp $ java -jar checkstyle-7.6-all.jar -c /google_checks.xml main.java starting audit... audit done. --------------- as per the google guidelines mentioned here https://google.github.io/styleguide/javaguide.html s3.3.2-import-line-wrapping this should cause an error. if static keyword is removed, audit prints the expected result. also in this example i used 3 indentation instead of 4 java .math .biginteger .one; and 0 indentation for static this should have been 4 indentations but were ignored when static was placed. if static is removed audit works properly