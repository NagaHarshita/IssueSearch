 URL : "https://github.com/repology/repology/issues/196" TITLE : add problems config BODY : instead of generating problem text from sql just store problem type and some arguments. handle problem types in config file, preferrably with template fragments to render them.