 URL : "https://github.com/garyburd/redigo/issues/228" TITLE : pubsub doc is outdated BODY : it still shows psc := redis.pubsubconn{c} psc.subscribe example for { switch v := psc.receive . type { case redis.message: fmt.printf %s: message: %s , v.channel, v.data case redis.subscription: fmt.printf %s: %s %d , v.channel, v.kind, v.count case error: return v } } which yields a vet warning that was corrected it the tests it should be psc := redis.pubsubconn{conn: c} ...