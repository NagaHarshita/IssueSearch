 URL : "https://github.com/udacity/robotics-beta-test-issues/issues/709" TITLE : c++ lesson 13 - ros nodes BODY : i cloned the simple_arm_c package as requested. however when doing the catkin_make i get the error multiple packages found with the same name simple_arm . this is because we also had a simple_arm in term 1. might be better to rename the new package as simple_arm_c too, to avoid confusion.