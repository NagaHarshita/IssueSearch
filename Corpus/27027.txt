 URL : "https://github.com/igvteam/juicebox.js/issues/40" TITLE : click and drag zoom BODY : implement juicebox-desktop click-drag zoom, enabled by holding alt key down while clicking. this is a very useful mode for zooming. see slack video for demonstration. could we have a transparent canvas overlay on top of the canvas used to draw the heatmap? or some other element on top? probably similar to the ruler in igv.js, but this one would be a rectangle with an anchor point the initial alt-clock location . again see slack for video.