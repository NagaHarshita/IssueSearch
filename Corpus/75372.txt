 URL : "https://github.com/audiojs/contributing/issues/36" TITLE : handling arrays directly BODY : pondering on _audio-noise_ and _audio-filter_ modules. what if we extend basic no-stream modules to work with float arrays and not force _audiobuffer_ type, while we have a chance 34? \+ that makes _audio- _ components a first-class citizens, allowing for low-level processing and graceful readme examples \+ that lowers entry barrier for non-audio folks \+ that enables direct nodejs application of modules, out of @audiojs infrastructure js const noise = require 'audio-noise/pink' let data = noise new float32array 1024 //vs let buf = noise new audiobuffer {length: 1024}