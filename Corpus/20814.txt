 URL : "https://github.com/charliekassel/vuejs-autocomplete/issues/8" TITLE : custom ajax method BODY : great component! is there a way to implement custom logic for the ajax ? so the ajax request is more complex than a single url.. something like this for example: <autocomplete ref= autocomplete :placeholder= 'search :source= lookupfieldreference > </autocomplete> lookupfieldreference { ..do a more complex ajax request and return the results ? }