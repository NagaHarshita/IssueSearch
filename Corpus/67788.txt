 URL : "https://github.com/leafo/moonscript/issues/318" TITLE : putting numbers after @ BODY : quote from your document http://moonscript.org/reference/ the-language/function-literals/considerations : > when there is no space between a variable and a string literal, the function call takes precedence over any following expressions. no other arguments can be passed to the function when it is called this way. however, the behavior of compiling @<number> is undefined and, in fact, will not raise an compile error. moonscript v0.5.0