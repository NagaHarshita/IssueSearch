 URL : "https://github.com/irazasyed/telegram-bot-sdk/issues/340" TITLE : issue with delivery order BODY : hi, im using v2.0 with laravel 5.2 , but im having an issue when i want to send 2 or more message to the same chat_id . they don't arrive in the order of my code. even more each time they do in different order. using webhook and tried both asycn true and false. public function callback { / @var \telegram\bot\objects\update $update / //$update = telegram::commandshandler true ; $update = telegram::getwebhookupdates ; $chatid = $update->getmessage ->getchat ->getid ; telegram::sendmessage 'chat_id' => $chatid, 'text' => $chatid ; telegram::sendmessage 'chat_id' => $chatid, 'text' => '1' ; telegram::sendmessage 'chat_id' => $chatid, 'text' => '2' ; telegram::sendmessage 'chat_id' => $chatid, 'text' => '3' ; return 'ok'; } i have also tried usleep after each sendmessage but the only thing that change is the response time for the first message they arrive close and still wrong order .