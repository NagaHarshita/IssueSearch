 URL : "https://github.com/ThePhD/sol2/issues/360" TITLE : new_usertype lambda this BODY : is it possible to write something like this: c++ a_state.new_usertype<crscene> scene , addnode , crscene this_, nodet1 a_node { //do stuff with this_ //e.g. wrapper around template func: this_->addnode<nodet1> a_node ; }, addnode , crscene this_, nodet1 a_node { //do stuff with this_ //e.g. wrapper around template func: this_->addnode<nodet2> a_node ; } ; so a lambda function that provides the current instance of crscene and takes additional arguments. basically, what i am trying to achieve is writing a wrapper that pins down various template functions by overloading them.