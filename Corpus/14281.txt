 URL : "https://github.com/gregtyler/sortie.js/issues/7" TITLE : add refresh event BODY : you may update the table contents and want to trigger a sort of the table. to do so currently, you need to find the individual th you want to sort by and send a sortasc/sortdesc event. we should automate that with a refresh event on the table that you can trigger. basic jquery written for doing this manually in lsgnt: $th = $ '.table-striped' .find 'th aria-sort ' ; $th 0 .dispatchevent sortie.events $th.attr 'aria-sort' === 'ascending' ? 'sortasc' : 'sortdesc' ;