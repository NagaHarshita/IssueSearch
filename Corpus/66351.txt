 URL : "https://github.com/veritrans/Veritrans-ios-sdk/issues/206" TITLE : missing redirection url BODY : hi midtrans, im anand, ios developer, sephora digital. when i try to generatetoken in swift, i found that it doesn't return me the redirection url, is that an issue or the new api changes don't require 3ds authentication? please find my code: obj-c code: midtransclient shared generatetoken:tokenrequest completion:^ nsstring token, nsstring redirecturl, nserror error { if error { //handle error } else { } } ; swift: midtransclient.shared .generatetoken tokenrequest!, completion: { token, error in if error == nil { //use the card token } else { //handle error } } thanks, anand