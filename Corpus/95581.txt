 URL : "https://github.com/rust-lang-nursery/rls-vscode/issues/116" TITLE : deglob adds unused imports BODY : starting with a snippet like: rust use chrono::prelude:: ; let now: datetime<utc> = utc::now ; println! {:?} , now ; and using the deglob command, the result is: rust use chrono::prelude::{timezone, utc, datetime}; where timezone is not needed, and actually detected as such by rls :