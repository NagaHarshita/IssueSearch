 URL : "https://github.com/recharts/recharts/issues/728" TITLE : tick text and tickline BODY : hi i have an issue with the tickline attribute in a line graph: i'm adding tickline='false' to the x axis. but then getting 'invalid prop tickline supplied to xaxis'? also, i need to change the color of the tick text, i've tried to create a function for that, but then, the text is positioned in the top left of the graph instead of where it's supposed to be? this is the code i use: function customxaxislabel props { return <text fill= white > {props.payload.value} </text> }