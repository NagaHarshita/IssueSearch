 URL : "https://github.com/clarus-proxy/JSqlParser/issues/2" TITLE : pull request ? BODY : do you think that it will be usefull to merge these version of jsqlparser https://github.com/jsqlparser/jsqlparser with the official version ? at least do a pull request ? otherwise on each build on the clarus proxy we will need to build this fork on the side since we cannot push it in maven central.