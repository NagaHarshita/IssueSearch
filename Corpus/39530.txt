 URL : "https://github.com/UofS-Pulse-Binfo/kptheme/issues/2" TITLE : shrinking the header BODY : currently the header takes up a lot of space unnecessarily. we have already shrunk the header on the homepage. task: the header on all pages other then the front-page should be reduced in height substantially. it should be smaller even then the front-page not larger.