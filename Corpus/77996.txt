 URL : "https://github.com/OhmGeek/aidans-jcr/issues/12" TITLE : page speed optimisations BODY : we need to eliminate above the fold css and js render blocking which just involves restructuring html also, add django compressor to the project and use it to minimise html, css and js make sure we integrate into the build process by using manage.py compress so that we can avoid render time minimisation . make sure you update requirements.txt! otherwise heroku will break.