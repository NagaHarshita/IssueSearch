 URL : "https://github.com/stijnbeki/TeamBS_GeoScripting/issues/1" TITLE : review team dragon master BODY : hello team bs, we ran your script and it works perfectly! we enjoyed your use of the function layerrd, it is quite elegant solution. one suggestion would be to use the prj_string as a parameter in that function and only set the variable in the main, and that way it gets even more general with that you would probably need to change the name too ; . the citbuf function is quite nice too. we would say that it would be more understandable if you had a plotting function and a buffer and intersection function that returns the cities names, but that's just a minor improvement. overall, great job :+1: cheers, rodrigo & ping