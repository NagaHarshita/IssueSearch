 URL : "https://github.com/manchesergit/material-ui/issues/100" TITLE : unique id libary BODY : all of the components that generate their own unique key use the same code this should be brought into its own library and included back where its required the function needs to be able to take a variable to add extra information to the unique id as required a version of the function may be required that takes a name to add as a class parameter as some classes don't have a constructor to read from eh?