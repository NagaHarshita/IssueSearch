 URL : "https://github.com/Miserlou/Zappa/issues/683" TITLE : binary upload support BODY : binary _downloads_ now work fine, thanks @wobeng - but uploads still don't. we'll have to fatten up our templates a bit to add support for the top _n_ mime-types we want to support, but it should be fairly do-able. related django test function for jpegs : python from django.shortcuts import render from django.http import httpresponse from django import forms class uploadfileform forms.form : file = forms.filefield def upload_file request : if request.method == 'post': form = uploadfileform request.post, request.files if form.is_valid : image = request.files 'file' .read mimetype = image/jpeg return httpresponse image, content_type=mimetype else: form = uploadfileform return render request, 'upload.html', {'form': form} related blog post here: https://aws.amazon.com/blogs/compute/binary-support-for-api-integrations-with-amazon-api-gateway/