 URL : "https://github.com/anarklab/todo-api/issues/10" TITLE : don't expose your database BODY : i've noticed that the endpoint of tasks respond exactly what he gets from the database, you are exposing your database schema that way, and that's bad for security, one way to solve that is by implementing transformers, you can have a transformer per model for example, since every model has a different schema.