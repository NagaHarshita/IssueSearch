 URL : "https://github.com/mapbox/mapbox-gl-js/issues/4597" TITLE : on should be idempotent BODY : like addeventlistener https://dom.spec.whatwg.org/ dom-eventtarget-addeventlistener , evented on and map on with delegation should be idempotent, and only do something if there wasn't already a listener for the given event type and callback. for example, the sequence on 'a', cb , on 'a', cb , off 'a', cb should leave no cb 's listening for a .