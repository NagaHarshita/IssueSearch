 URL : "https://github.com/ballerinalang/composer/issues/4287" TITLE : anonymous struct support BODY : need to support anonymous struct in transform. it could be used in several levels, we need to support adding them from ui and generate ui from the source. 1. as a struct field. struct person { string name; struct { string no; string lane; string city; } address; } 2. as a variable type function testfunc2 { struct { string no; string lane; string city; } address; } 3. as a function parameter type function testfunc1 struct { string no; string lane; string city; } address2 { }