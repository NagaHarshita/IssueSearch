 URL : "https://github.com/vlucas/valitron/issues/195" TITLE : illogical errors messages BODY : let's take this example $v = new \valitron\validator inputs ; $v->rule 'required', 'email', 'name' ; $v->rule 'email', 'email' ; if the email input is empty the validator will generate two messages $v->errors ; email is required and email is not valid !! but logically the validator have to generate only one message error: email is required , and ignore the second one ! i can validate required fields separately then validate the others, but i think this should be done automatically by the validator ! am i right ?