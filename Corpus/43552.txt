 URL : "https://github.com/HurBizo/Estudo/issues/2" TITLE : criando arquivos em c BODY : include <stdio.h> include <conio.h> void main { file arq; int i; int result; clrscr ; arq = fopen arqgrav.txt , wt ; // cria um arquivo texto para gravação if arq == null // se nào conseguiu criar { printf problemas na criacao do arquivo ; return; } for i = 0; i<10;i++ { // a funcao 'fprintf' devolve o número de bytes gravados // ou eof se houve erro na gravação result = fprintf arq, linha %d ,i ; if result == eof printf erro na gravacao ; } fclose arq ; }