 URL : "https://github.com/agm-dev/pushing-stories/issues/4" TITLE : does not work on firefox BODY : the use of module in script tag is not supported by default on firefox you have to enable it on about:config , so the app does not work by default on firefox. there is no reason to keep the module script tag as finally the import sentence is not used in the script, so it can be safely removed and that will make the app to work in firefox too ;