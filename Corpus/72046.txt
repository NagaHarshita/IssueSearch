 URL : "https://github.com/nextcloud/calendar/issues/465" TITLE : attendee displayed twice BODY : calendar 1.5.3 on nc12.0 release on debian 8.8, client: fx on win/lin steps to reproduce 1. have two users a and b, each of them sharing their default calendar with each other, enable can modify 2. a creates a meeting, invites b within 3. b accepts the meeting expected behaviour a and b see one entry for a and one for b actual behaviour - in nc web interface: a and b see one entry for a and two entries for b - in thunderbird calendar, a and b see one entry for a and one for b as expected