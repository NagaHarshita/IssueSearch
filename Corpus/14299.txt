 URL : "https://github.com/kazazor/coinsmarket/issues/33" TITLE : add search filter BODY : where should it be? above the table? in the header? if we do not get pagination, the search should be in the client side. if we do, it should be on the backend side. add auto complete auto filter the table esc should cancel basically just set the type to search if the material-ui auto complete components doesn't do it already