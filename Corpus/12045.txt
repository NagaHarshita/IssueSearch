 URL : "https://github.com/mjspncr/lzz3/issues/26" TITLE : mingw lzz errors BODY : there are a few errors in the .lzz files when building on mingw-w64, i'm not sure if they're the same with the normal mingw: change file.lzz c++ char const fullname = loc.getfullname ; to c++ std::string fullname = loc.getfullname ; std::replace fullname.begin , fullname.end , '\\', '/' ; remove in file_op.lzz c++ include <util/throw_exception.h> for basilcc add c++ include <sys/types.h> to all hdr... end defines make one if it doesn't exist in files where size_t is used.