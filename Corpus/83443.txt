 URL : "https://github.com/acquia/blt/issues/1448" TITLE : cannot install site in vm BODY : on the latest 8.x-dev, it seems like aliases, drupalvm, and site installs are not playing nicely. steps to reproduce: create a new project on blt 8.8.3 initialize vm blt vm verify that blt local:setup works fine update blt to 8.x-dev run blt local:setup again expected behavior: the site is installed in the vm actual behavior: error messages like this: > error mysql is not available. please run blt doctor to diagnose the issue. debugging, it appears that blt thinks the drush alias it should be using is @local , regardless of whatever is defined in project.yml or project.local.yml .