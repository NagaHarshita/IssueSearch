 URL : "https://github.com/rcrowley/go-metrics/issues/201" TITLE : meter memory leak BODY : i metrics.getorregistermeter many different meters to count events over the course of a second. every second i emit the meter values to my downstream monitoring system before calling unregisterall . the resources of the meters are never gc'd and the memory climbs drastically when many 50-hundreds meters are created.