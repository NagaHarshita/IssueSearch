 URL : "https://github.com/Vydia/react-native-background-upload/issues/52" TITLE : module vydiarnfileuploader requires main queue setup since it overrides init but doesn't implement requiresmainqueuesetup. in a future release react native will default to initializing all native modules on a background thread unless explicitly opted-out of. BODY : module vydiarnfileuploader requires main queue setup since it overrides init but doesn't implement requiresmainqueuesetup. in a future release react native will default to initializing all native modules on a background thread unless explicitly opted-out of.