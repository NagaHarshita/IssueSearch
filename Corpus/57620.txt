 URL : "https://github.com/CompuIves/codesandbox-client/issues/165" TITLE : react hot loader error BODY : loading the editor shows this error in the console: appcontainer.dev.js:32 react hot loader: it appears that react-hot-loader/patch did not run immediately before the app started. make sure that it runs before any other code. for example, if you use webpack, you can add react-hot-loader/patch as the very first item to the entry array in its config. alternatively, you can add require react-hot-loader/patch as the very first line in the application code, before any other imports.