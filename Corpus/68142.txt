 URL : "https://github.com/Whalen207/WH/issues/25" TITLE : remove loops on disable BODY : currently both loops main render loop in a mission eventhandler each frame, + update loop in a cba pfh every 0.5s continuously run, albeit with executing anything, even when the nametag system is disabled. for optimal performance both loops should be suspended or outright removed when the nametag system is disabled with disablekey = , scripts, or the cba menu , then reintroduced if the nametag system is re-enabled.