 URL : "https://github.com/haoliangyu/ngx-mapboxgl-starter/issues/7" TITLE : adding markers to the map BODY : hey any idea how i can add marker to the map ? i have this code in app.component.ts but it didn't show any markers on the map the same way for the popup works import {lnglat, map,popup,marker} from 'mapbox-gl'; let marker = new marker .setlnglat -96, 37.8 .addto map ; this.mapservice.marker = marker;