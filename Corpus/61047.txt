 URL : "https://github.com/benwbrum/fromthepage/issues/737" TITLE : add thumbnails to works BODY : need to add thumbnails for works. set up an interface to allow owners to upload thumbnails within the work settings. create a work.thumbnail method - if there's an uploaded thumbnail, return that; otherwise get a thumbnail from pages. on the pages tab of the work, allow the owner to select which page to feature as the thumbnail; otherwise as a default pick a page approximately a third of the way through the work.