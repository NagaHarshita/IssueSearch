 URL : "https://github.com/dhale/jtk/issues/26" TITLE : build and test failures BODY : @chrisengelsma hi chris! i just downloaded and built the project, but i ran into three problems along the way: 1 i got a gradle error from the distributions block in core/build.gradle. i removed the block and it resolved the error. 2 i got a gradle error in the signing block in core/build.gradle. i removed the block and it resolved the error. 3 my test in projectortest.testautolinear failed! the new call should not have the axis scale specified. i have almost zero experience with gradle, so the first two issues could just be my ignorance showing. the test should be fixed so that it passes...