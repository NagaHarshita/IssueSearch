 URL : "https://github.com/damianavila/RISE/issues/249" TITLE : not working within julia? BODY : if i run jupyter from the command line the slideshow button is available and works, but without interactive support for things like @manipulate that i am used to using in julia if i launch jupyter from within julia , then there is no slideshow button. have i installed this correctly? i can't figure out how to install it for julia directly. i had to install a separate miniconda3 to get jupyter going from the command line. thanks for any help