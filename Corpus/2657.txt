 URL : "https://github.com/DSASanFrancisco/membership_api/issues/44" TITLE : cli db access BODY : i'm putting this here until we can automate a way to access the cli that's connected to the database. bash docker-compose run --entrypoint=/bin/sh api python3 python from sqlalchemy import create_engine from sqlalchemy.orm import sessionmaker from membership.database.models import meeting example engine = create_engine 'mysql://root@db:3306/dsa' replace as needed engine.connect session = sessionmaker bind=engine session = session now query away meetings = session.query meeting .order_by meeting.start_time.asc -3: