 URL : "https://github.com/danialfarid/ng-file-upload/issues/1834" TITLE : progress without chunk BODY : hi, the progress without chunk feature not work? i'm trying in last version of chrome execute this code: javascript upload.upload { url: admin/arquivos , data: $scope.arquivo } .then function response { console.log response ; }, function err { console.log err ; }, function evt { console.log 'evt', evt ; } if i include the chunk config the progress work fine, but i no need chunk because the files are small.