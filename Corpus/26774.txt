 URL : "https://github.com/state-machines/state_machines-audit_trail/issues/19" TITLE : can't delete object BODY : since audit_trail is included > activerecord::statementinvalid: mysql2::error: cannot delete or update a parent row: a foreign key constraint i can't figure out why. we have overriden the destroy method since we can only destroy objects on certain states def destroy if added? || edit? super else raise activerecord::recordnotdestroyed, only mods with state :added and :edit can be destroyed end end the super-call throws the exception