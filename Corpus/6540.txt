 URL : "https://github.com/clearcontainers/tests/issues/580" TITLE : add swarm tests BODY : while running the swarm tests using ginkgo has a problem while launching the replicas exit code 127 . we need a bash script where the user can only perform only make swarm in order to run the swarm tests that will verify that each replica has two interfaces and another that will check the service ip among the replicas.