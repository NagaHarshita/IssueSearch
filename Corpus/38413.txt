 URL : "https://github.com/winstonjs/winston/issues/1161" TITLE : pintf is not defined BODY : const winston = require 'winston' const { createlogger, format, transports } = winston const { combine, timestamp, label, printf, colorize } = format const logger = createlogger { format: combine label { label: red 'my label' } , timestamp , transports: new transports.console { format: combine colorize , myformat , level: 'silly' } , new transports.file { format: myformat, filename: path.resolve logdir, 'info.log' , level: 'info' } , new transports.file { format: myformat, filename: path.resolve logdir, 'error.log' , level: 'error' } }