 URL : "https://github.com/byterock/mojolicious-plugin-routes-restful/issues/10" TITLE : typo in code BODY : this line warn api parent ->/ . $url . /:id->via->post-> $contoller_prefix-$contoller create if $verbs->{create} and $api->{debug} ; should be warn api parent ->/ . $url . /->via->post-> $contoller_prefix-$contoller create if $verbs->{create} and $api->{debug} ; does not effect operations just bad debug info