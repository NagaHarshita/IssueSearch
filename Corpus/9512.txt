 URL : "https://github.com/pauldendulk/Mapsui/issues/150" TITLE : zoom to markers BODY : hi i have more than 2 layers markers and want to zoom to best position so both markers is shown. i'm using map.navigateto minp, maxp . and running through my own markers and getting min and max. but it only show white screen, i've tried with markers that got a good distance. but is this the wrong way to do this? i know this will is a problem if markers is to close, but i've tried with markers far away from each other. public void zoomtomarkers { if markers.count < 1 || map == null return; if markers.count == 1 map.navigateto sphericalmercator.fromlonlat markers.firstordefault .longitide, markers.firstordefault .latitude ; var minp = new mapsui.geometries.point 99999, 99999 ; var maxp = new mapsui.geometries.point 0, 0 ; foreach var marker in markers { if minp.x > marker.latitude minp.x = marker.latitude; if minp.y > marker.longitide minp.y = marker.longitide; if maxp.x < marker.latitude maxp.x = marker.latitude; if maxp.y < marker.longitide maxp.y = marker.longitide; } map.navigateto new boundingbox sphericalmercator.fromlonlat minp.y, minp.x , sphericalmercator.fromlonlat maxp.y, maxp.x , scalemethod.fit ; }