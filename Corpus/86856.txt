 URL : "https://github.com/camroach87/validatr/issues/22" TITLE : improve validatr function BODY : 1. move data argument to first position to be consistent with piping methodology. can then have dplyr munging data in front of validatr. 2. change validatr to non-standard evaluation for y argument. consistent with ggplot2. parameters not dependent on the data frame input should be quoted, e.g., data_type = ts same idea as colour = red in ggplot2 . 3. add error if y is a factor/character for regression/ts data_type.