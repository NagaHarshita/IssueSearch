 URL : "https://github.com/PhleboMorph/PhleboMorph/issues/6" TITLE : export landmark as txt BODY : just a function in file/export landmark as txt
into a simple format like clic, for each image
this allows the user to export the image + landmark to analyse in other program.