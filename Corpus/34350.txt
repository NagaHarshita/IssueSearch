 URL : "https://github.com/fergaldoyle/vue-form/issues/93" TITLE : cannot use maxlength BODY : code like this <validate show= $dirty && $touched auto-label class= form-group required-field :class= fieldclassname formstate.name > <label>number</label> <input maxlength= 10 type= number name= number class= form-control required v-model.lazy= model.number > </validate> if model.number has default value 123 . the formstate has error of maxlength. minlength value, length { return value.length >= length; }, maxlength value, length { return length >= value.length; }, should be minlength value, length { return value.tostring .length >= length; }, maxlength value, length { return length >= value.tostring .length; }, because the type of number doesn't have method length