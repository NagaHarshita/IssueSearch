 URL : "https://github.com/codeschool-projects/CodeBreakerProject/issues/7" TITLE : task 8 issue BODY : in the task only set the answer and attempt hidden inputs when they aren't already set , it starts with: when we go to call the sethiddenfields functions in our guess function, we should only update our hidden inputs when they're not set. should that instead be something like: call the sethiddenfields function in the body of the guess function, but also write some logic so that it's only called when answer and attempt haven't already been set.