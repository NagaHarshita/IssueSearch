 URL : "https://github.com/ConciergeAuctions/react-mui-table/issues/29" TITLE : allow more customization BODY : not every table needs to have every available functionality. the table should be versatile and gracefully handle missing props. make the following optional: - filter - search - actions - sort - delete - pagination with the following additional options: - allow consumer to pass in custom number of rows currently 15,25, and 50 are hardcoded - display text instead of dropdown if rowchange handler isn’t passed in.