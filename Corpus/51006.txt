 URL : "https://github.com/dssg/tyra/issues/46" TITLE : add linting npm script BODY : to make it easier to use eslint, wrap it in an npm script. command to lint all files: node_modules/eslint/bin/eslint.js -c ../.eslintrc.json ./ we can add this to the scripts in package.json. extra points if we can figure out how to pass arguments through npm scripts to enable a file-specific interface instead of just the entire directory: npm run lint components/newcomponent/component.js