 URL : "https://github.com/Islandora-CLAW/CLAW/issues/588" TITLE : start the claw ontology BODY : although i've recently pulled out everything that would need to be put into an ontology, i'm introducing a refinement to pcdm:hasfile that will require one. there's a lot to getting one set up, but let's start wiht just the ontology file and we'll add the transforms and html machinery later. this is just the start.