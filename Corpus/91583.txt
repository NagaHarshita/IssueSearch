 URL : "https://github.com/espressif/esp-idf/issues/940" TITLE : uartrxstring block xtimer BODY : when i start a new task to use uartrxstring, the xtimer with auto-reload mode can't work. void uart_task { recv_idx = 0; while 1 { uartrxstring uint8_t buf, sizeof buf - 1 ;// 导致timer无法使用 int len = strlen buf ; memcpy &recv_buf recv_idx , buf, buf_size ; recv_idx += len; len = decode_buf recv_buf, &recv_idx ; } }