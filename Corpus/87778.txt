 URL : "https://github.com/MarlinFirmware/Marlin/issues/6799" TITLE : manual bed leveling delta BODY : using the delta mini config i have the following defines // define use_zmin_plug // define z_min_probe_uses_z_min_endstop_pin // define z_min_probe_endstop // define z_min_probe_pin z_max_pin define probe_manually // define auto_bed_leveling_3point // define auto_bed_leveling_linear define auto_bed_leveling_bilinear // define auto_bed_leveling_ubl // define mesh_bed_leveling define debug_leveling_feature define lcd_bed_leveling when i select prepare > level bed > level bed it will home and the lcd will say homing xyz then nothing.... if i click the knob the lcd will blank then if i hit the knob again it will say moving... but nothing will happen hitting the knob after this will have no affect do i have the incorrect setting or did i mess something up