 URL : "https://github.com/UMM-CSci/URSAmajor/issues/5" TITLE : sort by importance button BODY : under the submission list tab, there are three options where you can choose: sort by importance sort by newest sort by oldest the latter two work fine, but sort by importance doesn't do anything. it might sort by newest, but that doesn't really make them more or less important. how do you want to do this?