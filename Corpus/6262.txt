 URL : "https://github.com/wycats/handlebars.js/issues/1314" TITLE : = being mutated BODY : base64 string passed in with trailing = get mutated. in fact any = in a string gets mutated. var handlebars=require 'handlebars' var template=handlebars.compile {{val}} ; console.log template {val: a=b } ; result: a& x3d;b is there any option to change this behaviour? on the web its fine but for pure text parsing it causes issues.