 URL : "https://github.com/GNS3/gns3-gui/issues/2319" TITLE : req : autodetect kvm BODY : hi when we install gns3 on live distribution, we can't know if kvm can be used on the host. can we have an other option who is enable only if enable kvm acceleration is checked : this new option will be : enable kvm only if detected . i believe that you only need to verify if the file /dev/kvm exist