 URL : "https://github.com/RxSwiftCommunity/RxSwiftExt/issues/103" TITLE : weakify for 'do' BODY : name and description providing weak function pointer for do next, error, etc motivation for inclusion currently we have subscribeerror and subscribenext for weak function reference, but nothing like that for do example of use currently for subscribe loginobservable.subscribenext weak: self, login.routeto for do it would be something like this loginobservable.donext weak: self, login.routeto