 URL : "https://github.com/Adventurey-Things/Adventure-Bot/issues/8" TITLE : json filesize information BODY : so, for some adventures, there might end up being thousands upon thousands of lines long, with a lot of data being stored, we need to figure out if there is a maximum size of file that the bot can load, so we can either work around the limitation, or implement a way to make it work with larger files. you can add a more appropriate label than question i just couldn't add one so it was the most accurate i could get.