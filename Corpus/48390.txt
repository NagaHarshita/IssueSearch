 URL : "https://github.com/mattgreen/watchexec/issues/68" TITLE : run multiple commands BODY : at the moment cargo watch tries to execute multiple commands by joining them with && into one long string e.g. cargo build && cargo test && cargo doc --quiet . what are your thoughts on tweaking watchexec::cli::args so it takes a list of commands to run, instead of just a single command? i.e. something like this: diff pub struct args { - pub cmd: string, + pub cmd: vec<string>, pub paths: vec<string>, ... cc: passcod/cargo-watch 80 watchexec::cli::args : https://docs.rs/watchexec/1.8.5/watchexec/cli/struct.args.html