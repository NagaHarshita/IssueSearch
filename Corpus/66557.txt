 URL : "https://github.com/phalcon/cphalcon/issues/12945" TITLE : mvc add afterfind event BODY : expected and actual behavior there is no afterfind event for database models. in this case, there is no easy way to implement re-usable extensions behaviors for manipulating data after retrieving from database and before returning model s . if such an event were triggered after finding records, softdelete behavior could automatically filter results and exclude deleted records. details phalcon version: 3.2.0 php version: 7.1.4 operating system: windows 7 installation type: downloaded dll zephir version if any : n/a server: apache other related info database, table schema :