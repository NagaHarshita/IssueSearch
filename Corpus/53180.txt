 URL : "https://github.com/driftyco/ionic-cloud/issues/161" TITLE : deploy stopped working BODY : hi, deploy has stopped working today. i haven't changed anything. my app just no longer switches to the latest deploy. it downloads, extracts and restarts and shows the new build id in console, but it is still the old build displaying. i literally haven't changed one thing with my code. it just no longer displays the latest deploy. checked on multiple devices. this.deploy.check .then snapshotavailable: boolean => { if snapshotavailable { this.deploy.download .then => { this.deploy.extract .then => { this.deploy.load ; } ; } ; } } ; i'm at a loss....