 URL : "https://github.com/napcs/node-livereload/issues/88" TITLE : callbacks feature request BODY : what version are you using? don't say latest. be specific. 0.6.0 expected result i would expect livereload to be able to for a callback when a certain kind of extension was changed. example: javascript const livereloadserver = livereload.createserver { port: 35729, list: { ext: '.md', callback: => { dosomething ; } } } .watch dir ; actual result n/a steps to reproduce issue use the code above. why is this important? flexibility.