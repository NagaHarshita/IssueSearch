 URL : "https://github.com/fega/arduair-hardware/issues/2" TITLE : overflow in serials usage BODY : due that the project uses this winsenze03 library https://github.com/fega/winsen-ze03-arduino-library with 3 sensors, the sram appears to be overflowing and it behaves in unexpected ways. right now the only work around is to edit the hardware serial serial_tx_buffer_size and serial_rx_buffer_size parameters to 8 a solution without the modification of the arduino native libraries is preferred