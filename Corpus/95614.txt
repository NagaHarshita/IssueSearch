 URL : "https://github.com/sdaps/sdaps/issues/112" TITLE : deploy as docker container BODY : in order to reach a larger user base - including windows users - sdaps could provide a dockerfile that allows creation of a docker image to be run in containers on both linux and windows. dockerfile proposed - install dependencies - build from sources git clone - form last stable tag, setup.py install - setup default project dir - shared with host sdaps container features - allows execution of sdaps command - shared directories with the host - for project data files transfer - functions as described on the download/windows https://sdaps.org/download/ page. refs - docker for windows https://docs.docker.com/docker-for-windows/