 URL : "https://github.com/dweidenfeld/plexdrive/issues/30" TITLE : caching by usage BODY : hi, so i’m just wondering if it’s possible that you implement the feature that instead of expiring by time, the cache expire base on the last usage, for example we say we can have cache maximum of 500gb and then each time if any of the files that didn’t access for specific amount of time like 10 days they delete from the cache. this way we have the file that get more use in the cache handy.