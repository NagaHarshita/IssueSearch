 URL : "https://github.com/joaoleite/redash_oracle/issues/1" TITLE : oracle driver version BODY : apparently the redash code is built specifically to the 5.2 version of cx_oracle. could you change the dockerfile to install pip install cx_oracle==5.2 ? thanks!