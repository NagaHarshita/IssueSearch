 URL : "https://github.com/SockDrawer/StoryBB/issues/211" TITLE : debug templates switch BODY : while debug templates is off by default, it would be neat if the system actually had an option for this so you could switch it on or off. should be buried somewhere in system because it's practically a developer level switch. as regular people will have the template editor which would work differently by design