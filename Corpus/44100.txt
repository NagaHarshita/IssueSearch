 URL : "https://github.com/rancher/rancher/issues/8295" TITLE : ebs volume scheduling BODY : in order to better schedule ebs volume, we need to add the following functionality: 1. agent needs to be aware of the cloud provider and populate the correct availability zone label to the host. go-agent 2. ebs volume needs to populate the cloud provider info and availability zone to cattle and persist it. rancher/storage 3. scheduler needs to schedule the ebs volume to only the hosts with the same availability zone label. cattle or outside micro-scheduler @llparse @cjellick