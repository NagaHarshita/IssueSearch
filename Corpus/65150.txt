 URL : "https://github.com/specla/specla/issues/19" TITLE : implement the base cli. BODY : the goal with the cli is to improve the developer experience and to make the creation and scaffolding off new projects simple and efficient. commands to implement. - create : should create a new specla project. - create:controller : should create a new controller. - help : should print a beautiful help page. - version : should print the current version of the. - ... example code of how a command could be implemented. should support the registration of external commands from the project and project dependencies. js export default mycommand extends command { static signature = 'my:command' static description = 'some description of what the command does' constructor { // run some functionality! } }