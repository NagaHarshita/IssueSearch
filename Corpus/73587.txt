 URL : "https://github.com/w3c/csswg-drafts/issues/1875" TITLE : css-overflow shrink text BODY : basicaly if text won't fill in, the text would be choped, to it's maximum fittable characters option 1: preserve words... text would end up not trimming in middle of word, but in by words option 2: string it would end by... so overflow: chop; chop: <words || characters>, <'...'> so.. ... is the string it would end up inner text by