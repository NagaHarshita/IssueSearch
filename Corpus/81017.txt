 URL : "https://github.com/UtahDave/salt-vagrant-demo/issues/41" TITLE : vagrant 2.0.0 ssh issues BODY : when trying to run vagrant up on 2.0.0 the environments but exits during provisioning. if i run vagrant status , i see all the boxes running but when trying to vagrant ssh master , i get this error: ssh_exchange_identification: connection closed by remote host after looking through the issues, i saw that in https://github.com/utahdave/salt-vagrant-demo/issues/32 that this was tested with vagrant 1.9.0. when i downgraded to 1.9.0, destroyed my boxes and reran vagrant up everything seemed to work fine.