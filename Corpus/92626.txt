 URL : "https://github.com/mtimkovich/em/issues/5" TITLE : 'enter' to go to next line BODY : original ed has this useful feature. you just hit enter and it is assumed that you want +1,+1p . replacing this snipper in parse : go if len text == 0 { e.error invalid address return 0, 0, } with this: go if len text == 0 { if e.line + 1 >= e.buffer.len { e.error invalid address return 0, 0, } else { return e.line+1, e.line+1, p } } creates just that effect.