 URL : "https://github.com/akvo/akvo-lumen/issues/743" TITLE : improve dataset importers BODY : context currently each importer csv, flow at the moment need to generate postgres tables. this makes writing new importers unnecessarily complex and leads to code duplication in the individual importers. problem or idea introduce a datasetimporter protocol with minimal requirements for each importer. all that's needed is basically what are the columns? and sequence of data rows . solution or next step introduce the protocol and port the existing importers to use it.