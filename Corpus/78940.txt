 URL : "https://github.com/budgie-desktop/budgie-rd/issues/2" TITLE : replace use of qsettings BODY : turns out qsettings is woefully limited and cannot really read a .desktop file properly, i.e. ; separated values will only field the first value, with qt believing everything following is now a comment. thus, roll a custom reader ..