 URL : "https://github.com/Microsoft/vscode/issues/29759" TITLE : more task api feedback BODY : last round, i promise i'd to rename rebuildall to rebuild , we also just call it clean and build i'd add the name:string : any signature to taskdefinition to make clear that there is more i'd make task source not optional because it's also not optional in the ctor and since we want it in the ui