 URL : "https://github.com/jrean/laravel-user-verification/issues/140" TITLE : integration with doctrine BODY : suggestion: as there is laraveldoctrine, the direct handling on eloquent models e.g. $model->save, should be easily be overwritable/ extendable, so a seamless integration would be possible, agnostic to the used orm. update and save the model instance with the verification token. @param \illuminate\contracts\auth\authenticatable $user @param string $token @return bool @throws \jrean\userverification\exceptions\modelnotcompliantexception / protected function savetoken authenticatablecontract $user, $token { if ! $this->iscompliant $user { throw new modelnotcompliantexception ; } $user->verified = false; $user->verification_token = $token; return $user->save ; }