 URL : "https://github.com/kvichans/cuda_opts_report/issues/7" TITLE : parsing for pylib comment BODY : этот блок //sets python 3.x engine. //option has suffix for oses: windows: no suffix, linux: __linux , macos: __osx //if correct, you must see python version info in console panel ctrl+ //and command =sys.path works in console. // //instruction for linux, if py engine not found: // 1 open file manager, goto pc root, // 2 search for libpython3 , // 3 if not found, install python 3, goto 2 // 4 use one of found filenames; if don't work, use filename with path // //info for macos: // homebrew installs python which don't have 32-bit .dylib on 64-bit os // so you need to install python from python.org, and it must be 32-bit default 3.x is ok // // pylib : python35.dll , // pylib__linux : libpython3.5m.so.1.0 , // pylib__osx : /library/frameworks/python.framework/versions/3.5/lib/libpython3.5.dylib , // for macos: tried 3.4 to 3.8, which is found //api: delay for on_change_slow py_change_slow : 2000, дает опцию pychange_slow++огромный комент. про pylib. надо-- малый коменет 1 строка всего , что выше- не относится к опции.