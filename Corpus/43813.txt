 URL : "https://github.com/dancormier/react-native-swipeout/issues/159" TITLE : detect scrolling view BODY : is there a way to detect which view i scrolled? i'm using this in a scrollview list and need to get which view i scrolled. is there something i can do like: <swipeout right={swipeoutbtns} passdatatoonpress={e.id}> // where e.id is unique for this swpieout and then var swipeoutbtns = { text: 'lock', onpress: e => { console.log e ; // logs 1234 the id passed above } } ;