 URL : "https://github.com/openbmc/openbmc/issues/2265" TITLE : network:- enable zeroconf BODY : we need to make sure while enabling the zeroconf that ipmi should behave correctly. if on the system there are multiple ip address objects then we should not return the zeroconfing address. but if there is no other ip address on the system except zeroconfig then we can return the zeroconf address. zeroconf can be enabled on the system by writing the following parameter under nerwork section in network file. linklocaladdressing = yes.