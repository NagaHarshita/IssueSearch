 URL : "https://github.com/topcoder-platform/admin-app/issues/78" TITLE : add attributes to groups BODY : we'll need to have some additional attributes of a group. 1. public/private 2. is joinable? i.e. can the user join this group themselves or does an admin need to add them. if they can join themselves, then the api permission checks needs to be smart enough to allow them. 3. ... let's talk more about this one...