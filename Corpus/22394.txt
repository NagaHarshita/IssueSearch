 URL : "https://github.com/subgraph/subgraph-os-issues/issues/318" TITLE : kvm nat dhcp issue BODY : hi, i installed kvm with virt-manager. then, i installed debian 9 as a guest os. there was no problem during the installation except that the auto network configuration dhcp failed. the default network configuration in kvm is nat. i continued the installation without network configuration until it finished. then, i logged in to the guest os and tried dhclient command again without success. i also tried iptables -p to accept all the 3 policies on host os without success. dnsmasq is installed as it is required during the creation of the guest os with nat by default . thanks.