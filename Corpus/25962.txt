 URL : "https://github.com/Sailbook/harbour-matrix/issues/12" TITLE : add mer obs webhook BODY : we can have automatic builds on mer obs if we set up webhooks. to do this open up your github repository and go to settings > webhooks & services > add a webhook. use https://webhook.merproject.org/webhook/ as the payload url these following items should be set to default, but just check: content type should be application/json enable ssl verification enable just the push event enable active