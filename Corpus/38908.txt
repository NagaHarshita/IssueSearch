 URL : "https://github.com/theiviaxx/frog_ng/issues/61" TITLE : refactor viewer urls BODY : /v/:guid/:gallery - browse from gallery - guid changes when navigating images - if coming from link, gallery is used to query items /v/:guid - link to a single item - when closing, gallery will default to siteconfig /v/:guid/:guids - selection of items - guid is used to find index /v/:guid/:guids/:gallery - gallery is used when viewer is closed in the viewer, use this to adjust the url without cluttering history let route = /v/${this.index}/${this.guids} ; this.router.navigate route , {replaceurl:true} ;