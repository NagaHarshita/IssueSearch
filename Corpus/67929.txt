 URL : "https://github.com/zalando-stups/swagger-codegen-tooling/issues/58" TITLE : re-enable gradle plugin BODY : ensure same feature set to re-align the gradle plugin with the maven plugin. framework: - ensure gradle 2.10+ and gradle 3 compatibility is that sufficient? - java / kotlin ;- feature set: - use single input file instead of a source set as swagger source definition - apply same defaults as the maven plugin - ensure interoperability with compile phase testing: - smoke test using gradle testkit - at least one unit test for critical path