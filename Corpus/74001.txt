 URL : "https://github.com/servicecatalog/crowbar-openstack/issues/2" TITLE : docker registry settings BODY : the docker registry settings: - use_registry - boolean flag if other registry than docker hub should be used - host - port - auth - user - password the following issues still exist: - remove use_registry, registry must always be specified, default is docker hub if the host and port are empty. - not all registry settings are forwarded to the heat template and cloud init scripts - check how empty values are handled if docker hub used