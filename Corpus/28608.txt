 URL : "https://github.com/Distelli/graphql-apigen/issues/15" TITLE : hello world example BODY : hello, it is possible to provide a simple helloworld example. i try to use your code but it's not working. my schema file type helloworldquery { hello: string! } and my guicebinder public class binder extends abstractmodule { @override protected void configure { install new graphqlmodule ; bind helloworldquery.class .toinstance new helloworldqueryimpl ; } class helloworldqueryimpl implements helloworldquery { public string gethello { return world ; } } } but the result for the query: { query : {hello} } is always: { data :{ hello :null}} how it is possible to define a datafetcher ? something like this: datafetcher datafetcher = new datafetcher { @override public object get datafetchingenvironment environment { return world ; } }; thanks marcel