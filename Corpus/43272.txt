 URL : "https://github.com/renerocksai/sublime_zk/issues/2" TITLE : consider using expand BODY : i noticed in function of def select_link self http://docs.sublimetext.info/en/latest/reference/commands.html that it might be possible to use a command already provided by sublime text of expand_selection : > expand_selection extends the selection up to predefined limits. to enum : values: bol, hardbol, eol, hardeol, bof, eof, brackets, line, tag, scope, indentation. source: http://docs.sublimetext.info/en/latest/reference/commands.html when cursor is placed on word, calling expand_selection with arg of brackets expands selection up to first matching pair of brackets. for your plugin it would expand selection to get all of note id up to first instance of . with this method, users would be able to use both double brackets and single brackets for note id's. i know that some different zettelkasten users like to use both single and double brackets for denoting zettel links.