 URL : "https://github.com/velotaf/api/issues/4" TITLE : use configurable port BODY : at the moment the default port is hardcoded to 3000, use the env to change it and bind it to docker's exposed port which happens to be 3000 at the moment .