 URL : "https://github.com/osulp/Scholars-Archive/issues/914" TITLE : sa is duplicating files BODY : when depositing a work, something funky is going on with characterization that is causing the file to get re-attached multiple times, thus duplicating the file on the work.