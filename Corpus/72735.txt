 URL : "https://github.com/thednp/bootstrap.native/issues/169" TITLE : carousel v4 interval bug BODY : hi there! i want to stop automatic sliding for the carousel component, so i set the interval to false . however, the library ignores this and grabs the default 5000 . i created a quick fix by changing if ! options interval || intervaldata { // determine slide interval to if !options interval || !intervaldata { // determine slide interval