 URL : "https://github.com/NYPL-discovery/discovery-front-end/issues/549" TITLE : fix login cors issue BODY : when not logged in and hitting the request button from either the search results page or the bib detail page, we got cors error in the console trying to authenticate. this is fixed in pr 548. issue: we were trying to make a server side redirect in the ajax function. it should've just returned json data or error object but the redirect when checking user data caused it to fail.