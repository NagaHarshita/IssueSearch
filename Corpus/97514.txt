 URL : "https://github.com/MaxWason/GCROA-v3-Issue-Tracker/issues/17" TITLE : handle split trips from v2 BODY : there are a dozen or so trips in v2 that are split between multiple outfitters. since they are an edge case that breaks the migration algorithm, they are ignored by the script. however, we need to go back at some point and transfer them by hand.