 URL : "https://github.com/emberfeather/less.js-middleware/issues/146" TITLE : cannot change dist BODY : i can only use the default setting, my file structure is like below, -public ---assets ------less ------css i can only use the default settting, app.use lessmiddleware __dirname + '/public' ; , it'll compile into less folder directly. if changed to below, it never works anymore and has nothing outputted. js app.use lessmiddleware '/less', { dest: '/css', pathroot: path.join __dirname, 'public/assets' } ; what i want is to output the css file into the sibling folder css from less .