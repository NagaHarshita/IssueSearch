 URL : "https://github.com/UnknownShadow200/ClassicalSharp/issues/449" TITLE : map horizon offset issue BODY : say you have horizon block of grass: a when you join the map initially, it will have the offset of water b if you change block to lava then to grass, it will have 0 offset classicube just always has offset of water for horizon block. not sure if i should replicate that, or have it always use the actual block's offset