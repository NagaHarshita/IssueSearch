 URL : "https://github.com/interledger/connector.land/issues/13" TITLE : add registration form BODY : something like <form> <input name= addme placeholder= ilp-kit.example.com > <input type= submit value= add me ></form> on the front page, and then when you hit http://localhost:8000/?addme=ilp-kit.example.com this should be handled by a script that tests your added hostname live, and adds it to the list. this will also equivalently act as a test now function for hosts that are already listed.