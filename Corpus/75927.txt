 URL : "https://github.com/WISE-Community/WISE/issues/877" TITLE : implement teacher export BODY : this export will split workgroups into separate rows and duplicate the data to the separate rows. student names will be exported. scores for steps will be exported. the total score will also be exported.