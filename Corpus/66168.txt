 URL : "https://github.com/EpicEricEE/ShopChest/issues/130" TITLE : massive thread creation BODY : hi, on my server with +1000 shops, it crashes as soon as few players are online, due to too many thread being created. i looked into your code, and it may come from de.epiceric.shopchest.utils.shoputils line 312 and 384 : java if shop.gethologram != null { shop.gethologram .hideplayer player ; } so you are calling hologram hideplayer for every player and for every shop except the visible ones , and this method is: java public void hideplayer final player p { new bukkitrunnable { @override public void run { // ... so, _if i'm correct_, each time task is called, you are basically starting almost as many tasks as shops times players.