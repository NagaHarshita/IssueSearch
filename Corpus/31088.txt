 URL : "https://github.com/tkh44/emotion/issues/212" TITLE : rfc: remove theming BODY : i'm starting to think we should remove built in theming and just let users use the theming library themselves. having theming built in adds overhead and code complexity to styled that i don't think is necessary. i prefer to just import my theme file but i may be an outlier. some notes. if we remove theming... - styled because a pure function component that we can call with createeagerfactory https://github.com/acdlite/recompose/blob/master/docs/api.md createeagerfactory 🔥 - bundle size will drop - performance will be better - users that want theming can just wrap styled with withtheme - we may be able to add theming conditionally with babel option we could just wrap styled with withtheme and import it if the user wants theming