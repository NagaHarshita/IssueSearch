 URL : "https://github.com/FennecLabsLtd/VRUtilityBelt/issues/23" TITLE : overlay render models BODY : spawn invisible overlays for each model, so they can have an offset. then it can all just go into the json, yay