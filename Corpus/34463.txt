 URL : "https://github.com/spatie/db-dumper/issues/36" TITLE : character set setter BODY : we need a setter that sets the --default-character-set option. not sure how this should be handled in non-mysql environments.