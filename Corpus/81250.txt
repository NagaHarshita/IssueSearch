 URL : "https://github.com/styled-components/styled-components/issues/864" TITLE : what happened to .rules? BODY : hi guys, i used the .rules of an styled component to get the css rules only. with v2 this returns undefined. i used it like this: js export const arrow = styled.div position: absolute; width: 0; height: 0; border-style: solid; border-width: ${t 'size.blh' } ${t 'size.blh' } 0 0; border-color: ${t 'color.foreground' } transparent transparent transparent; // somewhere else export default styled.div margin: 0 auto; width: ${blh 1 }; &:after { ${arrow.rules} content: ''; border-color: ${b 'color.foreground', x => lighten 0.1, x } transparent transparent transparent; box-shadow: -3px -3px 3px 0px rgba 0,0,0,0.16 ; bottom: 0; transform: translatey ${blh 0.25 } rotate -135deg ; } // dont mind the app specific code like blh b am i missing something or do i have to define the styling with css .. and then use that on arrow and the other component? thanks!