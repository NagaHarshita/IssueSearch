 URL : "https://github.com/Automattic/mongoose/issues/5174" TITLE : count + distinct BODY : i think this might be a bug - js router.get '/count', function req, res, next { const query = req.parsedqueryparams; const info = req.parsedqueryopts; let q = response.count query ; if info.distinct { q = q.distinct 'userid' } q.exec .then stdpromiseresp res , next ; } ; bug: the count works fine, but if distinct is used, it looks like i don't get a number returned, instead i get an objectid, kinda weird!