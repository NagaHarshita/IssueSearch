 URL : "https://github.com/nodejs/node/issues/13225" TITLE : node debug cli freeze BODY : version : v6.10.3 platform : smp debian 3.16.43-2 2017-04-30 x86_64 gnu/linux accessed over shh with git bash on windows 10 64-bit. subsystem : debugger after debugging an example application see code below with node debug index.js the cli doesn't accept any input. to get back working closing git bash seems to be the only way for exiting the cli. everything seems to be blocked, so the shortcuts following didn't work: esc ctrl+c return enter etc. this can be reproduced after starting to debug index.js and typing n next to debug to the applications end. index.js: javascript const twitter = require 'twitter' ; const client = new twitter { access_token_key: '...', access_token_secret: '...', consumer_key: '...', consumer_secret: '...' } ; const params = { screen_name: 'nodejs' }; client.get 'statuses/home_timeline', params, function error, tweets, response { if !error { console.log response: ${response} ; console.log tweets: ${tweets} ; } else { console.log error ; } } ;