 URL : "https://github.com/bigfoot90/phpacto/issues/11" TITLE : rule create urlrule BODY : its like urirule described here https://github.com/bigfoot90/phpacto/issues/10 but in addition urlrule should match also - protocol required, eg. http or https or ftp or other - hostname required, eg. my.site.net - port if specified, eg. :80 or :443 or other if not specified it should match the protocol's default port if its protocol's default port it should be hidden from sample