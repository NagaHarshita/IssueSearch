 URL : "https://github.com/adambard/learnxinyminutes-docs/issues/3023" TITLE : návrhy na změny v textu BODY : špatně: python 0 == false => true 2 == true => false 1 == true => true správně: python při porovnání s boolean hodnotou nepoužívejte operátor rovnosti == . stejně jako u hodnoty none. viz pep8: https://www.python.org/dev/peps/pep-0008/ 0 is false => true 2 is true => false 1 is true => true špatně: python řetězce používají nebo ' a mohou obsahovat utf8 znaky toto je řetězec. 'toto je také řetězec.' správně: python řetězce používají nebo ' a mohou obsahovat unicode znaky toto je řetězec. 'toto je také řetězec.' špatně: python řetězce se také dají sčítat, ale nepoužívejte to hello + world! => hello world! dají se spojovat i bez '+' hello world! => hello world! správně: python řetězce se také dají slučovat hello + world! => hello world! dají se spojovat i bez '+' hello world! => hello world! špatně: python názvy proměnných mohou obsahovat i utf8 znaky název_proměnné = 5 správně: python názvy proměnných mohou obsahovat i unicode znaky, ale nedělejte to. viz pep 3131 -- supporting non-ascii identifiers: https://www.python.org/dev/peps/pep-3131/ název_proměnné = 5 špatně: python přístup k předtím nepoužité proměnné vyvolá výjimku odchytávání vyjímek - viz další kapitola neznama_promenna vyhodí nameerror správně: python přístup k předtím nedefinované proměnné vyvolá výjimku odchytávání vyjímek - viz další kapitola neznama_promenna vyhodí nameerror špatně: python seznamy můžete sčítat hodnoty sez a jiny_seznam přitom nejsou změněny sez + jiny_seznam => 1, 2, 3, 4, 5, 6 správně: python seznamy můžete slučovat hodnoty sez a jiny_seznam přitom nejsou změněny sez + jiny_seznam => 1, 2, 3, 4, 5, 6