 URL : "https://github.com/blevesearch/bleve/issues/637" TITLE : search for exact array? BODY : i understand that arrays is not fully implemented as there are some issues that relate to it eg: 570 but i couldn't find any information on searching on arrays. here's example code of what i'm trying to achieve: package main import fmt github.com/blevesearch/bleve log type doc struct { name string recipients string } func main { mapping := bleve.newindexmapping index, err := bleve.newmemonly mapping d := doc{name: xyz , recipients: string{ a@example.com , b@example.com }} if err := index.index 1234 , d ; err != nil { log.fatal err } d2 := doc{name: abc , recipients: string{ a@example.com , b@example.com , c@example.com }} if err := index.index 5678 , d2 ; err != nil { log.fatal err } query := bleve.newmatchquery a@example.com b@example.com search := bleve.newsearchrequest query search.fields = string{ name , recipients } searchresults, err := index.search search if err != nil { log.fatal err } // expecting 1 , but returning 2 fmt.printf num results: %d , searchresults.hits.len hit := searchresults.hits 0 fmt.printf %s , hit.fields } in the above example, i'm trying to find an array with an exact match of a@example.com b@example.com , which should only return 1 result; the document with id xyz , but it's returning both documents now. is there a way i can achieve this?