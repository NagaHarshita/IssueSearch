 URL : "https://github.com/seiyria/landoftherair/issues/27" TITLE : new gm commands BODY : there should be gm commands to: forge an item. syntax unknown, but it should allow you to specify all possible item attributes with the command. this will create entirely new, one of a kind items at runtime. create an npc. syntax unknown, but it should allow for the creation of an npc. probably, it will just create an npc based on a name much like @item .