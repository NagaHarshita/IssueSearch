 URL : "https://github.com/realbig/PL_PYIS_MEPR_LTV/issues/8" TITLE : clear transients button BODY : caching from 6 is done via a transient that holds the full query results. the transient is set currently to refresh once per week. a manual override in the form of a flush transients button would be useful. especially if this were set via ajax so that it could refresh the table all at once.