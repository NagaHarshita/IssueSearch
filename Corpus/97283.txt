 URL : "https://github.com/manovotny/sketch-plugin-export-react/issues/8" TITLE : generate a react component BODY : svgo is humming along https://github.com/manovotny/sketch-plugin-export-react/pull/5 , but now we need to turn it into a mountable react component, which means we'll also need to do things like: adding a class to the base svg icon name_of_icon add dynamic attributes to the base svg width height fill convert class to classname convert kebab attribute names to camelcase