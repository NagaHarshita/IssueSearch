 URL : "https://github.com/phalcon/cphalcon/issues/12679" TITLE : model validate mthod BODY : expected and actual behavior currently validate method in model counts messages returned only from validation, ignoring currently appended to model https://github.com/phalcon/cphalcon/blob/master/phalcon/mvc/model.zep l1514 i think this should be changed to count this->_errormessages . let me know what you think, i think this can be bug fix so it will be in 3.1.0, not sure if this can break some applications, but currently implemented way is wrong imho. > provide output if related. provide coredump if any. use https://github.com/phalcon/cphalcon/wiki/generating-a-backtrace as reference php class somemodel extends model { public validation { $validation = new validation ; $validation->add 'name', new presenceof ; if empty $this->price { $this->appendmessage new message price , error ; } return $this->validate $validation ; } } $test = new somemodel ; $test->name = xyz ; var_dump $test->create ; // will return true