 URL : "https://github.com/kinvolk/stage1-builder/issues/3" TITLE : circleci & rkt fetch BODY : capturing https://github.com/kinvolk/stage1-builder/pull/1 issuecomment-274869617 > - integrate with circleci for automatic builds > - save the aci in the circleci build artifact store > - update https://kinvolk.io/aci/ so that rkt fetch automatically fetches the aci from the circleci artifact store. the manifest.tmpl.json name will need to match.