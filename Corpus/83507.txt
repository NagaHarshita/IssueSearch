 URL : "https://github.com/therecipe/qt/issues/407" TITLE : casting qobject to widget BODY : some times when want to convert qobject to widget for example: go layout := widgets.newqhboxlayout parent := layout.parent if parent == nil { return nil } else if parent.iswidgettype { pw := widgets.qwidget parent return pw } above code does not works. tpye asserting does not works also. what should i do?