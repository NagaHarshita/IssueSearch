 URL : "https://github.com/steveklabnik/rustdoc/issues/134" TITLE : flag: configuration flags BODY : this issue is part of https://github.com/steveklabnik/rustdoc/issues/125 --cfg : passing configuration flags using this flag looks like this: bash $ rustdoc src/lib.rs --cfg feature= foo this flag accepts the same values as rustc --cfg , and uses it to configure compilation. the example above uses feature , but any of the cfg values are acceptable. ---------------------------------------------------- should we keep this flag or not?