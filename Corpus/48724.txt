 URL : "https://github.com/stipsan/epic/issues/881" TITLE : simplify socket logic BODY : instead of complicated logic, simply emit to game channels. the notifications only tell other subscribers that another socket mutated the game state, there's no need for any security or complicated logic around that.