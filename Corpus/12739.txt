 URL : "https://github.com/cdnjs/cdnjs/issues/10257" TITLE : request add alex BODY : library name: alex git repository url: https://github.com/wooorm/alex npm package url optional : alex note that there seems to be no pre-built dist files in both git repo and npm package yet, but looks like it supports browser runtime in its package.json, so maybe we can just ask the author for the possibility of providing official builds.