 URL : "https://github.com/deeplearning4j/libnd4j/issues/409" TITLE : cuda build issue on mac BODY : hi guys, this is not a big deal, but it was suggested that i post an !issue about it on gitter. on cuda 8.0 it expects to use clang and clang++ on mac and seems to run it automatically within its binary, but some of the flags passed to the compiler throw errors, -fopenmp i believe is one such example, they are gcc specific but cuda 8.0 won't allow usage of gcc i used gcc 5.2 to compile everything else with no problems and have a fully operational build for native platform . i tried setting a number of environment variables like cxx and cc and editing the buildnativeoperations.sh information but that will not override the nvcc settings. this looks like it effects both libnd4j and nd4j. i tried to use trick_nvcc=yes but it did not make it use gcc 5, even though everything else was using it. i was pointed to this commit but it does not work: https://github.com/deeplearning4j/libnd4j/commit/6654b3604440540bb10421aebc447061ff6d155b it looks like nvcc directly invokes clang so the only thing i tried was commenting out the -fopenmp setup part of the cmake file, but it compiled a long time before finally giving these errors: /applications/xcode.app/contents/developer/toolchains/xcodedefault.xctoolchain/usr/bin/../include/c++/v1/__config 259 : error: invalid combination of type specifiers /applications/xcode.app/contents/developer/toolchains/xcodedefault.xctoolchain/usr/bin/../include/c++/v1/__config 260 : error: invalid combination of type specifiers