 URL : "https://github.com/ulule/limiter/issues/24" TITLE : readme is wrong BODY : docs are wrong. // or use a in-memory store with a goroutine which clears expired keys every 30 seconds store := limiter.newmemorystore prefix_for_keys , 30 time.second this should just read: // or use a in-memory store store := limiter.newmemorystore // alternatively, use a in-memory store with some extra configuration store := limiter.newmemorystorewithoptions ??? something like that anyway.