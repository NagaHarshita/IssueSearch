 URL : "https://github.com/asmodehn/pyros-setup/issues/46" TITLE : how to call pyros_setup ? BODY : we need to find a better way to use pyros_setup. currently the user is supposed to do : python try: import something except importerror: import pyros_setup pyros_setup.configurable_import .configure .activate import something the main drawback with this is that if an import trigger an error, which is not related with setting up ros, it will be hidden by a further error, potentially related with pyros_setup...