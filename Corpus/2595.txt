 URL : "https://github.com/marcobambini/gravity/issues/104" TITLE : built in sort BODY : it might be useful to have some kind of built in sorting algorithm for things like lists and maybe a reverse : swift var list = 2, 4, 1, 5, 3 list.sort // returns 1, 2, 3, 4, 5 list.sort .reverse // returns 5, 4, 3, 2, 1 if this is something that may make it's way into gravity it is probably worth thinking about what would happen in the event that a list contains numbers and strings, or if we want other things to be sorted as well, such as maybe a list of objects sorted by a specific field. also what sorting algorithm would be used. i have heard that for example, python uses timsort.