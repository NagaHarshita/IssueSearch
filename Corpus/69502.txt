 URL : "https://github.com/aio-libs/aiohttp-session/issues/221" TITLE : testing data in session BODY : hello, tell me please, how i get session in test? for example, check last_visit is not none from https://github.com/aio-libs/aiohttp-session/blob/master/demo/redis_storage.py ? python async def test_last_visit client : resp = await client.get '/' assert resp.status == 200 how get session? thanks