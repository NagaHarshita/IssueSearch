 URL : "https://github.com/golang/go/issues/21737" TITLE : proposal: defer functions BODY : hello, i would like to propose defer func. example: when a struct pointer is created, any defers that are registered to that struct through its struct interface would be executed. type mystruct struct { id int } defer func ms mystruct { //...code } thank you,