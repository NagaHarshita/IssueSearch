 URL : "https://github.com/fable-compiler/Fable/issues/787" TITLE : prevent stack overflows BODY : related to https://github.com/microsoft/visualfsharp/issues/2682 issuecomment-290769571 the transformexpr functions in fsharp2fable and fable2babel are called recursively many times, we should prevent to have deep stacks with these. two measures we can take: - make those functions are slim as possible, refactoring most of the code to helpers. - trick from paket: create a data type that contains all parameters of the recursive call create a system.collections.generic.stack put your first call into that stack use a while not stack.isempty do pop from stack execute that bit of work for every original recursive call put on element on the stack