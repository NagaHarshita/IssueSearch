 URL : "https://github.com/react-native-community/react-native-tab-view/issues/262" TITLE : api for selecting pager BODY : i am experiencing problems with tab view on android. tabs are placed in parent scrollview and scroll view height is programatically changed with tab index change. on ios everything works great but on android it's buggy, tabs changes are weird. by using tab tabviewpagerpan it's working great but for using it on android i need to change switch statement. api is needed to use custom tab view pager.