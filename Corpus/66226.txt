 URL : "https://github.com/smi2/tabix.ui/issues/41" TITLE : mention chproxy in docs BODY : chproxy https://github.com/vertamedia/chproxy provides the following features to tabix : - chproxy may accept requests from readonly=1 users if allow_cors config is set. there is no need in add_http_cors_header hack. - chproxy supports https https://github.com/vertamedia/chproxy authorize-users-by-passwords-via-https out of the box, so tabix can send requests over untrusted networks without worrying that passwords may be intercepted.