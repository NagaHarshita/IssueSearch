 URL : "https://github.com/httpswift/swifter/issues/254" TITLE : cors for files BODY : i have looked at the other issues that involve adding cors to the server but they didn't help. one of the other issue's answer was the following, is there any way to use the .sharedirectory features with it? server /cors = { _ in return .raw 200, ok , access-control-allow-origin : , { writer in try? writer.write uint8 hello .utf8 } }