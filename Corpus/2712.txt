 URL : "https://github.com/ArlonMoreira/data/issues/1" TITLE : data m phyton BODY : biblioteca utilizada para obter arquivos csv from pandas import read_csv biblioteca utilizada para obter knn from sklearn.neighbors import kneighborsclassifier biblioteca de acuracia from sklearn.metrics import accuracy_score biblioteca para testar os dados from sklearn.model_selection import train_test_split biblioteca para calcular o recall from sklearn.metrics import recall_score biblioteca para calcular a precisão from sklearn.metrics import precision_score gerando arquivo csv car = read_csv 'carevaluationdatabase.csv' separa os atributos preditivos em x car.data = car 'precodecompra','precodemanutencao','portas','pessoas','capacidadebagageiro','seguranca' separa atributo classe em y car.target = car 'class' separa % dos dados para testar x_train, x_test, y_train, y_test = train_test_split car.data, car.target, test_size=0.50, random_state=42 cria uma instancia da classe kneighborsclassifier com os parâmetros setados em default, n_neighbors=5 knn = kneighborsclassifier 9 cria o modelo de classificacao knn.fit x_train,y_train testa o modelo nos dados de teste aqui ele vai predizer informar a classe .... predicted = knn.predict x_test calcula a acuracia do modelo acc = accuracy_score y_test, predicted calcula o recall recall = recall_score y_train, y_test, average=none calcula a precisão precision = precision_score y_train, y_test, average=none print acurácia: print acc print 'recall:' print recall print 'precisão:' print precision