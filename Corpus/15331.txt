 URL : "https://github.com/madisonhicks/blob/issues/14" TITLE : define orm models BODY : in the blob_server package, create a new python module called models.py . here is where we will define the orm classes that will describe our database. for this issue, you'll want to define classes to implement the data model described in 9. to see some introductory information on sqlalchemy orm classes, check out the declare a mapping http://docs.sqlalchemy.org/en/latest/orm/tutorial.html declare-a-mapping section of sqlalchemy's orm tutorial. for a real world example, see my blog's data models https://github.com/mplanchard/mplanchard/blob/master/mplanchard_server/models.py you can ignore all the schema stuff in there for now . - create a class for the blog_posts table with appropriate column attributes - create a class for the tags table with appropriate column attributes - create a many:many table linking the two see basic relationship patterns http://docs.sqlalchemy.org/en/latest/orm/basic_relationships.html basic-relationship-patterns at the sqlalchemy docs