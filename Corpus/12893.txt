 URL : "https://github.com/neovim/neovim/issues/6966" TITLE : shada slow down? BODY : - nvim --version : .2.1 - vim version: behaves differently? na - operating system/version: arch ju 2017, - terminal name/version: urxvt - $term : xterm-256color actual behaviour working with a file which has large shada history seems gets slower. this is especially noticeable with save to disk operation, as auto save eventually becomes unusable. it might be also dependent on the contents of shada, as the behavior is not quiet consistent. wiping out shada seems to reset things back to normal. expected behaviour no slow down, as shada history gets larger. steps to reproduce using nvim -u norc not sure. nvim -u norc