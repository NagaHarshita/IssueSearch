 URL : "https://github.com/Foliotek/Croppie/issues/416" TITLE : reduce image size output BODY : hello guys, i'm using the latest version of croppie and having a hard time with this. the most weird thing is that, 'size' property only works with original , if i change that, my script stop working. any help, i'll be glad. cheers. $ document .ready function { var $uploadcrop; function readfile input { if input.files && input.files 0 { var reader = new filereader ; reader.onload = function e { $uploadcrop.croppie 'bind', { url: e.target.result } ; $ '.upload-demo' .addclass 'ready' ; } reader.readasdataurl input.files 0 ; } } $uploadcrop = $ ' upload-demo' .croppie { enableexif: true, // url: 'storage/products/no.png', viewport: { width: 200, height: 200, type: 'square' }, boundary: { width: 200, height: 200 } } ; $ ' myfile' .on 'change', function { readfile this ; } ; $ '.form_submit' .on 'click', function ev { $uploadcrop.croppie 'result', { type: 'canvas', // size: 'original', format:'jpeg', size: { width: 450, height: 450 } } .then function resp { $ ' imagebase64' .val resp ; $ ' form' .submit ; } ; return false; } ; } ;