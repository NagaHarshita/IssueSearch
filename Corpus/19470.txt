 URL : "https://github.com/dotCMS/core/issues/12731" TITLE : add content by url BODY : as a stake holder i want user to be able to create content as fast as possible expected behavior when user goes to an url like: http://localhost:8080/dotadmin/ /add/contentname should load jsp to add the specific content. current behavior when user goes to any /add/ url redirect to the first portlet possible solution i think the problem is that the canactivatechild https://github.com/dotcms/core-web/blob/master/src/app/api/services/routing-private-auth.service.ts l34 is not finding the id since the id in this case is add known workarounds go to the url from the + button. steps to reproduce for bugs <!--- provide a link to a live example, or an unambiguous set of steps to --> <!--- reproduce this bug. include code to reproduce, if relevant --> 1. try to go to http://localhost:8080/dotadmin/ /add/banner context with the new canactivatechild method in place we didn't take in consideration this type of route.