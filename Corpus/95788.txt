 URL : "https://github.com/viewflow/django-material/issues/295" TITLE : django admin customisation BODY : i have a django admin which has a list view and on click of the list item, the detail view appears. i want to customize how the layout looks. how do i do that? i tried using the layout field in my models field like below layout = layout 'tail_number', 'schedule_id', 'flight_group_code', 'scheduled_days', row 'origin_port_code', 'destination_port_code' , row 'start_date', 'end_date', 'flight_departure_time' but this doesn't change my layout in the page. how do i customize the admin site looks ? is it do able? if you could provide me an example it would be of great help.