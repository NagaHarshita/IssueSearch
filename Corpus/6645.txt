 URL : "https://github.com/matty22/SurveyTool/issues/19" TITLE : app needs tests BODY : what is the current behavior? ---- the app currently has no tests what is the expected behavior? ---- the app should have tests if this issue is a question or feature request, what is the motivation / use case for changing the current behavior? ---- tests will ensure that new additions do not break current functionality