 URL : "https://github.com/PhaedrusTheGreek/nagioscheckbeat/issues/4" TITLE : guide for setup BODY : do you have a setup guide? working in a docker container, installed go, and glide. go setup docs say to set gopath to the project: export gopath=/opt/nagioscheckbeat-master glide installation requires a bin and src directory: mkdir /opt/nagioscheckbeat-master/bin ; mkdir /opt/nagioscheckbeat-master/src glide is now installed so build: bin/glide create bin/glide install --strip-vcs this downloads the source into the src/github.com so now i have /opt/nagioscheckbeats-master with all the files from the zip, and a second copy in /opt/nagioscheckbeat-master/vendor/github.com/phaedrusthegreek/nagioscheckbeat during go installation i set goroot to /usr/local/go, so the downloaded source is expected under this directory, so copy files over: cp -r /opt/nagioscheckbeat-master/vendor/ /usr/local/go/src/. so trying to run the main.go: go run main.go github.com/phaedrusthegreek/nagioscheckbeat/beater beater/nagioscheckbeat.go:18: undefined: publisher.client