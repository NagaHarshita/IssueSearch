 URL : "https://github.com/NSI-IT/ATG/issues/49" TITLE : admin - add status BODY : as an admin user, i can add new status so that i can update the status list. acceptance criteria verify that admin will provide following details while adding a new status: > status name e.g. empty, loaded, loading, etc. mandatory verify that if any mandatory field is left blank, a message fields are mandatory displayed under the mandatory field. verify that new status gets added to the status list. verify that status name gets updated for front end users.