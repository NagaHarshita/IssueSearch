 URL : "https://github.com/CogRob/catkin_grpc/issues/23" TITLE : protobuf needs zlib BODY : the ros official build does not have zlib, so protobuf is compiled with have_zlib=0, which disables gzip stream. we should make this package depend on zlib