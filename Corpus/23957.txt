 URL : "https://github.com/oraclesorg/test-templates/issues/9" TITLE : rights and folders BODY : by default azure downloads and runs installation script in /var/lib/waagent/custom-script/download/0/ under root . by default root doesn't have a password, i.e. you can run sudo any-command without password. 1. should we move all necessary files to /home/$admin_username and change files ownership to $admin_username ? 2. should we make a password for sudo and ask it in cloud ui? 3. should we run docker as non-root that is still equivalent to root https://docs.docker.com/engine/installation/linux/linux-postinstall/ ?