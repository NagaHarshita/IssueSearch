 URL : "https://github.com/kdaveed/masterThesisFramewrok/issues/7" TITLE : initial form data loading BODY : right now the form was responsible on the decision based on the objecturi variable if the form was in edit mode or not. right now i think it were more elegant if only one ajax call were sent, and the server the prepares the response according to that if objecturi were in the initial response. the initial response consists of the following fields: formdescriptor dependencies independentvariables formdata the server returns the first three every time, but the formdata comes only if the initial request contained the parameter objecturi.