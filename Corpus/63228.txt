 URL : "https://github.com/chriscauley/under-construction/issues/9" TITLE : diffing tests in editor BODY : one thing that could be cool is build a quasi diff interface in the editor. say someone uses /test.js as their test file. they can view this in the editor just as if their tests were stored in local storage. if they edit this file in browser and click run it refreshes the page and runs the in memory test. when it loads /tests.js, it is diffed against the in browser version of the test. if they have diverged, show them so that the person can make edits to their file system. this persistent console edit feature would be fun for css too.