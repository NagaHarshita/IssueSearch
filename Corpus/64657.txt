 URL : "https://github.com/defuse/php-encryption/issues/359" TITLE : hash_pbkdf2 is too slow BODY : hi, php hash_pbkdf2 is slower when the number of iterations are bigger, which in some cases not neccessary. const pbkdf2_iterations = 100000; makes it around 180ms in my case and it is too slow, at least for my case. 10000 makes it around 20-30ms. it would be better, if we could change the iteration count programmatically for performance reasons in different scenarios.