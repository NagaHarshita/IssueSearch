 URL : "https://github.com/gchq/gaffer-tools/issues/251" TITLE : create query page BODY : create a query page which will: 1. select an operation 2. customise the view if applicable 3. fill in parameters if applicable 4. execute the operation this should be a single page - not split up with tabs. it should go through much the same steps and have a similar feel to the query builder minus the tabs . feel free to rip code from the query builder to complete this page.