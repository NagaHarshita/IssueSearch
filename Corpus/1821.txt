 URL : "https://github.com/madskristensen/BundlerMinifier/issues/293" TITLE : how do i get BODY : installed product versions - visual studio: example 2017 professional - this extension: example 2.4.353 description when bundling together more than one file with source map for example some js generated by typescript , the output bundle only contains source map for the first file bundled. steps to recreate 1 create a project 2 add two typescript files 3 create a bundle with both enabling source map 4 create a web page that references the bundle 5 run the page with f12 tools and try to debug it current behavior the browser debugging tool will resolve only the symbols of the last added file to the bundle the last line with // sourcemappingurl= expected behavior the browser debugging tool should resolve the symbols of all the file bundled.