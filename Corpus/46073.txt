 URL : "https://github.com/mitchcurtis/slate/issues/6" TITLE : layers are all screwy BODY : 1. copy an image from somewhere 2. create a new layered image project with the dimensions of that image 3. create a new layer and make it the active layer 4. paste the image into that layer 5. rename the layer to tiles 6. rename the first layer to bg 7. hide the tiles layer; the bg layer will be hidden instead 8. unhide the tiles layer; the by layer is still hidden