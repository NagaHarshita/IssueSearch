 URL : "https://github.com/tfbirk/video-player/issues/2" TITLE : set max height BODY : the max width=100%, which causes the height to extend past the screen try big bunny buck in a browser . need to set a max height. there is a css style for max height.