 URL : "https://github.com/istio/manager/issues/601" TITLE : compress cluster names BODY : envoy set a hard limit of 60 characters to the cluster name which must be unique per proxy. a typical cluster names looks like this: outbound:world.default.svc.cluster.local:http:version=v0 we need to update config gen to hash these names. this will likely make stats difficult to interpret without some reverse mapping. @costinm since you're a heavy user of stats