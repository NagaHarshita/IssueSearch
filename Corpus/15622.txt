 URL : "https://github.com/phstc/shoryuken/issues/334" TITLE : extra boot config BODY : where is the best place to put extra boot config these days: ruby shoryuken.configure_server do |config| config.aws = { secret_access_key: env aws_secret_access_key , access_key_id: env aws_access_key_id , region: env aws_region , receive_message: { wait_time_seconds: ruby_env == production ? nil : 5, attribute_names: %w approximatereceivecount senttimestamp } } end i understand we can just remove the specific env references now, but where do we put the receive_message config?