 URL : "https://github.com/slashquery/slashquery/issues/3" TITLE : proxy the full path BODY : when having this: ~: url: http://localhost or ~: path: / a request to http://proxy/foo will proxy all to --> http://localhost/ and will ignore the /foo path, find a way to forward everything or give the option to pass everything only when using probably something like: path: / and only to / when no is defined: path: /