 URL : "https://github.com/lampepfl/dotty/issues/3658" TITLE : hk type aliasing bug BODY : scala object app { def main args: array string : unit = { trait modulesig { type f _ type type = f def subst f _ _ fa: f list : f type } val module: modulesig = new modulesig { type f a = list a def subst f _ _ fa: f list : f type = fa } } } doesn't compile, but if you change scala type f a = list a to scala type f = list it does...