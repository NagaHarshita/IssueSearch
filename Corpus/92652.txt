 URL : "https://github.com/cyrusimap/cyrus-sasl/issues/473" TITLE : ./configure --help summary BODY : print $dbpath in the summary and take into account that $sasl_mechs always starts with a space. diff diff --git a/configure.ac b/configure.ac --- a/configure.ac +++ b/configure.ac @@ -1612,8 +1612,9 @@ echo echo $package - $version echo echo prefix....................: $prefix -echo plugins...................: $sasl_mechs +echo plugins...................:$sasl_mechs echo saslauthd support.........: $with_saslauthd +echo db path...................: $dbpath echo java support:.............: $enable_java if test $enable_java = yes; then echo java base.................: $java_includes