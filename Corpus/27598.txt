 URL : "https://github.com/zerodayz/citellus/issues/153" TITLE : doc common-function BODY : actual behavior add description for is_lineinfile - is_lineinfile $file $pattern -- returns true if pattern match is found in file false if wasn't found. - example: ~~~sh if is_lineinfile $config_file ^debug \t = \t true ; then echo debug enabled. ; fi ~~~ expected behavior steps to reproduce the behavior