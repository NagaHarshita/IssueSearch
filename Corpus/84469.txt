 URL : "https://github.com/EmpireProject/Empire/issues/727" TITLE : data encoding in rest api BODY : empire version 2.1 os information linux flavor, python version ubuntu 16.04, python 2.7 expected behavior and description of the error, including any actions taken immediately prior to the error. the more detail the better. json input through the rest api is being stored as unicode data. when the unicode string is added to a normal string the result is a unicode string at least with python 2.7 . for an example create a listener through the rest api post /api/listeners/http and set the host option. the error manifests itself when trying to communicate with a stager i was using osx/launcher screenshot of error, embedded text output, or pastebin link to the error any additional information