 URL : "https://github.com/benoitc/gunicorn/issues/1457" TITLE : sigterm not so graceful BODY : i was trying to get some code coverage for my application and was failing. while investigating, i found the problem: the worker processes are never gracefully shutdown. according to the doc: > term: graceful shutdown. waits for workers to finish their current requests up to the graceful timeout. the worker_int callback is never called in my code. if you look at the code, this is not what's being done: https://github.com/benoitc/gunicorn/blob/master/gunicorn/arbiter.py l259 shouldn't we add a line like that in arbiter.handle_term before raising the exception? self.stop true