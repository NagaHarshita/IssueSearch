 URL : "https://github.com/cakephp/cakephp/issues/11562" TITLE : paginator used finder BODY : this is a multiple allowed : x bug enhancement feature-discussion rfc paginator.php false if empty $query { $query = $object->find $finder, $options ; } else { $query->applyoptions $options ; } true if empty $query { $query = $object->find $finder, $options ->applyoptions $options ; } else { $query->applyoptions $options ; } ?