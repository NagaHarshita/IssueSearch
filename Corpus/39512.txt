 URL : "https://github.com/Leaflet/Leaflet.markercluster/issues/763" TITLE : show coverage always BODY : hi there, im looking for a way to show the bounds of a cluster permanently, not only on mouse over. i've tried to achieve this by modifying the iconcreatefunction . i tried to generate a polygone around the shown clusters. but in this case every cluster wich is inside the shown cluster gets marked by a polygone, too. var markers = l.markerclustergroup { showcoverageonhover: false, iconcreatefunction : function cluster { var val = 0; for i = 0; i < cluster.getallchildmarkers .length; i++ { val = val + parseint cluster.getallchildmarkers i .options.speed } var avg = val / cluster.getallchildmarkers .length; avg = math.round avg 10 / 10; if cluster.polygon { map.removelayer cluster.polygon ; } if cluster.getchildcount > 2 && cluster !== this._spiderfied { cluster.polygon = new l.polygon cluster.getconvexhull ; map.addlayer cluster.polygon ; }