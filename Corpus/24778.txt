 URL : "https://github.com/ranska/ranska_dotfiles/issues/2" TITLE : tmux mouse options BODY : tmux has been updated, mouse option are no mroe the same ; . activate mouse set-option -g mouse on wheel up/down on each pane bind -n wheeluppane if-shell -f -t = {mouse_any_flag} send-keys -m if -ft= ' {pane_in_mode}' 'send-keys -m' 'select-pane -t=; copy-mode -e; send-keys -m' bind -n wheeldownpane select-pane -t= \; send-keys -m