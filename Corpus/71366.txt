 URL : "https://github.com/mozilla/something-awesome/issues/117" TITLE : add dockerflow endpoints BODY : we should add a /__heartbeat__ endpoint that touches s3 and redis, and returns 500 if unreachable, and emits a statsd metric like heartbeat.pass and heartbeat.fail for monitoring we should add a /__lbheartbeat__ that just returns 200 we should add a /__version__ endpoint that responds with a json object that has the fields commit , source , and version