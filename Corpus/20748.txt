 URL : "https://github.com/cptechinc/soft-crm/issues/32" TITLE : create quote class BODY : switch to the master branch, then make a branch call it quote-order-classes if it doesn't exist all ready you may want to look at useraction.class.php as an example, it uses the table useractions the classes you make will be in the vendor/cptechinc/dpluso-processwire-classes folder create the quote.class.php file in the file the class will be called quote class quote - uses all the fields from ordrhed - custom fields: canedit preset to true, hasnotes preset to false - _init function needs to set these fields - if notes == 'y', hasnotes = true