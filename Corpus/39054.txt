 URL : "https://github.com/yannickl88/features-bundle/issues/5" TITLE : replace method calls BODY : service definitions that use factories can't afford to use the arguments key to do injection. a good solution might be to replace method call arguments. this will have the side effect to make the feature as an optional dependency possible.