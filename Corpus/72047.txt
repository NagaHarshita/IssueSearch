 URL : "https://github.com/frhagn/Typewriter/issues/230" TITLE : attribute.value is empty BODY : hi, i'm trying to use method.attributes.value and it's returning an empty string. it seems to be a new bug as i had code using it before that worked. the code below is my test setup. string getattribute method m { if m.attributes.any { return string.join | ,m.attributes.select q=>q.value ; } return any ; } i have been using this method to determine the return type of my webapi code, using the producesresponsetype attribute. an example is below. httpget producesresponsetype typeof usagemodel , 200 public async task<iactionresult> getcurrentusage fromquery sguid id { is there another work around i could use?