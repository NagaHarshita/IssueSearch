 URL : "https://github.com/TxUniverse/tx-base-website/issues/2" TITLE : clean up warning BODY : - the module with address http://localhost:8080/node_modules/can-zone/register.js is being instantiated twice. this happens when module identifiers normalize to different module names. modules: node_modules/can-zone/register - http://localhost:8080/package.json can-zone@0.6.13 register - http://localhost:8080/node_modules/can-zone/lib/zone.js hint: import the module using the ~/ modulepath identifier. learn more at https://stealjs.com/docs/modulename.html and https://stealjs.com/docs/tilde.html - warn: state is not in the current scope, so it is being read from the parent scope. this will not happen automatically in an upcoming release. see https://canjs.com/doc/can-stache.scopeandcontext.html preventingscopewalking. - warn: slug is not in the current scope, so it is being read from the parent scope. this will not happen automatically in an upcoming release. see https://canjs.com/doc/can-stache.scopeandcontext.html preventingscopewalking. - warn: can-stache/src/expression.js: unable to find key or helper blogpost .