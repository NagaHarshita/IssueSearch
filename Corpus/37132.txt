 URL : "https://github.com/nikomatsakis/nll-rfc/issues/11" TITLE : kinds of constraints BODY : the text says > the first set of constraints that we generate are derived from liveness. specifically, if a lifetime l is live at the point p, then we will introduce a constraint like: > > l: {p} @ p however, that kind of constraint doesn't even appear in the constraint grammar, which only lists inclusion constraints. either this is written in a confusing way, or the grammar is incomplete.