 URL : "https://github.com/opennota/findimagedupes/issues/4" TITLE : problem with spaces BODY : gave it a try and liked it a lot, thanks! anyway, ran into some problems with filenames containing spaces, from what i gather you output the duplicates as: path1<space>path2<newline> ... which gave me problems when i wanted to pipe the filenames to other programs, like mv/cp etc, because if the filenames have spaces, then i can't use the space as a delimiter, and if i use the newline as a delimiter, i will get two paths instead of one and if the filenames have spaces i can't separate the two paths . so is there any chance you could add an option that outputs each path followed by a newline ?