 URL : "https://github.com/calebj/calebj-cogs/issues/26" TITLE : duel error cooldown BODY : if you input a command wrong, or hit someone on protected list, and you have a cooldown enabled i do, as 60 seconds to avoid too much spam - it still triggers cooldown. so whether the command is performed successfully or not, it activates the cooldown. possible to only have cooldown trigger on successful commands, where the duel is physically initiated