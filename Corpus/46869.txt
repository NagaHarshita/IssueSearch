 URL : "https://github.com/eclab/ecj/issues/14" TITLE : neat loadsensors bias BODY : jorge gomes <jorgemcgomes@gmail.com> writes: > as you are probably aware, when using loadsensors in neatnetwork , the bias 1 also has to be passed. > this is not intuitive at all. there’s a comment in neatnetwork class, loadsensors method, that suggests to me this is some kind of bug: > > // only load values into sensors not biases > if inputs.get i .type == nodetype.sensor > > however, bias es also have the nodetype.sensor , so it really doesn’t exclude them. > the loadsensors method results in an arrayoutofbounds if the bias is not provided in the array.