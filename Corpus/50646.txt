 URL : "https://github.com/monica001/leetcode/issues/19" TITLE : 541. reverse string ii BODY : 1.這道題不難，卡在subsring很久，以為它壞了， 如果starindex超過長度，會回傳空字串，程式不會炸掉 用法一： string.substring 方法 startindex, length 用法二：string.substring方法 startindex 2. stringbuilder的效能這麼好，以後串字都用它 3. 字串的reverse方法，要用array.reverse 去反轉它 4. 其它作法https://my.oschina.net/liyurong/blog/1204673 public string reversestr string s, int k { stringbuilder str = new stringbuilder ; int len = s.length; // 要切成幾段，有餘數的話+1 int times = len / k +1; for int i = 0; i < times; i++ { int start = i k; int end = i+1 k-1; string temp = ; if end < len { temp = s.substring start, k ; } else { // 最後一段字 temp = s.substring start ; } if i % 2 == 0 { // 要reverse char add = temp.tochararray ; array.reverse add ; str.append new string add ; } else { // 不用reverse str.append temp ; } } return str.tostring ; }