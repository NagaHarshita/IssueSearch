 URL : "https://github.com/janfreyberg/niwidgets/issues/3" TITLE : make background black BODY : hi, cool widget you built! perhaps the widget could improve by making the background black? at this moment, the background has the color of the color corresponding to 0 given a certain colormap. one idea is to use the numpy.masked_where function, like this: np.ma.masked_where data == 0, data assuming that the background, i.e. voxels outside the brain, are coded zero. should i submit a pr? if so, should this be the default, or accessible through a parameter upon initialization or upon called nifti_plotter ?