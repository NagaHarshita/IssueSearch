 URL : "https://github.com/500tech/angular-tree-component/issues/442" TITLE : ngmodel update tree nodes BODY : hi, how you are handling model update using ngmodel or form. what is the right approach?, i have an input text and and a multi select box value on each node. i thought of ng model it didnt work. <tree-root tree nodes = nodes options = options > <ng-template treenodetemplate let-node let-index= index > <app-context ngmodel = node.data.name name= name{{node.data.id}} node = node.data ></app-context> </ng-template> </tree-root> and on submit button i tried to access the node json object variable, but new nodes created and edits happened are not reflecting. addnew { this.nodes.push { id: 0, name: 'new node', type: 'usecase', children: } ; this.tree.treemodel.update ; } savetree { console.log this.tree.treemodel ; } any previous links or any doc reference will be helpful thank you