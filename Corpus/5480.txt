 URL : "https://github.com/webautotester/docker_compose/issues/21" TITLE : plugin : input BODY : when the input field is already set and when the user erase the value before to input a new one, the plugin still save the old value and concat the new value. ex: by default username is set to : anonymous using the plugin, the user delete anonymous and types his/her name : xavier the plugin records anonymousxavier