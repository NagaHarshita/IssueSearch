 URL : "https://github.com/RowanACM/TeamLinguini/issues/2" TITLE : card background colors BODY : okay so i did some more formatting today. the card elements should have a background color that is the same as the header. cards also have 2 div elements inside. a left div that will float on the left and contain an image of the person over their name. then there is a descript element that will float on the right side of the card and will be a description of the person. for some reason, floating these 2 elements within the card is breaking the background color of the card, even though rest of the formatting is correct. commenting out only the left float, means that only the left element gets the correct color and the formatting is messed up. commenting out only the right float, means entire card gets the correct color, but the formatting is still not correct obviously.