 URL : "https://github.com/Dogfalo/materialize/issues/4680" TITLE : modal repeats ajax calls BODY : description hi, i have an issue with modal component, basically, i create a modal, and open in a button click, and everything works fine, but, inside this modal, in the footer, i have a button to send an ajax post request, and the first time it works fine, and the if everything works okay, i close the modal. but the problem that starts when you click the button again to open the modal and then the post ajax request, send two requests, and if you click again, the modal sends 3 ajax requests, and so it does every time i open the modal, increase the count, and make that number of requests, i want to think that when the modal is closed, it does not remove it from the scope, and always creates a new one, and therefore, when i click, bubble click on all the modal buttons and that causes, so many requests. there is my code: $ '.modal' .modal { dismissible: true, ready: function modal, trigger { $ '.modal-create-task' .on 'click', function e { var el = $ this ; var texttask = $ '.new_task' .val ; if texttask != { el.addclass 'disabled' ; $.post posturl , { variable: , }, function data, status { if data.result == success { // api respond correctly var taskid = data.data.id; console.log id del task: + taskid ; $ '.body-navegation' .append <div class=\ task\ > + <p> + <input type=\ checkbox\ class=\ filled-in\ id=\ + taskid + \ /> + <label for=\ + taskid + \ > + texttask + </label> + </p> + </div> ; el.removeclass 'disabled' ; $ '.new_task' .val '' ; } else { alert error ; } } ; } } ; }, complete: function { //alert 'closed' ; } // callback for modal close } ; thank you so much, for everything.