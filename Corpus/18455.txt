 URL : "https://github.com/bloomberg/chef-bach/issues/776" TITLE : test cases for ha BODY : 1. during initial cluster build the cluster is built with two namenodes running in ha mode 2. after the cluster is build, a third head node journalnode , zookeeper is added successfully. 3. after addition of third head node existing cluster is converged successfully and new head node is added to configuration files on existing head nodes 4. a journalnode head node can be destroyed and restored successfully 5. a namenode head node can be destroyed and restored successfully 6. both the namenode can switch roles i.e. primary can be re-installed as standby and standby can be reinstalled as primary