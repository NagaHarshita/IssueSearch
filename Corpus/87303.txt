 URL : "https://github.com/freifunk-bielefeld/firmware/issues/36" TITLE : kein ipv4/dhcp über mesh BODY : mit dem aktuellen dev-branch funktioniert ipv4 bzw. dhcp nicht. die clients bekommen keine ip zugewiesen. an den gateways liegt es nicht, ich habe das mit mehreren gateways versucht. batman-adv hab ich noch nicht debuggt, weil auf meinem wr703 anscheinend kein platz mehr für batctl war und ich die debugpfade nicht auswendig kenne um das manuell auszulesen. ipv6 dagegen ist kein problem. wenn ich mir statisch eine ipv4-adresse gebe, geht es auch.