 URL : "https://github.com/cs361fall2017/team36/issues/18" TITLE : columns and cards BODY : we need to have each card as a button. which sits inside a column. don't worry about what clicking the button does, just have the button exist in its own column. an empty column is just one without buttons. each button should have its number and a utf8 symbol for the suit.