 URL : "https://github.com/Maatwebsite/Laravel-Excel/issues/1173" TITLE : bug edit existing data BODY : package version, laravel version 5.4 expected behaviour when looping each row from a file and edit its details, the result export file should contain the updates. actual behaviour when looping a csv file inside the load callback, the row updates aren't persisted in the exported file. exception stack trace there's no exception, the expected logical output is wrong. screenshot of excel file the csv column updated still having the same data that the loaded file after updating it in the callback. steps to reproduce the behaviour upload a csv. load the csv. loop through them by using the each method from the collection either use $obj->put method or $obj->key property to set the new value export the resultant csv in the same format.