 URL : "https://github.com/Flaxximillion/helper/issues/22" TITLE : build simple calendar view BODY : working in ./public/caltest.html currently using fullcalendar.io, which seems to be exactly what we need. tasks:
- try attaching a button or link to a specific date hard-coded - try pulling in dummy data as an object and using to dynamically attach tasks to a single date
- the same as previous but across multiple dates