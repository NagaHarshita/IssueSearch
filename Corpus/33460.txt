 URL : "https://github.com/subutai-io/tray/issues/376" TITLE : add proper changelog BODY : tray doesn't have root changelog file, but it uses one for debian package. we need to start using root changelog file and generate changelog in debian format during tray packaging. you can take changelog from p2p as an example: https://github.com/subutai-io/p2p/blob/master/changelog changelog converter to debian format is also exists in p2p inside a configure script: https://github.com/subutai-io/p2p/blob/master/configure example of proper debian changelog is here: https://github.com/subutai-io/tray/blob/master/deb-packages/deb-packages-internal/debian/changelog