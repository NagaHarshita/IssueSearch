 URL : "https://github.com/tjoskar/ng2-lazyload-image/issues/114" TITLE : ionic2 custom component BODY : if i have custom angular2 component, for example custom user card, then all lazyload images loads instanly. for example there is a page with scrollable area and 100 user card components, when i open the pages all lazyload images loads immediately, but it should load only then i scroll to them.