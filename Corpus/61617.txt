 URL : "https://github.com/QISKit/qiskit-sdk-py/issues/49" TITLE : better offline support. BODY : we should allow the usage of the sdk in offline scenarios. this will restrict the user to deal with local simulators only. if the user tries to access some online feature, we will throw a controlled exception. current behavior if there's no internet connection, the quantumprogram class will throw and exit. steps to reproduce for bugs <!--- provide a link to a live example, or an unambiguous set of steps to --> <!--- reproduce this bug. include code to reproduce, if relevant --> 1. switch off networking 2. run your program which highly probable uses quantumprogram class 3. program stops because of connection related exceptions. your environment <!--- include as many relevant details about the environment you experienced the bug in --> version used: 0.3.4 environment name and version e.g. python 3.6.1 : all supported versions operating system and version: all supported versions