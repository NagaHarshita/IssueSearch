 URL : "https://github.com/Sly777/ran/issues/70" TITLE : project schema avaiable? BODY : hey @sly777 can you add the type only schema so it's a straight forward map to another backend? i tried to use get-graphql-schema endpoint_url > schema.graphql but there was so much information in there i found it hard to drill down to just the type only information you'd need to create a project.graphcool file