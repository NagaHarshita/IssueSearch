 URL : "https://github.com/atk4/ui/issues/255" TITLE : modal::show arguments. BODY : i wonder if it's difficult to allow to pass arguments to show . those argument would be accessible from $_get inside set: $modal = $app->add 'modal', 'title' => 'title' ; $modal->set function $p use $modal { $p->add 'button', 'hide', $_get 'color' ->on 'click', $modal->hide ; } ; $app->add 'button', 'show' ->on 'click', $modal->show 'color'=>'green' ;