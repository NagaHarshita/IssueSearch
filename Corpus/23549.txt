 URL : "https://github.com/jmoiron/sqlx/issues/320" TITLE : problem with with? BODY : db : postgres the following works just fine if i execute it against the db with hardcoded $1 values. however, using it in my script, no rows are affected. what am i missing? help would be much appreciated. upd := with live as from livedata . broadcasts_public where id = $1 returning insert into livedata . history_broadcasts_public select from live for _, v := range bcasts { // bcasts == slice of strings res, err := f.db.exec upd, v if err != nil { log.printf error executing update query with id %s -> %+v , v, err } // rows affected = 0 .... }