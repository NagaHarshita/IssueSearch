 URL : "https://github.com/szokeptr/homebridge-tradfri-plugin/issues/8" TITLE : bugg with same uuid BODY : i found a bug when i have two groups with a device i each group that has the same name they somehow get the same uuid. like this, two groups i trådfri app, one kitchen and one bedroom in each group there is a device called window. that setup did not work, i had to change the names so they were not the same.