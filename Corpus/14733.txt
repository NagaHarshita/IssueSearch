 URL : "https://github.com/hapijs/inert/issues/91" TITLE : cache into memory BODY : https://www.npmjs.com/package/connect-static > when you create the middleware, it will immediately scan the requested directory, gzip all the files, and save the cache into memory, where it will forever remain. when a request hits the middleware it never touches the file system. in a similar way, i would like to specify per directory handler to cache files in memory or not.