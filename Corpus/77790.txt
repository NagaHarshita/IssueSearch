 URL : "https://github.com/ChurchCRM/CRM/issues/2144" TITLE : number of items in lists BODY : it would be nice to either 1. set a session variable that makes it so when you look at a list ie view all persons, view all families that the session remembers that information so when the page is refreshed you don't need to re select it. or 2. set a user variable in user settings to a default value.