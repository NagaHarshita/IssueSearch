 URL : "https://github.com/Athospd/forkliftr/issues/6" TITLE : discuss about the concept BODY : the package will rely on readr::read_delim function for instance. so, my suggestion is to give focus in it's arguments so we can call it safely after our best guesses about them. my thoughts about each of these arguments until now follows r read_delim file, input delim, guess_delim quote = \ , guess_quote escape_backslash = false, ? escape_double = true, ? col_names = true, ? col_types = null, guess_col_types locale = default_locale , list guess_encoding , ..., decimal_mark = . , grouping_mark = , na = c , na , guess_na_string or optional input? quoted_na = true, ? comment = , ? trim_ws = false, optional input skip = 0, detect_first_row_with_content n_max = inf, optional input guess_max = min 1000, n_max , optional input progress = show_progress optional input is there other issues to be aware of?