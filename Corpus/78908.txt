 URL : "https://github.com/acidrainstudios/TrueReality/issues/66" TITLE : render to texture camera BODY : create a utility camera type that will inherit from the default camera and will have an easy internal setup for rendering to texture and geometry for that texture if needed. it should give the user access to the internal camera, the rendered texture or image, and the geometry node, if the user wanted to have one.