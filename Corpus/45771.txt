 URL : "https://github.com/FriendsOfSymfony/FOSUserBundle/issues/2659" TITLE : integration symfony 4 BODY : i have installed the bundle on symfony4 but there is a bug : i can't register because it says that the csrf token isn't valid, but it's work on login and i can create users from the terminal. i precise that the token is sent with the form and the controller returns false on isvalid function