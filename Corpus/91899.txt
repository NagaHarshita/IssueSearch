 URL : "https://github.com/zalando-incubator/api-discovery/issues/118" TITLE : upgrade to swagger-ui 3.x BODY : what upgrade to swagger-ui 3.x https://github.com/swagger-api/swagger-ui and rewrite server and build chain. why the current implementation of the api-discovery frontend is based on an outdated version of swagger-ui and a somewhat hacked-together server combined with a complicated set of build scripts. this makes the project hard to maintain and improve on, plus we're missing out on new features in upcoming versions of swagger-ui. proposal create a webpack based frontend project pull in the latest swagger-ui as an npm package rewrite the server