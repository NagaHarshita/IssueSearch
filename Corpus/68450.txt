 URL : "https://github.com/vsivsi/meteor-job-collection/issues/220" TITLE : run a recurring job now BODY : i have a job that runs every 10 minutes. if i want to override a single instance of it and run it now i.e. via a button press , how should i do it? i figured the following should work, but to no avail: javascript const job = jobs.findone { 'data.name': name, status: 'waiting' } ; if job { const j = jobs.getjob job._id ; j.ready { time: new date , force: true } ; }