 URL : "https://github.com/47deg/case-classy/issues/64" TITLE : decoder creation from try BODY : it would be useful to be able to create decoders directly from a => try b . the error can be directly turned into the underlying error wrapper. for example, for monix-kafka : scala implicit val decodekafkaproducerconfig: configdecoder kafkaproducerconfig = decoder.instance c => try kafkaproducerconfig c .toeither.leftmap decodeerror.underlying _ this ideally could just be written as: scala implicit val decodekafkaproducerconfig: configdecoder kafkaproducerconfig = decoder.instance c => try kafkaproducerconfig c