 URL : "https://github.com/fvisin/main_loop_tf/issues/20" TITLE : expose useful placeholders BODY : as suggested by @marcociccone val_targets = self.placeholders false 'targets' actual_val_targets = tf.concat val_targets :self.sym_num_devs i guess i should: 1. rename self.placeholders --> self._placeholders or self._per_dev_placeholders 2. store in self.placeholders the placeholders that can be of some use for the end user