 URL : "https://github.com/Pure-D/serve-d/issues/8" TITLE : unknown ddoc macros are BODY : currently, if the documentation parser encounters a macro that isn't a predefined one, what ever is inside that macro is left out of the documentation popup. perhaps not the best solution, but i wonder if it would be better to considered all unknown macros as being equal to $0 so that the text being passed to them is at least visible. some projects will need to rework macros to take this into account perhaps the text they are displaying is not the first macro param , but at least it is a way to get text to appear in the documentation popup.