 URL : "https://github.com/efidroid/projectmanagement/issues/84" TITLE : htc one m8 gpe support BODY : arch: msm8974 oem: htc branch used to build: la.br lk builds fine, and boots with no display. if i used the display dtb option, display works just fine. uefi builds fine, but whether booted or flashed to the boot partition, uefi doesn't initialize, and it hangs at the google aboot logo. while hung at the google logo, i can see the device with fastboot over usb, but any attempt at executing a fastboot command results in ... being returned to imply fastboot is waiting on a response , then the device panics and reboots.