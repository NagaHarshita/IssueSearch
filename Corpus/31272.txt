 URL : "https://github.com/lxc/lxd/issues/3689" TITLE : seccomp not enabled BODY : distribution: gentoo the output of lxc info or if that fails: kernel version: 4.9.34-gentoo lxc version: 2.0.8 lxd version: 2.14 storage backend in use: default lxc were compiled with the seccomp support --enable-seccomp , however, a tool that inspect the hardening of the running binaries, reports no seccomp enabled. the tool reports the seccomp enabled for other programs which have the seccomp option available, like tlsdate, here is the example: ~ checksec --proc-all | grep -e ' lxc|tlsdate ' command pid relro stack canary seccomp nx/pax pie fortify lxc-monitord 15377 partial relro no canary found no seccomp nx enabled no pie yes lxc 16128 partial relro canary found no seccomp nx enabled no pie yes tlsdated-setter 2482 partial relro no canary found seccomp-bpf nx enabled no pie yes the tool is available here: https://github.com/slimm609/checksec.sh