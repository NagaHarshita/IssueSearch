 URL : "https://github.com/statsmodels/statsmodels/issues/3983" TITLE : internals todo list BODY : this is a running todo list for myself instead of opening a bunch of issues. code cleanup - avoid 1-2 character variables names - pep8 cleanup - avoid import - avoid non-standard imports - indentation fixup code removal - remove code cannot possibly be run e.g. sandbox.km_class - remove commented-out code e.g. discrete_model.weibull - if __name__ == __main__ blocks tests - mark smoke tests, get coverage statistics that exclude them - coverage for cython code - anything jpkt is hesitant to accept a pr --> consider suspect attribute existence/setting - avoid __dict__ and setattr - validate __init__ args, kwargs ideally make them explicit - instead of specifying _data_attrs , record them in the call to handle_data - some results classes inherit endog/exog , not consistent de-duplication - _get_robustcov_results in models and in base.covtype - lots of stuff in sandbox - summary vs summary2 degrees of freedom - avoid altering model df_model and df_resid attributes - check/formalize/standardize assumes const - standardize df_resid = nobs - df_model + k_const - standardize exog.shape 1 vs np.linalg.matrix_rank exog - aic/bic/... inconsistent definitions naming conventions - unify self.y vs self.endog , endog_lagged , y_lagged , ... - arparams vs ar vs arpoly vs arcoefs vs params