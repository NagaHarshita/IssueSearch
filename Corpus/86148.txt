 URL : "https://github.com/nasa-gibs/worldview/issues/330" TITLE : gruntfile.js http/s proxy BODY : for uat, we need a way to pass in an http/s proxy value to the exec:python_packages directive. i made the following hack, but i'm sure there is a better way to do it, and it needs to be switchable via command line flag command: 'export http_proxy=http://198.118.198.210:3128 && export https_proxy=http://198.118.198.210:3128 && export https_proxy=http://198.118.198.210:3128 && export http_proxy=http://198.118.198.210:3128 && virtualenv python && bash -c \ path=' + pythonpath + ':${path} pip install xmltodict isodate\ '