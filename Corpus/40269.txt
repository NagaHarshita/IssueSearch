 URL : "https://github.com/GeekyAnts/NativeBase/issues/1068" TITLE : toast issue for root BODY : hi i would like to know how can i add root toast in my index.android.js ? i'm using the stacknavigator same as the example on this link http://docs.nativebase.io/components.html toast but the problem is i'm using appregistry appregistry.registercomponent 'myapp', => myapp ; on the example on the toast doc it state in an export default which is not working on my main root file. this is the code of my index.android.js import react, { component } from 'react'; import { appregistry, stylesheet, text, view, toolbarandroid, activityindicator } from 'react-native'; import { root } from native-base ; import login from './src/android/pages/login'; import home from './src/android/pages/home'; import { stacknavigator,navigationactions } from 'react-navigation'; const myapp = stacknavigator { login: { screen: login, navigationoptions: { title:' ',headerstyle :styles.bgmainbar, headertitlestyle :styles.txttabbar} }, home: { screen: home, navigationoptions: { title: 'home',headerstyle :styles.bgtabbar, headertitlestyle :styles.txttabbar} }, }, { initialroutename: 'login' } ; console.disableyellowbox = true; console.warn 'yellowbox is disabled.' ; appregistry.registercomponent 'myapp', => myapp ; do you know how can i add the <root></root> on index.android.js and index.ios.js? thanks