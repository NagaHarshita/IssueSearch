 URL : "https://github.com/paolobenve/myphotoshare/issues/48" TITLE : bug in 012-display.js BODY : forgot to have math. before max --- a/web/js/012-display.js +++ b/web/js/012-display.js @@ -634,7 +634,7 @@ $ document .ready function { } calculatedwidth = thumbwidth; } else if options.media_thumb_type == square { - if max width, height < options.media_thumb_size { + if math.max width, height < options.media_thumb_size { thumbheight = height; thumbwidth = width; } else {