 URL : "https://github.com/istio/istio/issues/511" TITLE : sample does not work BODY : 1. i follow the command > curl -l https://git.io/getistio | sh - to install the istio. but i found no source there or docker images only with files ended with suffix .yaml. when i fired the command after installed the bookinfo > istioctl get route-rules i got no resource found status code from > curl -o /dev/null -s -w %{http_code} http://${gateway_url}/productpage is always 503 not 200. so what should i do?