 URL : "https://github.com/wso2/message-broker/issues/51" TITLE : refactor binding registry BODY : it seems we are using the binding registry incorrectly. we call exchange.getbindingsforroute routingkey and get a binding where we only need the set of storage queues. we don't need a binding registry. 1. for direct exchange we can keep the list of bound queue names 2. for topic exchange the constituenttables is the data structure