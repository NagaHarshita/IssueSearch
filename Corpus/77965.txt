 URL : "https://github.com/stefanwebb/tensorflow-datasets/issues/1" TITLE : overwirte the tensorflow BODY : hi stefan webb thanks for you great work i find one problems as i'm using gpu when i install using : pip install -user tensorflow-datasets it just overwrited the tensorflow with cpu version. so i have to reinstall the cpu version i'd like to suggest to remove from install_requires= 'numpy', 'scipy', 'tensorflow>=1.0.0', 'matplotlib', 'six', 'enum34', 'pillow', , to install_requires= 'numpy', 'scipy', 'matplotlib', 'six', 'enum34', 'pillow', , or it's confused to use for someone with different version tensorflow. also when i follow the setup.py it always shown error when source built these packge. thanks.