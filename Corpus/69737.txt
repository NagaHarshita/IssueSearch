 URL : "https://github.com/VanthStudios/UnityTemplateOpenSource/issues/3" TITLE : travis ci configuration BODY : config the .travis.yml file and scripts to build a unity project. when a pull request is submitted, travis will check if it is possible to build it and in negative case block the merging the target branch.