 URL : "https://github.com/ronancpl/MapleSolaxiaV2/issues/40" TITLE : fix portal bug fixed BODY : instead of using random spawn point for normal jumping we do this instead this is built where if theres no portals on a map, itll revert to random spawn portal. this fixes issues with mystic doors and fm market where u spawn at a random door instead main gate lol public void changemap int map { maplemap warpmap; if geteventinstance != null { warpmap = geteventinstance .getmapinstance map ; } else { warpmap = client.getchannelserver .getmapfactory .getmap map ; } if warpmap.getportals == null { changemap warpmap, warpmap.getrandomplayerspawnpoint ; } else { changemap warpmap, warpmap.getportal 0 ; } }