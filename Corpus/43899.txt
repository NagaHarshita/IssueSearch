 URL : "https://github.com/rpm-software-management/libdnf/issues/304" TITLE : dnf.conf isn’t used BODY : /etc/dnf/dnf.conf isn’t used by libdnf. there are several global options in dnf.conf http://dnf.readthedocs.io/en/latest/conf_ref.html main-options , and most should work in libdnf. many of the features aren’t implemented in libdnf, and some aren’t relevant. however, these should all work: excludepkgs issue 303 proxy protected_packages fastestmirror throttle metadata_expire max_parallel_downloads install_weak_deps clean_requirements_on_remove deltarpm deltarpm_percentage