 URL : "https://github.com/mjswensen/themer/issues/15" TITLE : shade color interpolation BODY : i think users should only need to give the darkest color and the lightest color for the shades. themer should automatically generate the in-between colors using this formula: dark = somergb light = somergb n = number of colors we want 8 for themer delta = light-dark / n-1 colors = dark+ delta i for i in range n