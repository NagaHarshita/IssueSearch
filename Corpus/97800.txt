 URL : "https://github.com/grantcermak/Spring-Security-Kerberos-with-ActiveDirectory/issues/1" TITLE : how to set the kerberoskey BODY : i have a problem with the code as below: set<kerberoskey> creds = subject.getprivatecredentials kerberoskey.class ; kerberoskey keys = creds.toarray new kerberoskey creds.size ; it always return an empty sets, but there are some value in subject.getprivatecredentials ; so i want to know how where privatecredentials kerberos.class from?keytab file?if so,what we need to notice when we generate this file?