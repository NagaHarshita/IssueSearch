 URL : "https://github.com/wikibootup/lightning/issues/2" TITLE : sharing instance status BODY : after login, / /auth redirects / /dashboard correctly but when accessing / /dashboard directly, it redirects to login, it is because these two components use other is_authenticated instance.