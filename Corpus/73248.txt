 URL : "https://github.com/zombodb/zombodb/issues/169" TITLE : copy command and zombodb BODY : create table public.data pk_data bigint ,myfield text ,constraint idx_data_pkey primary key pk_data ; create index es_public_data on public.data using zombodb zdb 'public.data', ctid , zdb data. with url='http://127.0.0.1:9200/', shards='3', replicas='1' ; insert into public.data pk_data values 1 ; insert into public.data pk_data values 2 ; insert into public.data pk_data values 3 ; -- the csv file looks like this: -- cat /tmp/pk.csv -- pk_data -- 4 -- 5 -- 6 copy public.data pk_data from '/tmp/pk.csv' with format csv, delimiter ',', header ; insert into public.data pk_data values 7 ; insert into public.data pk_data values 8 ; insert into public.data pk_data values 9 ; select pk_data from public.data order by pk_data; pk_data --------- 1 2 3 4 5 6 7 8 9 9 rows select pk_data from public.data where zdb 'public.data', ctid ==> 'pk_data: ' order by pk_data; pk_data --------- 1 2 3 7 8 9 6 rows what happened to pk’s 4, 5, and 6? it appears that the copy command added the records to my table, but zdb didn’t index them? thoughts?