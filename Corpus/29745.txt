 URL : "https://github.com/Microsoft/opengcs/issues/45" TITLE : mkfs.ext4 in initrd BODY : mkfs.ext4 in the initrd is a dynamically compiled binary that was taken from the ubuntu 16.04 distribution when we were initially testing. in the final product, we want a statically compiled mkfs.ext4 that we can verify the origin. there are two things we can investigate: - busybox comes with mkfs.ext2. we can see if we can compile busybox to include mkfs.ext4. - statically compile our own http://e2fsprogs.sourceforge.net/ which has ext4 support