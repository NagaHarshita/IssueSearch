 URL : "https://github.com/Cloud-CV/Fabrik/issues/117" TITLE : add a title pane BODY : we should add a title pane with the model name – this should be an editable field which: • defaults to untitled if the user is building a model from scratch takes the name of whatever the model file is called if loading from zoo / importing from any framework should go into db – when a shared link is opened, the same name should show up.