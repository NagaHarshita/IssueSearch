 URL : "https://github.com/Pritraj/MyNotes/issues/5" TITLE : procedure with trigger BODY : procedure -------------------------- create or replace function client_r_0_5.fn_check_skill_validity returns trigger -- <------function needs to return trigger if called in trigger language plpgsql as $function$ declare description text := new.column_name; begin if fn_test description is null then raise exception 'test: testname:- % ', new.column_name; end if; return new; end; $function$ trigger ------------------------------------------ create trigger tr_test before insert or update on table for each row execute procedure fn_test ;