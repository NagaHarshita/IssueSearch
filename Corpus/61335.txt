 URL : "https://github.com/codefordc/housing-insights/issues/431" TITLE : api endpoint for filter BODY : update the api endpoint for the 'filter' dataset: - add all fields of the 'project' table by default, so that anything that is available on the project table including new calculated fields being added in 430 are available on the endpoint automatically - join the current query to the 'zonefacts' table using the census_tract field, and return all data columns of zone facts as census_tract_datafieldname . will decide about cluster and ward later - 'id' fields the uuids that are added during ingestion of project, subsidy, and zone tables can be skipped or need to be appropriately renamed