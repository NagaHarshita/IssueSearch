 URL : "https://github.com/ryanab/fileshare/issues/7" TITLE : ui: home layout setup BODY : - if user isn't logged in, don't render files - once user is logged in: - render all files on cards one file per card with newest on top, 10 at a time 'click to load more' button at the bottom - in thin sidebar on left, render a small plus dropzone box - if user clicks on the navbar filters pdf, image, video, misc , render only that type of file