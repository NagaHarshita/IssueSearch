 URL : "https://github.com/exmo-dev/exmo_api_lib/issues/6" TITLE : make a typed api. BODY : for now the https://github.com/exmo-dev/exmo_api_lib/blob/master/golang/exmo.go is just an example of making an http requests. the suggestion is ot make a go package, which could be imported in the code. also need to have typed calling. for example: go exmoapi := exmoapi key, secret userinfo, err := exmoapi.userinfo if err != nil { fmt.printf api result: %v , result.uid }