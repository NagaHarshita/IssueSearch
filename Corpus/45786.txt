 URL : "https://github.com/ga-wdi-boston/capstone-project/issues/748" TITLE : internal server error BODY : i get the following error message when trying to create a review post http://localhost:4741/reviews/ 500 internal server error my schema is the following: const mongoose = require 'mongoose' const reviewschema = new mongoose.schema { text: { type: string, required: true }, _reviewedbook: { type: mongoose.schema.types.objectid, ref: 'book', required: true }, _owner: { type: mongoose.schema.types.objectid, ref: 'user', required: true } }, { timestamps: true, tojson: { virtuals: true, transform: function doc, ret, options { const userid = options.user && options.user._id || false ret.editable = userid && userid.equals doc._owner return ret } } } mongoose i pass in text, the input on the front end looks like <input type= text name= review text placeholder= text class= form-control text-add required= true > </div>