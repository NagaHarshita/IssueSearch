 URL : "https://github.com/CpanelInc/perl-compiler/issues/43" TITLE : setting av->fill fails BODY : in this case we are not saving main::4 which explain part of the failure this subtest is coming from t/op/array.t https://rt.perl.org/public/bug/display.html?id=37350 perl sub is { my $a, $b = @_; if $a == $b { print stderr ok - $a ; } else { print stderr not ok - $a vs $b ; } } our @array; { bug 37350 begin { @array = 1..4 ; $ {@array} = 4243444; is $ {4}, 4243444 ; } is $ {4}, 4243444 ; } we have -1 instead of the expecting custom value.