 URL : "https://github.com/unicredit/neo/issues/8" TITLE : write more rewrite rules BODY : rewrite rules are useful to avoid intermediate allocations and perform more operations in a single blas call. we can support at least a few standard forms, and now there is a way to test they are actualy applied https://github.com/unicredit/neo/blob/master/tests/rewrites.nim . some functions used in rewrite rules may also be exposed in case some advanced user wants to use them directly