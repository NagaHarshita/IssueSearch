 URL : "https://github.com/airbnb/react-dates/issues/762" TITLE : rtl nav bug BODY : i'm unsure as to whether this is a bug or as intended i'm new to working with rtl . when using custom navnext and navprev elements and isrtl the icons appear to be incorrectly ordered due to the login https://github.com/airbnb/react-dates/blob/master/src/components/daypickernavigation.jsx l62-l96 jsx <daypicker date={datetime || moment } ondatechange={this.ondatechange} numberofmonths={1} hidekeyboardshortcutspanel enableoutsidedays isoutsiderange={isoutsiderange} navprev={<icon name={'chevron-left'} />} navnext={<icon name={'chevron-right'} />} isrtl /> results in: ! image https://user-images.githubusercontent.com/1085899/31431954-27df21b2-aeb0-11e7-839a-9bc5f0522d72.png i'm not sure if this is intended or not but my workaround is currently using dynamically changed isrtl : jsx navprev={<icon name={isrtl ? 'chevron-right' : 'chevron-left'} />} navnext={<icon name={isrtl ? 'chevron-left' : 'chevron-right'} />} isrtl={isrtl} i've tested this on 12.7.0, i can't try it on 13.x versions yet as we use sass not inline styles and i don't have the time to figure out how to implement the new themeing system doesn't seem hard just quite a bit time consuming .