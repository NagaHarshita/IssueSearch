 URL : "https://github.com/dmfs/jems/issues/13" TITLE : add frozen single BODY : getting the value of some single implementations might be quite expensive. in these cases it's useful to have a frozen decorator which queries the delegate only once and returns the same value instance ever after.