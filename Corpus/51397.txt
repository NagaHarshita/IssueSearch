 URL : "https://github.com/google/logger/issues/10" TITLE : allow change log depth BODY : log depth is currently fixed for inferring logging file and line number, which is not very handy in the case where a helper function wants to log as the caller. for instance: go func retry fn func error error { for attempt := 0; ; { if attempt > 0 { time.sleep backoff attempt logger.infodepth 1, fmt.sprintf retry %d , attempt } err := fn if err == nil { return nil } if !isretryable err { return err } logger.infodepth 1, fmt.sprintf got retryable error: %v , err } return nil } if err := retry func error { httpclient.do ... } ; err != nil { // .. }