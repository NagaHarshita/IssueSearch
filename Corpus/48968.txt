 URL : "https://github.com/KronicDeth/intellij-elixir/issues/715" TITLE : strip trailing spaces BODY : thank you so much for awesome plugin! i experienced a problem with striping trailing spaces after saving a file. let's say we have a code: defmodule foo do def bar do end def baz do end end i would expect that after saving a file those spaces in the line between functions will be striped. rubymine behaves like that in case of ruby code. what do you think?