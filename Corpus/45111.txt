 URL : "https://github.com/biryu2205/Biryu/issues/94" TITLE : hackerrank java stack BODY : java import java.util.scanner; import java.util.stack; public class solution { public static void main string args { scanner sc = new scanner system.in ; while sc.hasnext { system.out.println isbalanced sc.next ; } sc.close ; } static boolean isbalanced string parentheses { stack<character> stack = new stack<character> ; for int i = 0; i < parentheses.length ; i++ { char ch = parentheses.charat i ; if ch == ' ' || ch == ' ' || ch == '{' { stack.push ch ; } else if stack.empty { return false; } else { char top = stack.pop ; if top == ' ' && ch != ' ' || top == ' ' && ch != ' ' || top == '{' && ch != '}' { return false; } } } return stack.empty ; } } link https://www.hackerrank.com/challenges/java-stack/problem