 URL : "https://github.com/Artjom-Metro/ggsignif/issues/17" TITLE : change asterisks size? BODY : hi, very nice package! one question: -is it possible to specifically change only the size or colour of the asterisks on top of the line, instead of changing only the line size? i tried the argument size = x, but that works only for the line and not for the asterisks. many thanks, ni-ar