 URL : "https://github.com/nextcloud/end_to_end_encryption/issues/5" TITLE : ios question - start BODY : hi @tobiaskaminsky & @schiessle, first question : on start i get private key & get public keys if private key and/or public keys do not exists i generates a private key and a public key ... why i have 4 api get private key -> store private key get public keys -> sign public key why not : get private key + public keys put private key + public key thanks