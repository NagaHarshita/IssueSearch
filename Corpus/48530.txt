 URL : "https://github.com/tscheckl/awayfromlife/issues/3" TITLE : select events by city BODY : it would be cool if there was a feature with which one could select a city and get all events that are happening in that city. it could be an extra page, as i think the functionality is large enough for that to make sense, or implement the feature into the calendar somehow.