 URL : "https://github.com/tendermint/tendermint/issues/907" TITLE : indexing txs bonus parts BODY : refs 835 - rewrite indexing service to index a batch of transactions it could subscribe to block headers to find out when blocks start and how many txs they have, then wait for those txs and batch the writes. todo: add a note one day we can have this indexerservice run many go-routines to do the indexing in parallel. - benchmark kvindexer search