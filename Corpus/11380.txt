 URL : "https://github.com/auraphp/Aura.SqlQuery/issues/121" TITLE : group by feature BODY : hi, one of the missing feature i think about the library is creating grouping statements. i have been looking at https://github.com/paragonie/easydb grouping-of-conditions . an example taken : php $statement = easystatement::open ->group ->with 'subtotal > ?' ->andwith 'taxes > ?' ->end ->orgroup ->with 'cost > ?' ->andwith 'cancelled = 1' ->end ; echo $statement; / subtotal > ? and taxes > ? or cost > ? and cancelled = 1 / does anyone like this feature ?