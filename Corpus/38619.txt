 URL : "https://github.com/vibertthio/beact/issues/18" TITLE : problems about codes BODY : 1: animation.js const setanimation = index, set, animation => { const i = index % set.length ; // why? set i .foreach s => { const opt = s.options ? s.options : ; const param = two, two, tween, colors, animation ; s.animation ...param, ...opt ; } ; }; const setsequenceranimations = => { sequenceranimations.splice 0, sequenceranimations.length ; // why? setanimation currentsequenceranimationsindex, sequenceranimationsset, sequenceranimations, ; }; const setkeyanimation = => { keyanimations.splice 0, keyanimations.length ; //why? setanimation currentkeyanimationsindex, keyanimationsset, keyanimations, ; }; 2: animationkey2indexmapping in drummachine.js, we import animationkey2indexmapping from animation.js, and it is imported from animation.config.js. my question is: why don't we just import animationkey2indexmapping from animation.config.js?