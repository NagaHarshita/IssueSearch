 URL : "https://github.com/gajoseph/dbutils/issues/5" TITLE : numeric type w/ precision BODY : when there is a numeric type w/ decimal point need to map correctly in postgre salary decimal 5,2 no when you convert to mysql it comes in as decimal if jdbc type is numeric 2 , then do the following getcolumns decimal_digits => the number of fractional digits. null is returned for data types where decimal_digits is not applicable. column_size int => column size.