 URL : "https://github.com/DLR-SC/tigl/issues/236" TITLE : create macos ci on travis BODY : we should add ci builds on macos. this can be done with travis. we should define the deployment_target though. i suppose 10.9 should be low enough.