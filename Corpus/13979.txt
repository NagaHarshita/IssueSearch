 URL : "https://github.com/wellls/blogic/issues/4" TITLE : actor with python BODY : actor 模式 又叫参与者模式，是并行计算的一种模式。'actor' 可以被视为一个「演员」、「状态」，「参与者」或者也可以把它想象成 oop 编程中的一个对象。它是 actor 模型计算过程中的基本单元。每个 actor 之间使用「消息」来传递信息。众所周知 oop 泛型中有「一切皆对象」的概念, actor 模型中也有与之类似的「一切皆 actor 」的概念。但是 actor 可不是编程泛型，那么用那种编程泛型来实现 actor 更合适呢？答案并不一定是 oop ，这个我们会在后面的段落中讲到。 mailbox 既然有如此之多的 actor 可供使用，那么其调度过程是怎么实现的呢？erlang vm 中有个 mailbox 的概念，专门用来调度 actor ，使之运行起来井然有序。就像被信箱托管的书信一样严谨可靠。 python 3.6 python asyncio |> elixir actor two py processing:a supervisor, b actor --- 综上所述，我觉得 python asyncio 更加适合使用 event-loop 的 reactor 并发模式，而非 erlang 式的 actor .