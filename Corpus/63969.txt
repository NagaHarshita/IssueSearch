 URL : "https://github.com/kennethreitz/records/issues/105" TITLE : feature request: scalar BODY : iwbni, when requesting a single value from a single row, there was a way to return that value directly. currently the best we can do is: db.query somequery .first 0 i'd propose some sugar, just to make things look neater: db.query somequery .scalar though i guess i can monkeypatch it for now.