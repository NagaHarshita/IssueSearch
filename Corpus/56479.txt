 URL : "https://github.com/marklieberman/foxygestures/issues/102" TITLE : new action: zoom image BODY : in firegestures, i assigned the mousewheel gestures to dynamically zoom in/out the image under my mouse cursor using two custom scripts. is it possible to support this in foxygestures, either through custom scripts or built-in actions? http://www.xuldev.org/firegestures/getscripts.php for reference, the scripts: enlarge image const scale = 1.5; var node = firegestures.sourcenode; if ! node instanceof htmlimageelement return; if !node.hasattribute width node.setattribute width , node.naturalwidth ; if !node.hasattribute height node.setattribute height , node.naturalheight ; if !node.hasattribute originalwidth { node.setattribute originalwidth , node.width ; node.setattribute originalheight , node.height ; } node.width = node.width scale; node.height = node.height scale; reduce image const scale = 0.5; var node = firegestures.sourcenode; if ! node instanceof htmlimageelement return; if !node.hasattribute width node.setattribute width , node.naturalwidth ; if !node.hasattribute height node.setattribute height , node.naturalheight ; if !node.hasattribute originalwidth { node.setattribute originalwidth , node.width ; node.setattribute originalheight , node.height ; } node.width = node.width scale; node.height = node.height scale;