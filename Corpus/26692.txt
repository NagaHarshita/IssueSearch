 URL : "https://github.com/dherges/ng-hal/issues/16" TITLE : build: renew set-up BODY : demo/package.json : have a dedicated package.json for demo app with linklocal depenencies json { dependencies : { ng-hal : file:../dist/ng-hal }, scripts : { sync : rimraf node_modules/ng-hal && linklocal } } lib/package.json : have a dedicated package.json for the library have separate distributables for library and demo app: - dist/demo use angular cli - dist/ng-hal provide a proper package.json for ng-hal that is to be used for publishing have a root package.json that is intended for dev purposes only