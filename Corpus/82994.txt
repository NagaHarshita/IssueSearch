 URL : "https://github.com/wayspurrchen/moshy/issues/4" TITLE : bitrate issue with ppulse BODY : hello, i'm making some tests with -ppulse function. if i use -prep to encode my file, ppulse is very fast. but quality is bad because prep is encoding in .avi 5000kbit -g 600. but when i'm preparing my files without prep with higher bitrate ex : x264 50mbit/s -g 600 it takes hours and at the end the file is damaged green band . moshy commands actually use very few cpu ressources. what's wrong doctors ?