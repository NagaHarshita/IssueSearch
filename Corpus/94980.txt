 URL : "https://github.com/brandonphelps/conflagarg/issues/2" TITLE : configparser read function BODY : there needs to be some utility functions for the configparser that can read a file using configparser, returns a config object. the function should accept an optional parameter called schema, which will allow for validation of the parsed config file making sure it meets the requirements of the schema, however only if the schema is passed into the function.