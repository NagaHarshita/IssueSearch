 URL : "https://github.com/badoualy/datepicker-timeline/issues/8" TITLE : supply labels at runtime BODY : suppose i want to fetch labels from server upon selection of date. and then reflect the fetched data as label. label means the text shown below date, which is supplied by datelabeladapter as per your sample . how do i do it? how do i refresh labels with data fetched from server.