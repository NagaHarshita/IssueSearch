 URL : "https://github.com/chriso/validator.js/issues/620" TITLE : trim fields by tag BODY : i would like to add the ability to trim input before validating inside govalidator.validatestruct . there could be a sanitizer map that is run in govalidator.validatestruct before the validators are run if there is a sanitizer tag that fits the type, input will be sanitized before validating.