 URL : "https://github.com/DoSomething/rogue/issues/188" TITLE : remove photos table. BODY : _feature overview_ ------------------- user story remove the photos table and move the content storing onto the posts table. the posts table should be updated to have a url and caption field to store this content. also remove the polymorphic relationship between a post and a photo . additional information optional we will not store edited_url in the db anymore but work on convention that there will be and edited_ prefix on the file name when the frontend needs to display the post.