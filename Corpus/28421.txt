 URL : "https://github.com/adamschackart/HackmanGames/issues/27" TITLE : gcc warning removal BODY : clang builds clean as a whistle, but gcc complains about unused local typedefs from static asserts , narrowing conversion, and a real bug in idx_type_convert_ex where two printf-style arguments are swapped, causing a crash if that assert ever fires.