 URL : "https://github.com/hjalmers/angular-generic-table/issues/143" TITLE : filter / search to api ? BODY : hi it's great table how if i want to search or filter post to api point ? i have read this code public trigger = function $event { if $event.value && $event.value.selectedrows { this.selectedrows = $event.value.selectedrows.length; } }; and do this this.http.post this.url, req .subscribe res => { this.configobject.data = res.data; } ; but not really sure how to post request to api and get return data thanks