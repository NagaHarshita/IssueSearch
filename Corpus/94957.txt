 URL : "https://github.com/lxde/lxqt/issues/1352" TITLE : complex shortcut wont work BODY : i'm trying to bind the following command to the shortcut alt+print spectacle --activewindow --background --output /tmp/spectacle.png && xclip -sel clip -t image/png /tmp/spectacle.png 'print' is bound to spectacle works and the invoking the command from the shell works too. expected behavior take a screenshot of the active window and load that into the clipboard current behavior clipboard is cleared; short flicker sometimes looks like the command is initiated system information distribution & version: arch linux kernel, qt version, liblxqt: official versions always up to date