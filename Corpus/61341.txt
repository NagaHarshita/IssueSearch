 URL : "https://github.com/zulip/zulip-mobile/issues/229" TITLE : api timeout crashes app BODY : the apicall function in apifetch.js throws when a network fetch timeouts, but that timeout is not caught in the try..catch scope. as a result, the error bubbles up and crashes the app. let timeout; try { if !notimeout { timeout = settimeout => { throw error request timed out @ ${route} ; // send app_offline }, 5000 ; } const response = await apifetch auth, route, params, notimeout ;