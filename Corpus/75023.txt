 URL : "https://github.com/Mary2424/FiPCE/issues/16" TITLE : free form table creation BODY : method for creating a table with an arbitrary variable defining columns, an arbitrary variable defining rows and an arbitrary variable displayed in cells and an arbitrary numeric variable to define a slider to change the view e.g. time . we would need to be able to first subset the data.