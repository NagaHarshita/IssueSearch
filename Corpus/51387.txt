 URL : "https://github.com/pgRouting/pgrouting/issues/980" TITLE : algorithm tester fails BODY : algorithm tester worked fine and several postgres versions installed with the 9.x postgres series, but with postgres 10.1, it fails because now the directory is with /10/ without minor version.