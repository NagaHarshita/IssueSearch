 URL : "https://github.com/FrDH/jQuery.mmenu/issues/699" TITLE : bootstrap modals issue BODY : i have issue with modals box of bootstrap. my page structure is something like this: <div id= page > <!-- content --> </div> <div id= footer > <!-- footer --> </div> <nav id= menu > <!-- mmenu --> </nav> <div id= modals > <!-- different hiddens bootstrap modals --> </div> mmenu move div modals inside: <div id= mm-0 class= mm-page mm-slideout > <div id= page > <!-- content --> </div> <div id= modals > <!-- different hiddens bootstrap modals --> </div> </div> the result is that bootstrap <div class= modal-backdrop fade show > overlap also modal window, since is not moved by mmenu. the strange thing is that footer remain where is, i mean is not wrapped inside div mm-0, not sure why, is positioned absolute on bottom, like this sticky footer example of bootstrap https://v4-alpha.getbootstrap.com/examples/sticky-footer/ i guess how to avoid that mmenu wrap div modals. thanks!