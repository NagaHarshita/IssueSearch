 URL : "https://github.com/angular/angular/issues/16185" TITLE : view not render BODY : when i use ng2 i found a problom that i cant not fix it. i use route and outlet to route some component in a dashboard,when i routing in to component,that both page are request a http get use promise when nginit.when i first click and it work will route and render ,but when routed,i click the rowser's back button or use location.back function it also request the net resources and the http get is ok get the data ,but the view can not display such as ngfor data but simple str is displayed.in otherwise i try promise with a timeout instead of http request,the problom is also appeared,directly return a str or list it render will.