 URL : "https://github.com/coreos/etcd-operator/issues/1161" TITLE : etcd backup size limit BODY : we are using etcd operator with backups. and looks like backup size growth linear. from 50k to 20mb in 2 hours. looks like it is normal behaviour https://coreos.com/etcd/docs/latest/tuning.html and by default etcd truncates log after 10к revision. but in my case looks like it was not happen. is there way control this settings from etcd-operator side?