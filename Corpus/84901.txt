 URL : "https://github.com/fanglingsu/vimb/issues/380" TITLE : download path not honored BODY : the setting download-path is not honored. regardless of what is configured, downloads are stored in ~ . steps to reproduce - set the download path to some path except for ~ - e.g. :set download-path= ~/downloads - make sure the path exists - download a file expected behaviour - the download is stored in download-path actual behaviour - the download is stored in ~