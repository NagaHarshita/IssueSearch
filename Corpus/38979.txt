 URL : "https://github.com/adamjgnoel/mendeleyBibFix/issues/4" TITLE : remove debug symbols BODY : you may want to remove the -g flag in the batch file so the executable is smaller when compiling from source.