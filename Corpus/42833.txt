 URL : "https://github.com/boscoh/tkform/issues/2" TITLE : can't pip that module BODY : can't install tkform though pip with: sudo pip install tkform . the return is not version mathching distribution. actually i did sudo pip install github: https://github.com/boscoh/tkform/archive/master.zip specs: linux ubuntu 16.04, python 2.7 note: installing from github url does work. it's is interesting that the pypy package is published, pip don't gets it, but manually install works... :-v