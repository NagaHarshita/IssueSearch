 URL : "https://github.com/saqueib/qrss/issues/1" TITLE : optional xml validation BODY : hey @saqueib nice work! i'm just wondering if you can made the xml validation optional like cache, so we can use the library to parse all kind of xml files. maybe adding something like this: private $no_validate = false; public function novalidate { $this->no_validate = true; return $this; } if ! $this->no_validate { return $rss && $rss->channel ? $rss : false; } return $rss ; thanks in advanced.