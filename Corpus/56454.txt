 URL : "https://github.com/object88/sync/issues/2" TITLE : need way to prove done BODY : given a scenario... go count := 0 var wg sync.waitgroup f := func ctx context.context { time.sleep 10 time.millisecond select { case <-ctx.done : default: count++ } wg.done } r := newrestarter wg.add 2 go func { time.sleep 1 time.millisecond r.invoke f } r.invoke f wg.wait it is possible that after the wait , there will still be a context and cancelfn on r .