 URL : "https://github.com/glowing-bear/glowing-bear/issues/909" TITLE : add buffer jump alt+j BODY : when pressing alt+j, gb should: - put the search bar in a special mode, indicated visually, maybe with an input group http://getbootstrap.com/components/ input-groups - display buffer numbers. for this we need to shift the buffer names to the right a bit so that there is enough space - filter buffers after first digit has been input - immediately jump after two digits have been input like weechat i think this would be a really cool way of implementing buffer jump. i got the inspiration from @ttheuer's dirty hack https://github.com/ttheuer/glowing-bear/commit/d38acc7850cd6fe7b1e843da572fc5640f08556e :