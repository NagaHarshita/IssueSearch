 URL : "https://github.com/CalebFenton/dex-oracle/issues/19" TITLE : smarter plugin management BODY : right now, all plugins are enabled by default and the user can disable them. however, it's likely that some plugins should only run in some circumstances. should make it so plugins can declare themselves disabled by default. as a bonus, would be nice if plugins were a bit more documented.