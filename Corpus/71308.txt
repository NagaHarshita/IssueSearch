 URL : "https://github.com/spacejam/rsdb/issues/2" TITLE : add child split fragment BODY : this was skipped to simplify the in-memory prototype, but we need to use a frag so that we can linearize key updates with splits