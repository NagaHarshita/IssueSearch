 URL : "https://github.com/taljmars/DroneServer/issues/2" TITLE : why use soap ? BODY : why not just use the rest + json serialization? soap is a very slow protocol and now everything is gradually abandoning it. using rest json it's very easy to build webinterface gui angular, react ..... i looked in this code base and i can say that moving to this approach will be very simple. we will use the same model as in soap.