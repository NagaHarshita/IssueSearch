 URL : "https://github.com/kpau/beehive/issues/3" TITLE : add code quality check BODY : the project must have high code quality. this means it should have tests and static code analysis tools set to run on build. this task will be done when the following is completed: - setup a test project, to include unit tests. - the tests are build and ran as part of the library build process, - the test project contains at least one basic unit test to confirm the previous two requirements. - setup ts lint, which is ran as part of the library build process. --- 🚫 blocked by: 2. to implement these requirements, there should be a build process created, so 2 must be done before this.