 URL : "https://github.com/fv0/native-ui/issues/72" TITLE : option tree view vibrancy BODY : now that atom 1.19 is out, could we have an option to turn on vibrancy for the tree view ? the javascript api is as folllow : js // options : // 1. 'medium-light' // 2. 'light' // 3. 'dark' // 4. 'ultra-dark' // 5. null to remove vibrancy require 'electron' .remote.getwindow .setvibrancy 'light' ; for it to work the tree view and html element must have a transparent background like so i did this quickly so this can probably be improved : css html, atom-workspace, .atom-dock-mask, .pane, .tree-view { background: transparent !important; }