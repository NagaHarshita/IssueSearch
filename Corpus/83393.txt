 URL : "https://github.com/kubernetes/minikube/issues/1099" TITLE : can't use minikube on vpn BODY : is this a bug report or feature request? choose one : bug report minikube version use minikube version : 1.0.2 environment : - os e.g. from /etc/os-release : macos - vm driver e.g. cat ~/.minikube/machines/minikube/config.json | grep drivername : virtualbox and vmwarefusion - iso version e.g. cat ~/.minikube/machines/minikube/config.json | grep iso : file:///users/bjones/.minikube/cache/iso/minikube-v1.0.2.iso - install tools : - others : what happened : creating and using minikube works fine. open cisco anyconnect secure mobility client vpn client for work and minikube no longer works. minikube start hangs, kubectl commands cannot reach the host. what you expected to happen : minikube should work while connected to a vpn how to reproduce it as minimally and precisely as possible : 1. create minikube off of vpn 2. connect to vpn 3. try to use minikube anything else do we need to know : i'm assuming this has something to do with the host-only networking.