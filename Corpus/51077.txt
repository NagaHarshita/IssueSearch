 URL : "https://github.com/keang/pretty_search/issues/3" TITLE : add parallel searching BODY : since it is most likely that files fit into memory, the next optimisation would be to iterate through the loaded collection in parallel. we would implement a parallelcollection search which is the same as memorycollection but uses parallel iterator: https://github.com/grosser/parallel ruby records = parallel.map data, in_processes: number_of_cpu_cores { |document| ... }