 URL : "https://github.com/wso2/product-ei/issues/1546" TITLE : hl7 sends no ack via http BODY : description: we created a proxy service which only task is to receive hl7-messages over http as xml , store them into a message queue and send an ack back to the sender before or after storing the messages in the queue, does not matter . i configured the proxy as explained in the documentation. i tried every possible setup of the following properties/parameters which are suggested to create ack messages: <property name= hl7_result_mode value= ack scope= axis2 /> <property name= hl7_generate_ack value= true scope= axis2 /> <property name= hl7_application_ack value= true scope= axis2 /> <parameter name= transport.hl7.autoack >true</parameter> my current attempt is this: <?xml version= 1.0 encoding= utf-8 ?> <proxy xmlns= http://ws.apache.org/ns/synapse name= hl7store transports= http,hl7 statistics= disable trace= disable startonload= true > <target> <insequence> <property name= hl7_application_ack value= true scope= axis2 /> <log level= full /> <property name= messagetype value= application/edi-hl7 scope= axis2 /> <clone> <target sequence= storesequence /> </clone> </insequence> </target> <parameter name= transport.hl7.autoack >false</parameter> <parameter name= transport.hl7.port >55557</parameter> <parameter name= transport.hl7.validatemessage >false</parameter> <description/> </proxy> maybe i've just missed one step in my setup, but i tried nearly everything. affected product version: esb 4.8.1