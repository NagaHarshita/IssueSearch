 URL : "https://github.com/vlachoudis/bCNC/issues/623" TITLE : camera problems, fixed BODY : i had a problem with getting my camera to work. i'm on a laptop which has a camera, and now i added another. couldn't get it to work at first, then i changed in camera.py line 47 to: self.idx = utils.getint camera , camera so now it uses the settings from the ini-file. don't know if that is the correct way of solving it, but it works.