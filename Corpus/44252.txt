 URL : "https://github.com/paul-buerkner/brms/issues/294" TITLE : error in check_prior: BODY : i just upgraded to the latest brms on cran. when i try to run the cor_car example in the manual pg 45 , i get the following error: error in check_prior prior, formula = formula, data = data, family = family, : unused argument threshold = threshold i'm using the exact code from the manual: not run: generate some spatial data east <- north <- 1:10 grid <- expand.grid east, north k <- nrow grid set up distance and neighbourhood matrices distance <- as.matrix dist grid w <- array 0, c k, k w distance == 1 <- 1 generate the covariates and response data x1 <- rnorm k x2 <- rnorm k theta <- rnorm k, sd = 0.05 phi <- rmulti_normal 1, mu = rep 0, k , sigma = 0.4 exp -0.1 distance eta <- x1 + x2 + phi prob <- exp eta / 1 + exp eta size <- rep 50, k y <- rbinom n = k, size = size, prob = prob dat <- data.frame y, size, x1, x2 fit a car model fit <- brm y | trials size ~ x1 + x2, data = dat, family = binomial , autocor = cor_car w summary fit