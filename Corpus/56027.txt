 URL : "https://github.com/skuhl/RobotRun/issues/224" TITLE : manual function macros BODY : the index used to identify the manual function to execute in the manual function macro menu is incorrect. since, the list of macros includes both manual function and shift + user key macros, there needs to be a better way to map the macro selected in the manual function to the actual macro in the list of macros.