 URL : "https://github.com/amw/jpeg_camera/issues/44" TITLE : snapshot to blob BODY : i am trying to do this: var base64blob = my_photo; var image = document.createelement 'img' ; image.src = 'data:image/jpg;base64,'+ base64blob; document.body.appendchild image ; i get the snapshot taken in the preview window, but i cant seem to get it into a blob to upload into a database with the rest of my form. the above code is my simple test to confirm i have the image, but nothing happens. var take_snapshots = function count { var snapshot = camera.capture ; my_photo = snapshot; if jpegcamera.canvas_supported { snapshot.get_canvas add_snapshot ; } else { // <canvas> is not supported in this browser. we'll use anonymous // graphic instead. var image = document.createelement img ; image.src = no_canvas_photo.jpg ; settimeout function {add_snapshot.call snapshot, image }, 1 ; } if count > 1 { settimeout function {take_snapshots count - 1 ;}, 500 ; } }; i presume its something simple?