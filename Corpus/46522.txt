 URL : "https://github.com/AlbrechtL/welle.io/issues/50" TITLE : meson or cmake build? BODY : is using qtcreator the only way of building this? it seems so currently, the cmake build has gone. perhaps create a meson build? having a way of creating this out of tree, from a command line, without the development environment is a must.