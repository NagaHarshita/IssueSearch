 URL : "https://github.com/CERT-BDF/TheHive/issues/125" TITLE : report on closed case BODY : report on closed case - security incident report request type feature request work environment | question | answer |---------------------------|-------------------- | os version server | rhel | os version client | seven | thehive version / git hash | 2.x | package type | binary | browser type & version | chrome problem description ability to generate a report using a template on a case. complementary information we are required to produce a security incident report on certain internal security events. thehive is able to keep track of the tasks that our team perform, which is great. if it could also produce a final report when a case is closed, we could use a template to mimic our existing security incident report and save ourselves a lot of work.