 URL : "https://github.com/bwilcox-1234/ChatScript/issues/151" TITLE : ^input with empty input BODY : i want to use ^input with no input involved, that means i want to simulate that the user made an empty input. with version 7.42 it was working using ^input . in the current version 7.52 its causing the input to be ' i tried varous things like ^input ^ ^input ^ and so on, but they either don't invoke the revised input or keep the old input.