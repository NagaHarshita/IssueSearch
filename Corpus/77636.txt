 URL : "https://github.com/LightTable/LightTable/issues/2327" TITLE : no tab headers scrolling BODY : 0.8.1, ubuntu 16.04 x86_64, binary from download link on the official site when a lot of files are opened and there is not enough space for all tab headers, there is no way to scroll to previously opened tabs. one can only try to click the edge of the leftmost tab so that it scrolls a bit to the right and do the same thing over and over again do get to the first tab.