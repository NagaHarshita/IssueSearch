 URL : "https://github.com/BenLangmead/bowtie/issues/61" TITLE : error for colorspace data BODY : hi, after version 1.2.0, an error occurs when aligning colorspace data generated by solid. when using version 1.1.2, there is no problem. could you solve this problem? $ bowtie-1.2.1.1/bowtie -c hg19-cs sample.fastq -n2 -k1 > /dev/null reads file contained a pattern with more than 1024 quality values. please truncate reads and quality values and and re-run bowtie terminate called after throwing an instance of 'int'