 URL : "https://github.com/karino2/editbook/issues/17" TITLE : introduce eslint on ci BODY : abstract - eslint http://eslint.org/ is a de-facto lint tool in today's javascript ecmascript world. this lint tool is built on some static analytic libraries. its features covers not only coding styles also an error detection. - it has a auto-fix mode to fix style run with --fix command line flag . - and some editor has a integration for it e.g. vscode-eslint https://github.com/microsoft/vscode-eslint . dependencies - enable ci travisci or others - choose rules which we enables. eslint's rules are opt-in at all. but there are some presets. - all rules has 3 level, disabled , enabled warn , and enabled error . - eslint:recommended is built-in rule set. - eslint-config-airbnb https://www.npmjs.com/package/eslint-config-airbnb is well known rule set.