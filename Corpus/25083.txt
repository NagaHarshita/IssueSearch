 URL : "https://github.com/gatling/gatling/issues/3249" TITLE : use el in uri/path BODY : the ability to dynamically set query parameters is well documented http://gatling.io/docs/2.2.3/http/http_request.html query-parameters , however there doesn't seem to be any way to do the same with path parameters. an example use case would be scala http getting issue .get https://github.com/gatling/gatling/issues/${issuenumber} if there is a way to do this that i'm missing, could it be added to the docs? if not, adding it as a feature would be greatly appreciated.