 URL : "https://github.com/statamic/v2-hub/issues/1668" TITLE : fr: user 'hasroles' BODY : expected behaviour be able to check a if a user has one role out of an array. for example, if your cp view can be access only by users with one of a list of roles. note, should check against 'super' as well. possible code: return $user->issuper || !empty array_intersect $user->roles ->keys ->all , array_get $this->getconfig , 'roles', ;