 URL : "https://github.com/gbochenek/esri-wab-build/issues/35" TITLE : no dojo.js output BODY : i must be doing something wrong. everytime i run esri-wab-build it create the folders, creates the zip file but does not create a dojo.js. the arcgis app\arcgis-js-api is empty. all js files are removed, which i assume is correct bit i would expect the dojo.js to include them all. attached is the build-report against a new app from the web app builder version 2.3.