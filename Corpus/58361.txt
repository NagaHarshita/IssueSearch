 URL : "https://github.com/jsdavis/dotfiles/issues/12" TITLE : setup.py needs updates BODY : 1. doesn't need to put things in sudo 2. don't want it to blindly copy all dotfiles in the folder anymore 3. needs to intelligently copy the fewest number of files possible, based on os/envrionment it's in, i.e. i don't need .iterm2 stuff if i'm not on osx. don't need .zshrc if not using zsh. don't need .tmux.conf if no tmux, etc