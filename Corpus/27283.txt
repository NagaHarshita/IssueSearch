 URL : "https://github.com/philipbkemp/PHP-Validator/issues/1" TITLE : create initial page BODY : the initial page should prompt for a url, download the page to a local temporary file, and then delete it. the processing tasks will then be carried out between these two steps.