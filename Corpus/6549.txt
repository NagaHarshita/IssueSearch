 URL : "https://github.com/sindresorhus/p-some/issues/1" TITLE : add test option BODY : would be useful with a tester function, so you could get the first two promises that passes a check rather than just the first two promises. could be something like: js pany p1, p2, p3, p4 , { count: 2, test: value => value > 5 } .then first => { console.log first ; } ; the above would return the first two promises that resolves to a value higher than 5. not sure about the naming. alternatives: - validate - check --- we should also move the count argument into an option.