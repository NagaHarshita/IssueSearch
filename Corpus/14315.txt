 URL : "https://github.com/SUPERCILEX/Robot-Scouter/issues/162" TITLE : add slider metric BODY : this metric would be a subset of a counter metric that enables choosing a ranges of possible values. the ui would be similar to the donate slider, but have two anchors so that users could create error bars of sorts.