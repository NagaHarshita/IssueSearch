 URL : "https://github.com/patronage/bubs/issues/73" TITLE : algolia search helper BODY : while i don't think we need algolia on by default, we do have enough code now that it would be useful to have the helper that controls posts in by default, even if it's commented out.