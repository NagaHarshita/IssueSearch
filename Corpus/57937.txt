 URL : "https://github.com/neuroailab/ptutils/issues/10" TITLE : required runner params BODY : - db interface same as tfutils - load params w/ default do_restore false, and raise error if same exp_id exists - save_params should have all filters same as tfutils, and enforce metrics_freq for database bandwidth safety. also should take in arguments corresponding to model attribute names/dotpaths and their own save frequencies. or just one frequency for many auxillary intermediate computations. alternative is that the user can define this functionality in train loop -