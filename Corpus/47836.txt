 URL : "https://github.com/Daumen-Hoch-AG/PiHomie/issues/20" TITLE : python import und oop BODY : 1. wo findet denn der import statt ? sowohl in dev.py was später ja durch ein main.py ersetzt werden könnte brauche ich standardbibliotheken wie os oder auch requests . die brauche ich in unseren modulen aber auch. werden die dort dann nochmal erneut importiert ? das könnte ziemlich viele doppelte importe geben. muss das sein ? 2. methodenaufrufe ich habe eine klasse für das logging inkl der methoden erstellt. sie wird in der dev.py instanziiert. wenn ich jetzt aber im modul rollershutter.py diese instanz zum protokollieren nutzen möchte, muss ich sie dann extra übergeben ? kann man die instanz global machen ? wenn ja, wie würde ich die denn dann in der methode von rollershutter.py aufrufen ?