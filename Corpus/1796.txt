 URL : "https://github.com/Microsoft/vscode/issues/23854" TITLE : git diff bug BODY : <!-- do you have a question? please ask it on http://stackoverflow.com/questions/tagged/vscode --> - vscode version: 1.10.2 - os version: windows 10 x64 steps to reproduce: 1. having following: ! image https://cloud.githubusercontent.com/assets/11234139/24609310/a34353ae-187a-11e7-89ec-faf2fa09fe69.png 2. see that the unindention of $node instanceof node\expr\funccall won't be shown. code: before: php } else if $node instanceof node\expr\funccall && $node->name instanceof node\name && string $node->name === 'define' && isset $node->args 0 && $node->args 0 ->value instanceof node\scalar\string_ { after: php } else if $node instanceof node\expr\funccall && $node->name instanceof node\name && string $node->name === 'define' && isset $node->args 0 && $node->args 0 ->value instanceof node\scalar\string_ {