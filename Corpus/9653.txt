 URL : "https://github.com/react-community/react-navigation/issues/692" TITLE : access to params BODY : i have this issue with getting parameters in my component. const action = navigationactions.navigate { routename: 'order', params: { orderid, name } } ; navigation.dispatch action ; and then const withdata = graphql some_query, { options: { navigation } => { variables: { id: navigation.state.params.orderid } } , ... } ; is this the correct way to get parameters orderid and name ? i thought that it is sufficient to select parameters like const withdata = graphql some_query, { options: { orderid, name } => { variables: { id: orderid } } , ... } ;