 URL : "https://github.com/ubclaunchpad/minutes/issues/57" TITLE : cli for collection BODY : todo make a little cli to automate collection. flow: + prompt for video id + download audio and display some xml. + prompt user for some delimiter information and interior see extract_labels.py . + cache this info in <youtube_id>/<youtube_id>.json + run pipeline.py <youtube_id> to generate the labeled data.