 URL : "https://github.com/w3c/webex/issues/11" TITLE : redundant button role BODY : there is a hidden button in the host has not yet joined the meeting dialog, that has role= button applied. the role does not need to be set explicitly because it's implicit when the <button> element is used. <div class= header-buttons > <button class= icon-ic12 icon-close title= close role= button >close</button> </div>