 URL : "https://github.com/winnfsd/winnfsd/issues/48" TITLE : winnfsd with vpn BODY : hi, with vpn enabled, vagrant up hangs while mounting nfs shared folders . if i stop vpn, shared folders are well mounted. if i try to reconnect vpn after vagrant up , i cannot access to my virtualbox. when i put into my vagrantfile: config.winnfsd.host_ip = local_ip_my_laptop it works shared folders are well mouted, vagrant up is ok , but the virtualbox hangs sometimes :/ for example, when i use phantomjs + selenium server. did you have a trick to keep virtualbox working with vpn enabled? thanks.