 URL : "https://github.com/pivotal/workstation-setup/issues/160" TITLE : fixing macos clock drift BODY : in working with macos machines, i've noticed that their clocks can get a few minutes out of sync with the time even with the date and time system preference checked to set date and time automatically from time.apple.com a coworker is suggesting to change this to pool.ntp.org a do you have experience with this and is it better at keeping in sync than time.apple.com ? b do we want to set this setting to pool.ntp.org from the command line when running workstation-setup?