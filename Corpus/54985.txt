 URL : "https://github.com/codecombat/codecombat/issues/4265" TITLE : i think this is a bug. BODY : the page in question:multiplayer treasure grove my code: // be the first to 100 gold! // if you are defeated, you will respawn at 67% gold. while true { var item = hero.findnearestitem ; var enemy = hero.findnearestenemy ; var flag = hero.findflag green ; if hero.distanceto item < 10 { if item.value == gold { hero.movexy item.pos.x, item.pos.y ; } else if item.value == silver { hero.movexy item.pos.x, item.pos.y ; } else { hero.movexy item.pos.x, item.pos.y ; } else if flag { hero.movexy flag.pos.x, flag.pos.y ; } } } on line 21 it tells me expected an identifier and instead saw an 'else'