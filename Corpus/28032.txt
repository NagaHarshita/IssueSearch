 URL : "https://github.com/aspnet/Docs/issues/4606" TITLE : explain return page BODY : per customer: onget of indexmodel does not return page but its page is still rendered and populated with a list of movie if any . so what does the return page mean? i am totally a newbie. from https://docs.microsoft.com/en-us/aspnet/core/tutorials/razor-pages/page per ryan return page is the default behavior. this is meant for cases where you don’t do any control flow and just want to set a property or something. if you are doing control flow, then your handler method will be declared to return iactionresult, so you need return page so that you have an action result to return. return null also should work -- end or ryan so, for example, some methods have return redirecttopage ./index