 URL : "https://github.com/MaverickTse/pyMingwBuild/issues/1" TITLE : proposal use github api BODY : extracting url with regex hack is dirty. why don't you use github api? python import json from urllib import request, response j = json.loads request.urlopen https://api.github.com/repos/mirror/mingw-w64/tags .read .decode final_url = j 1 search latest version except rc for i, o in enumerate j : if not rc in o name : final_url = o tarball_url break search for preferred version for i, o in enumerate j : if o name == preferred_version: final_url = o tarball_url break