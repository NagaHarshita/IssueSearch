 URL : "https://github.com/jacknagz/osquery-cookbook/issues/69" TITLE : internal repo install BODY : how do you configure this recipe to install from an internal repo? i see the boolean attribute that one can set, but what other configurations need to be added to get it to install from an internal repo? it seems that once you set it to 'true' is just continues on and tries to upgrade the package.