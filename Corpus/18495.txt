 URL : "https://github.com/ramagottfried/symbolist/issues/48" TITLE : text bounds issue BODY : currently the text object has some weirdness when being resized -- 1 it seems like the texteditor part of the juce label needs more buffer space in order not to automatically offset the position when new characters are added. 2 for hit tests and more careful placement, we might want to make a new component class based on label, but using glypharrangement to store the text, which would provide tools for finding the bounding box for the specific glyphs i.e. t vs j