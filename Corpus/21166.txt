 URL : "https://github.com/Icinga/chef-icinga2/issues/264" TITLE : enable ssl for web BODY : currently the classic and icingaweb2 are deployed without ssl. imho we should by default enable ssl for security reasons. most distributions have ready to use self generated ssl certs for testing purpose. with attributes we could point to vaild ssl certs on te system.