 URL : "https://github.com/nyu-devops-payments-2017/payments/issues/3" TITLE : view single payment BODY : as a seller i need to look up a single payment so that i can view the information for that payment assumptions: the database can be queried for payments by id acceptance criteria: given the database for payments exists when i call 'get /payments/<id> then i should get a json object with that payment's information, or 404 if no payment with that id exists.