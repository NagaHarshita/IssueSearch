 URL : "https://github.com/tommoor/fontselect-jquery-plugin/issues/18" TITLE : set the initial font BODY : not necessarily a bug, but how do you set the initial font - so it starts with that font selected rather than the prompt to select a font?