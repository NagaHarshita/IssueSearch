 URL : "https://github.com/pasahitz/zirikatu/issues/2" TITLE : problem with avast! BODY : hello, i have a problem with avast, and its sanbox, in what the antivirus analyzes the file the session of meterpreter is closed, but since the executable is not detected as such, if not only by procedure avast parses the files in sanbox mode by default, when it finishes re-executing the file but the problem is just a simple code in the script so that this is not a problem, what i did was add in the handler this line echo set exitonsession false >> $dir/handler/handler.rc and ready with that is all full: d