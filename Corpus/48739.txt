 URL : "https://github.com/spark-jobserver/spark-jobserver/issues/861" TITLE : sjs clean my database BODY : i have deploy sjs in host1 use sqldao, the configuration is as follows . sqldao { slick-driver = slick.driver.mysqldriver jdbc-driver = com.mysql.jdbc.driver directory where default h2 driver stores its data. only needed for h2. rootdir = /tmp/spark-jobserver/sqldao/data full jdbc url / init string, along with username and password. sorry, needs to match above. substitutions may be used to launch job-server, but leave it out here in the default or tests won't pass jdbc { url = jdbc:mysql://test.db:10000/sparkjobsvr?useunicode=true&characterencoding=utf-8&autoreconnect=true&usessl=false user = abc password = 123 } db connection pool settings dbcp { enabled = false maxactive = 20 maxidle = 10 initialsize = 10 } } and i deploy sjs on host2 , the configuration is same as host1. but when i start sjs on host2, it drop all my talbe in sparkjobsvr. sjs on host2's version is diffrent from sjs on host1