 URL : "https://github.com/devopsdelta/recommendations/issues/74" TITLE : update folder structure BODY : as a developer i need to update the folder structure of the application so that there is a clear division of where all code lives in the project assumptions: the api service will live in a folder labeled app all unit tests will be stored in a folder labeled tests all integration tests will be stored in a folder labeled features all application configuration and environment setup will live in the root directory acceptance criteria: given ... when ... then ...