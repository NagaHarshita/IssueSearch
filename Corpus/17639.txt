 URL : "https://github.com/donejs/donejs/issues/898" TITLE : quick start guide BODY : went through the quick start guide https://donejs.github.io/donejs-next/guide.html and ran into a few things. when changing src/index.stache, the browser updates as expected. when reloading the page, however, i see the old version of the page while the new one is loading. it seems the server is rendering the old copy. restarting donejs-develop fixes this. this should perhaps be its own issue? under messages page / use the connection, the given template is: html {{ if messagespromise.isresolved}} {{ each messagespromise.value}} <div class= list-group-item > <h4 class= list-group-item-heading >{{name}}</h4> <p class= list-group-item-text >{{body}}</p> </div> {{/each}} {{else}} <div class= list-group-item > <h4 class= list-group-item-heading >no messages</h4> </div> {{/if}} this renders no messages while the result is loading, but will render nothing if the result is empty. i believe it should be an else loading... could be added as well : html {{ if messagespromise.isresolved}} {{ each messagespromise.value}} <div class= list-group-item > <h4 class= list-group-item-heading >{{name}}</h4> <p class= list-group-item-text >{{body}}</p> </div> {{else}} <div class= list-group-item > <h4 class= list-group-item-heading >no messages</h4> </div> {{/each}} {{/if}}