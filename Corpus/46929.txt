 URL : "https://github.com/anhaidgroup/py_entitymatching/issues/90" TITLE : new labeler scrolling BODY : when the user scrolls down the page and then selects a label for a tuple, the labeler returns to the top of the page instead of staying at the same place the user left off.