 URL : "https://github.com/you-dont-need/You-Dont-Need-Lodash-Underscore/issues/111" TITLE : _.groupby equivalent BODY : // underscore/lodash: _.groupby 'one', 'two', 'three' , 'length' ; // => { '3': 'one', 'two' , '5': 'three' } // native 'one', 'two', 'three' .reduce result, value => {let key = string value.length ; result.hasownproperty key ? result key .push value : result key = value ; return result}, {} // => { '3': 'one', 'two' , '5': 'three' } // native shorter version : 'one', 'two', 'three' .reduce result, value, i, a, key = string value.length => result key = result key || .push value , result , {} // => { '3': 'one', 'two' , '5': 'three' } an other example: // underscore/lodash: _.groupby 6.1, 4.2, 6.3 , math.floor ; // => { '4': 4.2 , '6': 6.1, 6.3 } // native 6.1, 4.2, 6.3 .reduce result, value => {let key = string math.floor value ; result.hasownproperty key ? result key .push value : result key = value ; return result}, {} // => { '4': 4.2 , '6': 6.1, 6.3 }