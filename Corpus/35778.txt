 URL : "https://github.com/psychopy/psychopy/issues/1635" TITLE : fbo with viewpos bug BODY : if viewpos is set and usefbo is set to false, the origin changes as expected. however, if usefbo is set to true, then within the window, the background seems to move with the origin, and parts of the window aren't drawn. see minimal example below toggle fbo : from psychopy import visual win = visual.window units='pix', viewpos= 100, 100 , colorspace='rgb', color= 0, 0, 0 , usefbo=true usefbo=false stim = visual.gratingstim win=win, size= 100, 100 , tex=none, units='pix' for i in range 100 : stim.draw win.flip