 URL : "https://github.com/SpoonX/boards/issues/5" TITLE : add default generator BODY : js const {generator} = require 'boards' ; const emoji = require 'node-emoji' ; const path = require 'path' ; class templategenerator extends generator { static defaults { return {}; } generate parameters { return this.runsteps 'read', 'replace', 'write' ; } complete stream { return stream; } } module.exports.templategenerator = templategenerator; a lot of the time you'll want to use this. it's tiny, it's simple. there's no cli output... perfect.