 URL : "https://github.com/mwcschool/ecommerce_api/issues/66" TITLE : implementare la reload BODY : in models la classe base dovrebbe implementare la refresh. ovvero una funzione che, chiamata su un oggetto peewee, vada a ricaricare i suoi valori presi dal database. questo: item = user.get user.uuid == item.uuid diventa questo: item = item.reload