 URL : "https://github.com/Level-2/Transphporm/issues/139" TITLE : html formatting is broken BODY : found this bug while testing with html elements. the following does not work: content: change <i>the </i>appearance ; format: html; but this works: content: <i>change the appearance</i> ; format: html; also it does not work either if the string does not have any html elements in it. also single tags like <br> does not work.