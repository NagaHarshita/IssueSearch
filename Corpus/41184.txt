 URL : "https://github.com/crowdresearch/daemo/issues/894" TITLE : yuge table ui tweaks BODY : - more whitespace between workers - toolbar is right aligning when there's a revision - max-height of cells and header - disable approve all button if there's nothing left to approve - let urls break across lines, which will make the table lay out better - load a lot more data at once, let's see how far we can push it