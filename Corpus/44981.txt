 URL : "https://github.com/arnesson/cordova-plugin-firebase/issues/171" TITLE : gettoken return empty BODY : i'm using ionic 2 with this plugin, and the token appears to be empty on my android: this.platform.ready .then => { if this.platform.is cordova { firebase.gettoken .then token => console.log the token is ${token} // save the token server-side and use it to push notifications to this device .catch error => console.error 'error getting token', error ; } } ; shows an empty value in the console. what's wrong?