 URL : "https://github.com/clevandowski/rabbitmq-cluster/issues/1" TITLE : minor bash inifinite loop BODY : même si je comprends comment tu fais une inifinite loop en bash qui s'arrete à la moindre erreur, j'ouvre cette pull request en mode discussion ouverte pour montrer des alternatives que je trouve personnellement plus interessante. set -e permet d'aretter un programe dés qu'une ligne d'instruction return un code status different de 0. pour moi automatique dans tout mes scripts et je rajoute || true a la fin des lignes qui peuvent avoir un exit code différent de 0 !/bin/bash -e while : do cmd done next will not be executed if cmd fail en plus explicit mais équivalent : !/bin/bash set -e while true do cmd done next will not be executed if cmd fail les condition en bash réagisse sur les exit status en faite d'ou l'utilisation de la commande test abrégé en et plus securisé en bash donc on peut directement écrire. plutot que de checker $? dans la condition directement y executer la comande est une bonne pratique ! !/bin/bash while cmd ; do : done next will be executed if cmd fail even with set -e mais ma préféré pour un script interactif reste : !/bin/bash watch --errexit --interval 0,1 cmd next will not be executed if cmd fail and set -e had been set wrapp ta commande curl entre des quotes même en l'écrivant en multi line et tous ces exemples marchent my useless 2 cents