 URL : "https://github.com/ModellingWebLab/WebLab/issues/5" TITLE : set up static files BODY : for now, the existing set of static files can be ported directly across and hooked into the new back end. however, it would be helpful to make some basic improvements to make the code easier to work with: static build system e.g. gulp improve the way javascript files are loaded e.g. bundler? set up sass build this can be done incrementally as css is a subset of scss