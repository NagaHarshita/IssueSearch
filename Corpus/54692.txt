 URL : "https://github.com/PyconUK/ConferenceScheduler/issues/17" TITLE : have sessions to slots BODY : in a similar vain to unavailability etc... the problem is fundamentally one of events and slots. as sessions are collections of slots we could just include a session attribute in the slot class. this just means that the only variables that get passed to the mathematical problem are events and slots . we could keep the sessions class as they are in case they're useful further down the line and ensure through preprocessing that the sessions instances are all correctly in the slot sessions attribute.