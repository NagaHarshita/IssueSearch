 URL : "https://github.com/jpeazy22/react-project1/issues/3" TITLE : file setup 3 BODY : 11. create a 'default' gulp task that runs multiple tasks under a single command 12. figure out how to configure your gulp sass task to compile all .scss files and combine into a single .css file that is minified. 13. create a webpack.config.js file 14. configure webpack so that it bundles multiple js components into a single bundle.js file 15. organize your project so there is a 'build' folder which is what you work out of, and a 'dist' folder that automatically gets populated by a gulp task with your compiled/minified production ready css and js and any other assets whenever you save a file.