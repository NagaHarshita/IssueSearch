 URL : "https://github.com/moul/advanced-ssh-config/issues/231" TITLE : notification not working BODY : i installed assh using homebrew and i try to make the notifications working under mac os. the following configuration adapted from the readme: ~~~yaml defaults: hooks: onconnect: - notify new ssh connection to {{.host.prototype}}. ondisconnect: - notify ssh connection to {{.host.name}} closed, {{ .stats.writtenbytes }} bytes written in {{ .stats.connectionduration }} {{ .stats.averagespeed }} ~~~ does not work. have i missed something?