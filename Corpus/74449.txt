 URL : "https://github.com/ivalkeen/capistrano-nginx/issues/9" TITLE : error when restart nginx BODY : capistrano-nginx is born to restart nginx but it can not do this. i install the following: gemfile: group :development do gem 'capistrano', require: false gem 'capistrano-rvm', require: false gem 'capistrano-rails', require: false gem 'capistrano-bundler', require: false gem 'capistrano3-puma', require: false gem 'capistrano-nginx' end cap file: require 'capistrano/deploy' require 'capistrano/rails' require 'capistrano/rvm' require 'capistrano/bundler' require 'capistrano/rails/assets' require 'capistrano/rails/migrations' require 'capistrano/puma' require 'capistrano/scm/git' require 'capistrano/nginx' i connect to the remote server by ssh public key file. cap production nginx:restart rvm 1.29.1 latest by michal papis, piotr kuczynski, wayne e. seguin https://rvm.io/ ruby-2.4.1 ruby 2.4.1p111 2017-03-22 revision 58053 x86_64-linux 00:00 nginx:restart 01 sudo service nginx restart 01 sudo password for kevin: ijieosdfsdw9dkksfq it hangs at here and not anything happens.