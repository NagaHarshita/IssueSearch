 URL : "https://github.com/jupyterhub/zero-to-jupyterhub-k8s/issues/351" TITLE : document configmap items BODY : in the v0.5 post we mentioned configmap items, but it was a bit too specific to put in the general blog post. instead we should clearly document this maybe in the schema? . here's the text below: > configmap items can be arbitrary yaml, and you can read them via the get_config function in your hub.extraconfig.