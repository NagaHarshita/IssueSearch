 URL : "https://github.com/corazzon/KaggleStruggle/issues/1" TITLE : what is difference? BODY : hi corazzon. thanks. i'm not familiar with machine learning. it helps me a lot to learn about machine learning. let me ask one. you used randomforestregressor in lesson 3 and lesson 4 youtube video . ////////////////lesson 3: from sklearn.ensemble import randomforestregressor max_depth_list = model = randomforestregressor n_estimators=100, n_jobs=-1, random_state=0 ------------------------------------ 1 %time score = cross_val_score model, x_train, y_train, cv=k_fold, scoring=rmsle_scorer score = score.mean -------------------------------------------------- 4 model.fit x_train, y_train ---------------------------------------------- 2 predictions = model.predict x_test ----------------------------------- 3 print predictions.shape //////////////////lesson 4: from sklearn.ensemble import randomforestregressor rfmodel = randomforestregressor n_estimators=100 ----------------- 1 y_train_log = np.log1p y_train ------------------------------------------ rfmodel.fit x_train, y_train_log ----------------------------------------- 2 preds = rfmodel.predict x_train --------------------------------------- 3 score = rmsle np.exp y_train_log ,np.exp preds ,false ----------------- 4 in fact, you used the same model and algorithm in this part. but i don't understand . when i remove and use y_train, the output is non . could you explain about this? thanks.