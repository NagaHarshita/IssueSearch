 URL : "https://github.com/frictionlessdata/tableschema-py/issues/158" TITLE : validate topojson values BODY : overview after v1 release the library will be supporting topojson format but without values validation. for now there is no implemented jsonschema to do this validation. but we could implement a basic python checker.