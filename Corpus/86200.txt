 URL : "https://github.com/vim/vim/issues/2066" TITLE : handled errors stop timer BODY : version: 8.0.0995 reproducible on windows and linux steps to reproduce: 1. vim --clean -s test.vim vim test.vim let s:count = 1 function! s:foobar ... abort echom s:count let s:count += 1 try produce error let baz = search '\{' catch do nothing endtry endfunction if exists 's:timer' call timer_stop s:timer unlet s:timer endif let s:timer = timer_start 1000, function 's:foobar' , {'repeat':-1} even though error message is not displayed, exception is always propagated to timer and as a result timer stops repeating itself after third error. i'm sorry for the noise if this is intended behavior.