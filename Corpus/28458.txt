 URL : "https://github.com/fuse-box/fuse-box-aurelia-loader/issues/22" TITLE : get the entrys path BODY : @stoffeastrom @arabsight @thanood @nchanged today we added a hack fix for files ending the same, but we need to improve this part was thinking we could use the entry path to make sure, because a module can have many files with same name in sub folders suggestion to solution for this: so for this one: https://github.com/fuse-box/fuse-box-aurelia-loader/blob/master/src/fuse-box-aurelia-loader.ts l341-l366 i was thinking this look try/catch block javascript let moduleid = object.keys fusebox.packages .find name => path.startswith ${name}/ ; if moduleid { let resources = object.keys fusebox.packages moduleid .f ; let resourcename = path.replace ${moduleid}/ , '' ; let resourceentry = resources.find r => r.endswith resourcename + '.js' ; // try and get entry path try { let subdirs = fusebox.packages moduleid .s.entry.split '/' ; if subdirs.length > 1 { subdirs.pop ; resourceentry = subdirs.join '/' + '/' + resourcename + '.js'; } } catch e {/ nothing atm /}; retunvalue = ${moduleid}/${resourceentry} ; } if !this.fuseboxexist retunvalue { debugprint 'error', 'findfuseboxpath failed to find', arguments ; }