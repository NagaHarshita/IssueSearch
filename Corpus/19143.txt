 URL : "https://github.com/pingryiRT/ChatClient/issues/12" TITLE : wth with line 214 BODY : first this is comparing a bool to a literal. if x == true: can always be just if x: but here's a bigger thing. doesn't the line of code do exactly what the comment says not to do?