 URL : "https://github.com/moxis/CryptoSight-Public/issues/10" TITLE : time range - add current BODY : time range function is kind of ambiguous, what time zone is it using? we need to establish it so the user understand what timeframe the bot will use. possible ideas, use specific dates? example: timeframe -> today -> day in the past 13/10 then bot uses those 2 days. we can think more about this and come back to it. meanwhile implement -> how far back would you like me to look? current: x m or x d or 9 d or all then eg. 1m, 3h, 9d, all