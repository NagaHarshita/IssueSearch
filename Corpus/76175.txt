 URL : "https://github.com/google/earthenterprise/issues/246" TITLE : install gee on localhost BODY : when i try to install fusion with this command sudo ./install_fusion.sh i have this message welcome to the google earth enterprise fusion 5.1.3 installer. the hostname of this machine does not match the fully-qualified hostname. requires that they match for local publishing to function properly. exiting the installer. if you wish to continue, re-run this command with the -hnmf 'hostname mismatch override' flag. how to configure my hostname do i need to install and configure postgres , postgis , apache server , before installing fusion and ge server ? thanks