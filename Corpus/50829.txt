 URL : "https://github.com/codenameone/CodenameOne/issues/2077" TITLE : add boolproperty class BODY : currently in order to have boolean properties we need to use something like: property<boolean, owner> property . but the problem is that on deserialization over propertyindex that property's value becomes a string and therefore we get the following exception when we try to access its value: java.lang.classcastexception: java.lang.string cannot be cast to java.lang.boolean