 URL : "https://github.com/zerodayz/citellus/issues/107" TITLE : mysql db size checks BODY : for the live check implementing mysql -u root -e 'select table_schema db name , round sum data_length + index_length / 1024 / 1024, 1 db size in mb from information_schema.tables group by table_schema;' du -mshc /var/lib/mysql/ sos report will only points the basic from df that can not be a good hint as most deployments have only one huge root file-system and most people will not notice the size of db until having other issues like 504 gateways errors and similars.