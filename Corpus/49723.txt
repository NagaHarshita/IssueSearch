 URL : "https://github.com/kubernetes/kubernetes.github.io/issues/4509" TITLE : update configmap docs BODY : this is a... <!-- choose one by changing to x --> - x feature request - bug report take a look these two topics to see whether they are redundant. if so, combine into one topic. if you decide that we need two topics, prune the content so that information is not repeated. configure containers using a configmap https://kubernetes.io/docs/tasks/configure-pod-container/configmap/ use configmap data in pods https://kubernetes.io/docs/tasks/configure-pod-container/configure-pod-configmap/