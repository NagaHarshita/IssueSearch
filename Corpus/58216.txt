 URL : "https://github.com/Giphy/GiphyAPI/issues/126" TITLE : search by not a rating BODY : in the readme it says the search can be optionally filtered by a rating. >rating - optional limit results to those rated y,g, pg, pg-13 or r . could it be possible to search by not a rating, so it would be possible to retrieve all results that are not rated r , which would return results y, g, pg, pg-13, and those not rated?