 URL : "https://github.com/haiwen/seahub/issues/1920" TITLE : security openredirect BODY : problem seahub do not check the forwarded host -header by apache/nginx. the user control the header so he can send any host -header. impact low, because request header manipulation can not be done easily by 3party. proof: bash curl https://seacloud.cc -v -h host: google.com solution seahub should check the host variable against the configured host in config. apache/nginx should not forward bad requests workaround filter malformed host -header by apache/nginx.