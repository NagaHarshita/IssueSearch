 URL : "https://github.com/becvert/cordova-plugin-zeroconf/issues/24" TITLE : discovering all services BODY : hi, the plugin works great to discover service by known types, for example zeroconf.watch '_http._tcp.', 'local.', function result { ... it works perfectly on android and ios. but is there a way to discover for exemple all _tcp. services ? i tried: zeroconf.watch '_tcp.', 'local.', function result { ...nothing any idea ? regards,