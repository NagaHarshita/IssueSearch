 URL : "https://github.com/owncloud/core/issues/29224" TITLE : webdav copy quota issue BODY : steps 1. create two users user1 and user2 1. set a quota of 1 mb for user1 1. login as user2 1. create a folder test 1. upload a 100 mb file data1.dat into test 1. share test with user1 1. login as user1 1. move or copy /test/data1.dat to / with webdav expected result move and copy both return 507 insufficient storage. actual result move returns 403 instead of 507 and blocks the operation, which is ok. copy not blocked at all. version owncloud v10.0.3