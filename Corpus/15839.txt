 URL : "https://github.com/nickoala/telepot/issues/248" TITLE : forced event call BODY : hello, thanks for your great work. i'm using delegatorbot class to handle all events from getupdates or webhook. some times i need to stop event handling in my class and then on next event i need to call previous one using saved msg from stopped event. for that purpose i'm using delegatorbot.handle method. to init the delegator i'm using this bot = telepot.aio.delegatorbot token=settings.token, delegation_patterns= include_callback_query_chat_id pave_event_space per_chat_id , create_open, mybotclass, timeout=10 , , loop=loop when i need to call previous event on_chat_message or on_callback_query i'm using this if event_type == 'chat': bot.handle msg=message elif event_type == 'callback_query': message 'id' = query_id bot.handle msg=message is it proper way? and if not what can i use for that purpose? many thanks! best regards, alan