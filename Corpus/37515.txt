 URL : "https://github.com/VarynInc/varyn/issues/185" TITLE : media upload service BODY : media upload service to allow uploading of media files + images + video + audio + zip archives, such as for games - service request service, end point to request an upload + file name + file size + content type + service object parameters e.g. game-id, conference-id, avatar, site-id, etc so we know what this file is associated with + session token + respond with end-point url, upload token one-time authorization to match parameters , final path - service follow-up + provide one-time authorization token + provide file data + respond with ok/error