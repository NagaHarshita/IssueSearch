 URL : "https://github.com/jovotech/jovo-framework-nodejs/issues/45" TITLE : restructure input access BODY : right now, parameters only include the string value of a slot/entity. to address new features like entity resolutions, let's switch the default to objects, not strings. the values could be accessed like this: javascript 'mynameisintent': function name { app.tell 'hey ' + name.value + '! how is it going?' ; },