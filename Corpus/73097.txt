 URL : "https://github.com/kai-language/kai/issues/35" TITLE : compiletime value helper BODY : a helper like below would be immensely helpful in numerous places. there is already code in the compiler that needs this but it does not yet exist. swift func compiletimevalue _ node: astnode -> exactvalue? { switch node { case .lit...: return litvalue case .ident: / lookup identifier in current scope check if returned entity is compiletime, return value / }