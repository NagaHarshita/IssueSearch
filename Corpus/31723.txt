 URL : "https://github.com/Skelp/nevis/issues/7" TITLE : support es2015 maps BODY : a built-in hash code generator and equals comparator should be added to support es2015's maps: map weakmap these should be supported in the same way as arrays since es2015 maps are iterated based on insertion order we cannot use the hash object approach while ensuring that it does not compromise our existing environment support range.