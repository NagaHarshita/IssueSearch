 URL : "https://github.com/Thizzer/JTouchBar/issues/6" TITLE : swing - frame never shows BODY : i tried to get jtouchbar working with swing, but the code alway hangs when making the jframe visible. i modelled the test application after com.thizzer.jtouchbar.awt.jtouchbarawttest : public class app { public static void main string args throws invocationtargetexception, interruptedexception { app app = new app ; int invoke = 0; if invoke == 0 swingutilities.invokeandwait app::show ; else if invoke == 1 swingutilities.invokelater app::show ; else app.show ; system.out.println finished ; } final jtouchbar jtouchbar; final jframe frame; app { jtouchbar = jtouchbartestutils.constructtouchbar ; objects.nonnull jtouchbar ; jframe.setdefaultlookandfeeldecorated true ; frame = new jframe jtouchbar ; frame.pack ; } void show { system.out.println showing ; frame.setvisible true ; system.out.println enabling touchbar ; jtouchbar.enableforcomponent frame ; system.out.println enabled touchbar ; } } when invokeandwait is used, i get: > showing when invokelater is used, i get: > finished > showing when setvisible is called from the main thread like in com.thizzer.jtouchbar.awt.jtouchbarawttest , the main thread runs to completion: > showing > enabling touchbar > enabled touchbar > finished in all of these cases, the jframe never actually appears, and the application must be terminated manually. when similar code is run as a junit test, when the test is complete, when the main thread exits, the background thread is killed by the junit test environment, so the test appears to pass. the jframe never appears. jdk1.8.0_121 macos 10.13.1 high sierra