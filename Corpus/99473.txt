 URL : "https://github.com/StargateMC/Project/issues/3" TITLE : mmocore event registry cme BODY : the event registry ticks for every event on server per world rather than just the events on that world, causing a cme server crash occasionally.