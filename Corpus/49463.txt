 URL : "https://github.com/an-cabal/an-editor/issues/5" TITLE : track cursor position BODY : the editor should track a cursor position in a buffer. it should: - display the current cursor position doesn't have to blink or do anything fancy - move left and right in the line when the <kbd>←</kbd> and <kbd>→</kbd> keys are pressed - move to the next line/previous line when the <kbd>↓</kbd> and <kbd>↑</kbd> keys are pressed depends on 4