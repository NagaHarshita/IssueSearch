 URL : "https://github.com/dazinator/DotNet.Glob/issues/25" TITLE : single asterisk behaviour BODY : i seem to get odd results when using the single asterisk wildcard . for example, let's take the following string: hkey_local_machine\software\adobe\shockwave 12 the pattern ave 12 matches, as expected, but ave 2 which uses 2 asterisks does not, neither doe shock 12 which uses a single asterisk, but not as the first character in the pattern . is this the expected behaviour? if so, what's the rationale behind it?