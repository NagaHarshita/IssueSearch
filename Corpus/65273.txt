 URL : "https://github.com/facontidavide/ros_type_introspection/issues/12" TITLE : include directory order BODY : when building, if you change the headers, you can run into the project being compiled against the installed headers rather than the current source headers. i think this is more proper. include_directories system ${catkin_include_dirs} include_directories before include