 URL : "https://github.com/kerongon/Collabo/issues/7" TITLE : missing team controller BODY : what is the problem? when running php artisan route:list it fails and gives the error: > class app\http\controllers\backend\teamcontroller does not exist steps to reproduce just run php artisan route:list proposed solution i have a feeling it's a namespace issue. there's probably a route that's been registered using the wrong one.