 URL : "https://github.com/Hammerspoon/hammerspoon/issues/1253" TITLE : spotify feature request BODY : i'd like to request one addition to the spotify api: hs.spotify.gettrackduration here's the lua code i'm using to make it happen now: lua function get_track_duration -- applescript code because hammerspoon can't return -- track time yet local script = tell application spotify set tt to duration of current track set tt to tt / 1000 return tt end tell local success, result, raw = hs.osascript.applescript script if success then return result end end