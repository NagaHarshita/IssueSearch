 URL : "https://github.com/mozilla/activity-stream/issues/2733" TITLE : consesus on event sources BODY : across https://github.com/mozilla/activity-stream/blob/master/content-src/components/pocketstories/pocketstories.js l18 pocketstories https://github.com/mozilla/activity-stream/blob/master/content-src/components/pocketstories/pocketstories.js l24 component there are a number of strings used for event sources. the problem is that: 1. they are strings and not constants 2. they have different names inside the same component even if they are used to mention two different things i still think we would benefit from having a single source name specific for the pocket component or one that can be used in other places as well.