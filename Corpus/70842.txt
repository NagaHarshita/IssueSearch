 URL : "https://github.com/daumPostcode/QnA/issues/232" TITLE : 인자값 전달 여부 BODY : daum.postcode.load function { new daum.postcode { oncomplete: function data { ... } } .open ; } ; 위 소스코드에서 oncomplete함수의 data변수에 주소정보가 담겨 있있는데요, daum.postcode.load함수 이외에서 사용한 값을 oncomplete함수 내부로 사용할 수 있는 방법이 없을까요? 예를 들면 oncomplete: function data, temp 이런 식으로요. 현재 react를 사용하고 있는데, 외부에서 사용하는 어떤 값을 oncomplete함수에서 사용할 수가 없어서 문의드립니다.