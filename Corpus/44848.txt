 URL : "https://github.com/z-song/laravel-admin/issues/1191" TITLE : form opened two times BODY : hi, i'm not 100% sure if this is correct use of this functions. anyway i wanted a box outside the tabbed form that i have in the form function, so i did this. public function create { return admin::content function content $content { $content->row $this->box ; $content->body $this->form ; } ; } the problem is that doing this the form is opened two times two <form ....> in the html source . i mean everything works anyway but it shouldn't happen. php 7.1.8, laravel 5.5.4, laravel-admin 1.5.4