 URL : "https://github.com/alexylem/jarvis/issues/656" TITLE : aide pour trigger mode BODY : tu as donné la possibilité de choisir le trigger mode physical_button mais il n'est pas intégré dans ton code, j'ai donc ajouté ceci dans jarvis.sh en dessous du trigger mode enter_key pour que l'on puisse l'utiliser bouton else if $trigger_mode = physical_button ; then order= $ cat /sys/class/gpio/gpio24/value while $cat /sys/class/gpio/gpio24/value == 0 ; do if $order == 1 ; then fi done bypass=true fi comme tu peux le voir, le bouton utilise la gpio24 du pi, et à chaque lancement du pi il faut lui créer un dossier : tapez sudo su pour passer en mode root echo 24 > /sys/class/gpio/export _export de la broche pour pouvoir y accéder_ cd /sys/class/gpio/gpio24 _allez dans le répertoire créé par l'exportation_ echo in > direction _spécifiez le sens de la broche entrée _ je te présente le montage que j'ai fait pour le bouton : ! image https://user-images.githubusercontent.com/29089765/27733166-f1e58cf2-5d94-11e7-8d47-fb80149afbdf.png