 URL : "https://github.com/tewarid/node-tcp-proxy/issues/2" TITLE : addition of cors headers BODY : hi, first of all thanks for providing a clean cli based proxy. secondly, can you please guide how we can enable cors headers to each request that pass through this proxy server. something similar to below: // set cors headers res.setheader 'access-control-allow-origin', ' ' ; res.setheader 'access-control-request-method', ' ' ; res.setheader 'access-control-allow-methods', 'options, get' ; res.setheader 'access-control-allow-headers', ' ' ; if req.method === 'options' { res.writehead 200 ; res.end ; return; } it would be a nice additional feature to use that as cli argument. thanks.