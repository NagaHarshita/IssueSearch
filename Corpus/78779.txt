 URL : "https://github.com/turgeonmaxime/rootWishart/issues/8" TITLE : register native routines BODY : starting with r 3.4, a note appears in the r cmd check if we haven't registered native routines. presumably, this will switch to a warning in the future. this is easy enough to do: simply run r tools::package_native_routine_registration_skeleton . and then copy the output to src/init.c . so there is no point in waiting