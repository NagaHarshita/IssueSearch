 URL : "https://github.com/mapbox/mapbox-java/issues/548" TITLE : new directions callback BODY : we can create a servicecallback which includes an additional callback onnoroute which is invoked when the response returns 0 routes. this would remove the boilerplate code needed every time a request is made: java if response.body == null { log.e log_tag, no routes found, make sure you set the right user and access token. ; return; } if response.body .routes .size < 1 { return; } and the new callback would look something like: java builder.build .enqueuecall new mapboxservice.servicecallback<t> { @override public void onresponse response<t> response { } @override public void onnoroute response<t> response { } @override public void onfailure throwable t { } } ; cc: @zugaldia