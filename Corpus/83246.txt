 URL : "https://github.com/suluvir/server/issues/16" TITLE : caches for external files BODY : the webpack.config.js and the package.json are read for every initial page load and then again for every external delivered. on top of that, the external files are small max 1mb , so they can be cached in memory easily. cache the files and cache the js config files. when changing them, a reload is not so bad...