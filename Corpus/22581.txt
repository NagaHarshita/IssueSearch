 URL : "https://github.com/openwrt/openwrt/issues/431" TITLE : luci peap not work BODY : i patch patches/420-indicate-features.patch, but it looks not working anyway. ---- local has_ap_eap = os.execute hostapd -veap >/dev/null 2>/dev/null == 0 ---- root@op:/tmp hostapd -veap root@op:/tmp echo $? 1 ---- i check the code for this problem, but it just not defind ieee8021x_eapol static inline int has_feature const char feat { ifdef ieee8021x_eapol if !strcmp feat, eap return 1; endif ifdef ieee80211n if !strcmp feat, 11n return 1; endif return 0; } and the only defind line is for config_win32_defaults ifdef config_win32_defaults define config_native_windows define config_ansi_c_extra define config_winpcap define ieee8021x_eapol ----- please any one tell me is it some wrong?