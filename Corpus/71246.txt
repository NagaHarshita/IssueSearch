 URL : "https://github.com/mkasberg/container-immersion/issues/18" TITLE : add a terminal css class BODY : add a terminal css class to render fixed-width samples like a terminal. the terminal style should have a black background with a bright green font. apply the terminal class in _labs/ .md where we are simulating a terminal wrapped with \ \ \ . we should be able to do something like this to selectively apply the class: http://digitaldrummerj.me/styling-jekyll-markdown/ if possible, leave the class off items that are not supposed to be a terminal like html samples .