 URL : "https://github.com/avenirer/CodeIgniter-MY_Model/issues/220" TITLE : reset relationship array BODY : after calling a model with relationship, the relationship array is not reset and the foreign key is added to the main query on subsequent call. this cause an issue if the subsequent call use group_by, with mysql 5.7 with only_full_group_by enabled. php $result = $this->user_model->fields 'id,username,group' ->with_profile ->get 12 ; // the local_key for the relation is added to the field list .... $group_count = $this->user_model->fields array 'group','count users.id ' ->group_by 'group' ->get_all ; // the local_key for the relation in the previous call is added to the field list creating an error