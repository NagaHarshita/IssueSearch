 URL : "https://github.com/matfish2/vue-tables-2/issues/356" TITLE : filters case insensitive BODY : hello i see the filters, its insensitive filters case. i have to do it for a filter custom the same thing. { name: 'compagny', callback: function row, query { if query === null { return row }else { let stockid = null stockid = row.stockid if typeof stockid === 'object' && stockid !== null && stockid !== 'undefined' { let mas = row.stockid.company.charat 0 .touppercase + row.stockid.company.substring 1 .tolowercase if row.stockid.company.indexof query > -1 || row.stockid.company.tolowercase .indexof query > -1 || row.stockid.company.tolocaleuppercase .indexof query > -1 || mas.indexof query > -1 { console.log row.stockid.company return row.stockid.company } } } } } do you have a cleaner method? thank you in advance for your assistance