 URL : "https://github.com/marvinroger/homie/issues/23" TITLE : add get next to set BODY : what do you think of a get topic extension? for interacting devices it's not really an issue, but for sensors it would make sense to request a measurement externally. in the arduino/esp8266 framework, the sensor examples are all working on a fixed hardcoded time base. a workaround could be to put the interval into the config json and update this via mqtt. but imho it would be simpeler if a property state can be requested by a .../topic/property/get -command void payload .