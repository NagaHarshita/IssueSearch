 URL : "https://github.com/oblador/react-native-animatable/issues/121" TITLE : animating a list BODY : i can't manage to animate elements of a list. not sure if i am doing it right. import react, { proptypes } from 'react'; import { flatlist, view } from 'react-native'; import { list, listitem, icon as iconcomponent } from 'react-native-elements'; import as animatable from 'react-native-animatable'; class customlistitem extends react.purecomponent { //... } const animatedcustomlistitem = animatable.createanimatablecomponent customlistitem ; class mainlist extends react.component { //... render { return <contentwrapper scroll> <list containerstyle={styles.listwrapper}> <flatlist data={list} keyextractor={item => item._id} renderitem={ { item } => <animatedcustomlistitem item={item} animation= fadeinleft />} /> </list> </contentwrapper> } } anything i am missing?