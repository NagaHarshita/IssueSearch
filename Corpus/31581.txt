 URL : "https://github.com/tarantool/tarantool/issues/2366" TITLE : sql subquery with = BODY : = select ... is allowed, but does a comparison on the first row it finds. that is, instead of returning an error when there is more than one row in the subquery result, it acts as if the expression was in select ... . box.sql.execute create table te45 s1 int primary key ; box.sql.execute create table te46 s1 int primary key, s2 int ; box.sql.execute insert into te45 values 1 ; box.sql.execute insert into te46 values 1,1 , 2,1 ; box.sql.execute select from te45 where s1 = select s2 from te46 ; result = 1 row.