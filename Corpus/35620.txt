 URL : "https://github.com/oswaldosantos/ggsn/issues/25" TITLE : error with coord_map BODY : when i try to use ggsn on a ggplot-map, i get an error when using coord_map . i do see that certain map projections might cause an error where to point to north on a globe for example , but in the default map projection it would not cause an issue as far as i understand it . here is an example: r library ggsn > loading required package: ggplot2 following loosely the example in ?map_data ggplot2 data preparation states <- map_data state arrests <- usarrests names arrests <- tolower names arrests arrests$region <- tolower rownames usarrests choro <- merge states, arrests, sort = false, by = region choro <- choro order choro$order , map creation figure_map <- ggplot choro, aes long, lat + geom_polygon aes group = group, fill = assault the base map without coord map r a minimum case figure_map ! https://i.imgur.com/dgsw63o.png r figure_map + north choro, symbol = 16, scale = 0.15 ! https://i.imgur.com/0x9jhop.png map + coord map r coordinates of a map figure_map + coord_map ! https://i.imgur.com/6oiqdxa.png r figure_map + coord_map + north choro, symbol = 16, scale = 0.15 > error: annotation_custom only works with cartesian coordinates ! https://i.imgur.com/eckuzr8.png any chance to fix this easily?