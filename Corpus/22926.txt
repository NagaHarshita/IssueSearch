 URL : "https://github.com/MatejMecka/HacktoberfestStats/issues/14" TITLE : name is undefined. BODY : when both testing and running the npm module myself mainstat returns name as undefined. everything else is returned except for the name which is undefined. your goal is to fix it :d and by fixing it you score a pr. also i get to test if i have set up circleci properly. happy hunting