 URL : "https://github.com/hypertidy/tidync/issues/56" TITLE : activate is confusing BODY : consider removing activate and replacing with select_variable and select_grid functions. they will still activate a grid, and keep the print all the same - but will add a another level of active to variable table. select_variable will work for most workflows, but should it be the default to get just one?