 URL : "https://github.com/stagemonitor/stagemonitor/issues/253" TITLE : request hierarchy problem BODY : i'm trying to implement the request monitor in an async context where the request aren't all executed on the same thread. i managed to delay monitor stop of parent request and to set a hierarchy in the requests infos but i can't see that hierarchy on request analysis in kibana.