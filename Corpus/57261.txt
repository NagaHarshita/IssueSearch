 URL : "https://github.com/fg123/wendy/issues/14" TITLE : prevent double imports BODY : like if an import has an import, imports that have dependencies on other imports how might that work after compile?