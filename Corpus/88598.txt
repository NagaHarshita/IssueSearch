 URL : "https://github.com/titipata/touchbar-example/issues/1" TITLE : note for touch bar slider BODY : the working code is from the following stack overflow question http://stackoverflow.com/questions/43061427/using-electron-to-show-value-on-touch-bar-slider/43065740 43065740 and looks like the following javascript const { app, browserwindow, touchbar } = require 'electron' ; const path = require 'path' const url = require 'url' const {touchbarbutton, touchbarlabel, touchbarslider} = touchbar const result = new touchbarlabel ; result.label = '30' + ' deg'; const slider = new touchbarslider { label: 'angle', minvalue: 0, maxvalue: 360, change: val => result.label = val.tostring + ' deg' // register event handler here! } ; const touchbar = new touchbar slider, // add slider result // add display for slider value ; let window; app.once 'ready', => { window = new browserwindow { width: 300, height: 200 } ; window.loadurl 'about:blank' ; window.settouchbar touchbar ; } ; // quit when all windows are closed and no other one is listening to this. app.on 'window-all-closed', => { app.quit ; } ;