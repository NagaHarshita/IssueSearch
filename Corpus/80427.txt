 URL : "https://github.com/asmodehn/pyros-release/issues/1" TITLE : travis ? build.ros.org ? BODY : we are currently using travis for testing this repo. since build.ros.org got github integration, it might be worth investigating if it is suitable to use build.ros.org to validate the package is building as per ros requirements. this would : - separate concerns ros builds with ros farm - keep travis sane so many ros builds are just a duplication of build in other languages with usual framework . currently however travis seems : - simpler to integrate with - nicer output - faster feedback. it could be interesting however to have both builds for comparison for a while...