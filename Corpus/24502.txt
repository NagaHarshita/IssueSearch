 URL : "https://github.com/PowerShell/PowerShell/issues/3229" TITLE : support for ctrl+z BODY : this is strongly tied to 716, but we want to make sure these have independent work items. steps to reproduce ------------------ 1. open powershell on linux/macos 2. hit ctrl+z expected behavior ----------------- current running process is forked to bg actual behavior --------------- powershell itself is forked to bg and the user is bounced back out to bash or whatever is the default shell that handled ctrl+z