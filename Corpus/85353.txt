 URL : "https://github.com/DataTables/RowGroup/issues/2" TITLE : expand / collapse a group BODY : i think it is doable. here some sample code, i did in startrender var hiddenstatus = {}; var tableconfig = {...} tableconfig.startrender = function rows, group { var elem = $ '<tr/>' ; // simple group header row element elem.on 'click', function e { if hiddenstatus group { $ rows.nodes .show } else { $ rows.nodes .hide } hiddenstatus group = !hiddenstatus group } ; return elem; }; i hope you can review and turn it to more high quality code. by the way, i'm thinking to show group items only by default hide all sub-items under groups ,