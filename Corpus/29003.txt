 URL : "https://github.com/Gideonnn/cordova-webpack-plugin/issues/2" TITLE : index.html is empty BODY : hi, i'm using a vue-webpack scaffold with an index.html in the root. i've added the cordovawebpackplugin to my build env webpack config. this is my config: new cordovawebpackplugin { output: 'dist', / specify the output folder / config: 'cordova-config.xml', / the source config for cordova which will be copied to the output folder as 'config.xml' / index: './index.html', / the source index.html which will be copied to the <output folder>/www/ folder as 'index.html' / disabled: false / optional boolean value to conditionally run the plugin / } but cordova renders an empty page and there's no index.html in the platform www folder. i've tried index.html , ./index.html , ../index.html to get the index.html from the root to copied along. the ../... variant gave me a warning about not being able to find the index.html file.