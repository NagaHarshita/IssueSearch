 URL : "https://github.com/meta-rust/meta-rust/issues/152" TITLE : switch to using rustbuild BODY : rustbuild is the new cargo driven system to build rust. upstream has declared this build system to be complete with the 1.15 release. preferred with the 1.16 release and with 1.17 the makefile based build system has been removed leaving only rustbuild. so we need to start taking the steps to move towards rustbuild