 URL : "https://github.com/nickjj/flask-webpack/issues/17" TITLE : images in .css files BODY : hi, i'm testing webpack in combination with flask and flask-webpack. i have a webpack.config.js that fetches my images and creates a copy in the static folder with a hash in the filename, which is cool. however, i saw that the bulk of my images aren't in my html, but in my css. jinja2 doesn't process css, so i can't use the _asset_url_for _ tags to load the hashed versions of my images. how can i use hashed image files fom within css? do i need to setup jinja2 in some way that it also processes css, because that would mean quite a bit of overhead. what is the recommended way to do this?