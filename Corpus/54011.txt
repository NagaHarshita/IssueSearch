 URL : "https://github.com/lefthandsimon/jammming/issues/6" TITLE : feature request review BODY : the idea for creating a separate seed list is a great idea. it will help to automate the users interaction with the app, making it easier to use. fleshing out the user interface as a sketch will also help significantly. implementing the feature with bare minimum features is also the correct approach. creating the seed list would be very similar to creating <playlist/> hence you already have the technical skills to implement it. the feature request is impressive.