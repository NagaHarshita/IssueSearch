 URL : "https://github.com/ddeboer/imap/issues/153" TITLE : can't get to work at all BODY : can't get composer to install it. so i downloaded instead, but here it cant find any of the classes even when i use 'use ddeboer\imap\server'