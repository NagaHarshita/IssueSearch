 URL : "https://github.com/projectcalico/calico/issues/548" TITLE : pool selection in mesos BODY : from 493 -------------- pool selection in mesos v2.1.0? unlike kubernetes, mesos operators can load multiple cni networks and choose which one to launch a task on. using cni conf pool selection, mesos operators can define multiple cni networks, each with their own cni subnet, and select which pool a pod will use by choosing the appropriate network. action: - document per-task pool selection in mesos via multiple calico cni networks.