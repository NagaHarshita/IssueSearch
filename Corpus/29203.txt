 URL : "https://github.com/dgading/priority_board/issues/1" TITLE : json data structure BODY : to make it simple, the data for the page should be stored in a flat json file instead of a db i'm thinking something like this: 1. each user is an object 2. each user has an active array, 0,1,2 3. each user has a backlog array 0,1,2,3,... each priority is it's own object include 1. type task, proposal, etc 2. title