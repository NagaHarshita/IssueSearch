 URL : "https://github.com/davezuko/react-redux-starter-kit/issues/1322" TITLE : multiple paths to a route BODY : guys, can a route have multiple paths? for example, i want 'test/:id' and 'test/blahblah/:name/:id' to go to the same page and have the same code executed. export default { path: '/test/:id', path: '/test/blahblah/:name/:id', async action { params } { return { title: 'test', component: <test params={params} />, }; }, };