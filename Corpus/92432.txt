 URL : "https://github.com/davidkpiano/react-redux-form/issues/939" TITLE : working with react-select BODY : the problem can't make the react-redux form work with react-select steps to reproduce expected behavior i would like the form to send value from react-select. actual behavior the props are passed down to my react-select component, but the values in form aren't updating. reproducible code example class searchdropdown extends react.component { constructor props { super props ; this.state = { value: this.props.options 0 , searchable: this.props.search, multiselect: this.props.multi } } updatestate element { this.setstate { value: element } } render { const {model, dispatch} = this.props; let options = ; for let i = 0; i < this.props.options.length; i++ { let option = { value: this.props.options i , label: this.props.options i } console.log option ; options.push option ; } return <select multi={this.state.multiselect} name= form-field-name model= .ziom value={this.state.value} options={options} searchable={this.state.searchable} onchange={this.updatestate.bind this } oncustomchange={e => dispatch actions.change model, e } /> ; } } and component, where i use it : <formgroup> <control component={searchdropdown} model= .example controlprops={{ search: false, multi: false, options: sex.selectoptions }} /> </formgroup> i think i wrapped it correctly, becouse the props are passed down to react-select component, but onsubmit values , doesnt seem to include my model.