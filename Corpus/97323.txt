 URL : "https://github.com/cfpio/callForPapers/issues/161" TITLE : get hosted on clever-cloud BODY : we can host on google cloud, so clever could as well we can't use docker-compose anymore ? how to get hosted with : - static frontend s content - we'd like to split ui development in 3 distinct projects - ngingx config and certificate maybe we should have ci to assemble them altogether as a single app / docker image - mysql db migration