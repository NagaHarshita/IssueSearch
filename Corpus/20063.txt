 URL : "https://github.com/jmcgover/k-rap/issues/8" TITLE : better data format BODY : - single isolate, all params, one algorithm - all isolates, one param set, one algorithm - all isolates, one param set, all algorithms