 URL : "https://github.com/unosquare/passcore/issues/92" TITLE : run with existing website BODY : hi everyone. is there a way to run passcore on an existing web site by adding the application? for instance, we have https://web.domain.org. in iis can i install passcore as https://web.domain.org/password it runs fine if we put pass core in as its own website, but as an application added to an existing site, we only get loading passcore... thanks, james