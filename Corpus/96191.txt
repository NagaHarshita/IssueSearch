 URL : "https://github.com/INWT/dbtools/issues/26" TITLE : encoding für die senddata BODY : in der senddata wird die sendquery verwendet, um die sqlloaddata zu konstruieren. aus der gleichen motivation wie für 19 sollten wir darüber nachdenken das encoding als argument aufzunehmen. die data.table::fwrite sieht kein argument für das encoding vor. eventuell wird dort der system-default verwendet, was dann beim einlesen zum problem wird. auch setzen wir über die sendquery das set names statement ab. das kann bei bestimmten serverkonfigurationen zu problemen führen. was tun mit encoding in senddata ?