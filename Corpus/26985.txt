 URL : "https://github.com/Leinvedan/FactionWars/issues/3" TITLE : city placer changes BODY : make the backtrack algorithm try all the possibilities and keep the one that placed most cities. so there will be array.lenght cities at the best the algorithm stops if it gets there . make the backtrack distinguish between stop by invalid position and stop by water. the first line is most likely to be water-only so the algorithm will need to ignore that line