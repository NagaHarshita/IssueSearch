 URL : "https://github.com/zotonic/z_stdlib/issues/18" TITLE : make hex package BODY : we should make a hex package for z_stdlib. we should use the 'zotonic' user on hex.pm for this @ddeboer has the keys :- /cc @arjan