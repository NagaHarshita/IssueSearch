 URL : "https://github.com/will/crystal-pg/issues/105" TITLE : hang on reading numeric BODY : cr require pg puts opening connection pg_db = pg.connect postgres://traikoa:traikoa@localhost puts creating table pg_db.exec <<-sql create table if not exists array_test position numeric ; sql puts inserting values pg_db.exec <<-sql insert into array_test position values '{0.1, 0.2, 0.3}' ; sql puts running query pg_db.query_one select position from array_test do |rs| puts rs puts rs.read array float64 end puts cleaning up pg_db.exec <<-sql drop table array_test; sql puts closing connection pg_db.close yielding: $ cr src/test.cr opening connection running query <pg::resultset:0x559743c2acc0> where it will then hang indefinitely. switching to float or similar resolves the issue ; it seems to be specifically with numeric workaround for needing numeric -like type is to use jsonb instead and read json::any .to_a and handle that. environment: $ cr -v crystal 0.23.1 2017-07-17 llvm 4.0.1 $ shards .... using pg cafe751c8dd33b988d57a2802506ac394cb84872 ... $ pacman -q postgresql postgresql 9.6.3-3 observe in above, i'm using crystal-pg master as i read about the fix from 100 / 98 no clue where to look to fix this / debug it further :sweat_smile: if i can provide any other information / debugging, let me know.