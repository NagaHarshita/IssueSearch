 URL : "https://github.com/Firebottle/Firebot/issues/371" TITLE : filterable effects lists BODY : right now we have all effects in a dropdown menu, but as that list gets longer it gets harder to use. we need to convert this dropdown menu to a filterable list. the filters would be based on the dependency for that effect, for example interactive would show all effects that require interactive connection.