 URL : "https://github.com/salk31/RedQueryBuilder/issues/46" TITLE : select if boolean BODY : hello, i test your project but with queries returning a boolean example: select if validation = 1 and aat.id_type_account = 3 , 1,0 from spc_da sd left join akao_account aa on aa.ct_num = fournisseur left join akao_acc_type aat on aat.id_account = aa.id_account where service={testservice} and sd.id_spc = ? it's possible with your project?