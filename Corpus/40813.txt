 URL : "https://github.com/DivanteLtd/open-loyalty/issues/13" TITLE : vagrant recipe issue BODY : after launching the vagrant recipe there you get 2 issues: - no composer executed - although it's in dockerfile script - wrong permissions not writeable backend/var/cache - it's probably because dockerfile for backend sets it up - if composer had been run i guess it created var/cache for it's purposes and maybe then the permission settings works just fine because in git repo there is no var/cache folder at all - something must created it but with wrong permissions tested on macosx @colejarczyk