 URL : "https://github.com/ethereum/ethereumj/issues/867" TITLE : upgrade google guava BODY : currently, ethj is using guava 16.0.1 the issue is that ethj is using some functions something about threads that don't exist anymore in guava 21 as a result, if your project imports ethj + guava 21, then mining silently fails. if you debug, you realize that you get an exception methodnotfound while starting light mining but the application hangs because it happens in a separate thread