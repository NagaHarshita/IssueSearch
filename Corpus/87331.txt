 URL : "https://github.com/kontena/kontena/issues/2600" TITLE : kontena master ssh glitch BODY : when performing an installation on digital ocean following the ubuntu https://www.kontena.io/docs/getting-started/installing/ubuntu.html installation instructions, all works well except one pesky detail; dfroberg@localbox:~$ kontena master ssh permission denied publickey . dfroberg@localbox:~$ kontena master ssh -i ~/.ssh/id_rsa permission denied publickey . unsurprising as do apparently don't include the ubntu user on ubuntu 16.04.2; dfroberg@localbox:~$ kontena master ssh -i ~/.ssh/id_rsa -u ubuntu permission denied publickey . while; dfroberg@localbox:~$ kontena master ssh -i ~/.ssh/id_rsa -u root welcome to ubuntu 16.04.2 lts gnu/linux 4.4.0-81-generic x86_64 it might lessen peoples utter confusion if this is included in the manual : additionally it would be awesome if the installation steps somehow made kontena-master aware of these deviations from the norm and configured the kontena cli accordingly.