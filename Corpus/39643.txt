 URL : "https://github.com/Idate96/Numerical_methods/issues/5" TITLE : solution fem changes BODY : if the solution fem in poisson.py is calculated multiplying: lag_base = basis.lagrange x print 'lagrange basis : ', lag_base the basis seems fine phi = 0 for i in range n : phi += c i basis.basis i yields a very different solution from: c = c.reshape n+1, 1 phi = np.sum c lag_base, axis=0 while they theoretically should be the same