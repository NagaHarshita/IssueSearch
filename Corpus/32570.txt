 URL : "https://github.com/substack/adventure-verify/issues/4" TITLE : verifying ava tests BODY : i'm trying to verify tests exercises with ava https://github.com/avajs/ava so the success test never passes. example: writing a test for the function pizzamaker n, cb , that calls the callback cb exactly n times. n can be any number you want in your test code. function: var pizzamaker = n, cb => { if n < 1 { return cb null, true ; } return cb 'error' ; }; test: test.cb '0', t => { pizzamaker 3, err, result => { t.deepequal result, undefined ; t.end ; } ; } ;