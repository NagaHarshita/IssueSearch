 URL : "https://github.com/schul-cloud/arix-search-adapter/issues/6" TITLE : create resource url BODY : the resources requested from the search adapter have no valid url. the url of the adapter should be passed through. these are different approaches: 1. ask the arix backend about the url. pass the url though to the client. 2. create a link to the search adapter which redirects to the link.