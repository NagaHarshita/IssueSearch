 URL : "https://github.com/FabricElements/skeleton-image-uploader/issues/15" TITLE : create _remove method BODY : update the component: create the property exist , is a boolean with reflecttoattribute that needs to validate if an image exist on storage using the path property --- the component needs a _remove method that need to use the path property to delete the image from storage. -- update the demos: add a button to remove the image just like the one we are using to upload the image.