 URL : "https://github.com/tabalinas/jsgrid/issues/672" TITLE : global variable issue BODY : just discovered a bit weird issue on jsgrid.js line 217 there is no var added to args = ; so i wasn't able to run lib with browserify after adding var args = ; all issues gone and code runs perfectly. js rendertemplate: function source, context, config { args = ; for var key in config { args.push config key ; } args.unshift source, context ; source = getorapply.apply null, args ; return source === undefined || source === null ? : source; }