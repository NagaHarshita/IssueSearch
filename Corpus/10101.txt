 URL : "https://github.com/devongovett/pdfkit/issues/724" TITLE : appending to pdf BODY : i am writing a application i.e. pdf file is created when user enters data from html form. now the pdf file is created. when second user enters data then the information must be append to that pdf. so how can i do this. here is my sample code : app.post '/api/file',urlencodedparser, function req, res { console.log req.body.title ; var doc = new pdfdocument { size: 'letter' } ; fs.exists 'will.pdf', function exists { if exists { console.log file exits ; <here what should i do> } else { doc.pipe fs.createwritestream 'will.pdf' ; doc.text req.body.title ; doc.end ; console.log file not exits ; } } ; res.render 'form', { title: 'express' } ; } ;