 URL : "https://github.com/mde/ejs/issues/302" TITLE : ejs repeat include BODY : i have tabs bootstrap 4, it includes a partial in which a table is created dynamically, when i ready the table referring to the database and i go to another tab and then i return to the tab in which i consult and graph the table dynamically, partial, ie the partial clean in the first instance and the same partial with the result of the query to the database listed in the html table, what am i doing wrong?