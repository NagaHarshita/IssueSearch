 URL : "https://github.com/facundoolano/socketio-auth/issues/34" TITLE : postautheticate question BODY : i want to send the userid from the client to server,so after the user is athenticated ,from then clinet side i want to emit the userid to the server .i would like to ask if there is a the right way of using postauthentcate?i used like this require 'socketio-auth' io, { authenticate: function socket, data, callback { if data.password=='123' { console.log 'authenticated' ; return callback null, true ; }else{ return callback new error 'wrong password' ; } }, postauthenticate:function socket, data { socket.on 'get-userid',function userid { console.log 'welcome user : '+userid.userid ; } ; } } ;