 URL : "https://github.com/robme91/ebp-berlincorner/issues/3" TITLE : https für meteor BODY : das problem ist, dass chrome keinen standortzugriff zulässt über eine http verbindung. da auf einem android smartphone aber nur chrome läuft als browser, können wir keine standortabfrage machen sonst... also muss eine https verbindung her. die meteor anwendung sollte ein ssl zertifikat bekommen, damit meteor lokal zur entwicklung als https localhost laufen kann.