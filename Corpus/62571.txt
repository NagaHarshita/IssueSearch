 URL : "https://github.com/termux/termux-packages/issues/1010" TITLE : consider setting tmpdir BODY : many programs needs to create temporary files, termux packages are patched to use @termux_prefix@/tmp instead of /tmp, but external compiled programs like rust programs are not. setting tmpdir in @termux_prefix@/etc/profile? would allow these programs works without users intervention.