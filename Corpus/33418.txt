 URL : "https://github.com/jwmoffat/ArrowConsoleMenu/issues/6" TITLE : add onselected event BODY : add feature to fire an event when a list's item is chosen. this is useful if another list needs to update / refresh itself based on that. _or maybe even some other reason_ perhaps something like: csharp var choices = new menuchoices<string> directory to use , new list<string> { .. , .. } ; choices.onselected += new choiceselectedeventhandler directorychanged ;