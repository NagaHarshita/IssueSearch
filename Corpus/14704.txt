 URL : "https://github.com/tenbaht/sduino/issues/10" TITLE : build for windows BODY : hey. i could not install until the end. the board is installed, but gives an error warning: board sduino:stm8:stm8sblue doesn't define a 'build.board' preference. auto-set to: stm8_stm8sblue warning: board sduino:stm8:stm8sdisco doesn't define a 'build.board' preference. auto-set to: stm8_stm8sdisco recipe.preproc.macros не найден шаблон could collect the necessary files from the folder in order to simply copy the whole to the folder arduino and it all worked