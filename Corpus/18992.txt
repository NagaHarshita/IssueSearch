 URL : "https://github.com/satijalab/seurat/issues/253" TITLE : vlnplot in a loop BODY : hi, i need to plot a set of genes dynamically determined using program . but cannot show p when doing in a loop. pdf 'test.pdf' seruat_sub <- genelist.seurat.12 genelist.seurat.12 %in% all_genes for i in 1:ceiling length seruat_sub /6 { if i 6 < length seruat_sub { vlnplot object = myproject, features.plot = seruat_sub i-1 6+1: i 6 } else { vlnplot object = myproject, features.plot = seruat_sub i-1 6+1:length seruat_sub } } dev.off however, individual plot command will work: >>> i = 1 >>> vlnplot object = myproject, features.plot = seruat_sub i-1 6+1:length seruat_sub