 URL : "https://github.com/CS2103JAN2017-T16-B3/main/issues/129" TITLE : code review for a0143076j BODY : 1. in general, magic numbers and magic strings can be found in your code. 2. i have an issue with your alternative command class. this class requires access to all existing commands in your code base. notice that this does not allow ocp, and increases the dependencies between this command and all the command classes. you should think of a way to invert the dependency. 3. the parse functions in your parser classes require slap. they are currently doing different levels of work in one function. otherwise, good work.