 URL : "https://github.com/shelljs/shx/issues/119" TITLE : feature: symbolic links BODY : i need to create a symbolic to a relative folder. here is how i do it with windows in mind: js const dirfileorjunction = 'dir'; fs.symlink targetpath, linkpath, dirfileorjunction, err => { if !err { return; } if !process.platform.startswith 'win' { throw err; } // error on windows child_process.exec mklink /d ${linkpath} ${targetpath} , err, stdout, stderr => { if !err { return; } // try junction will be absolute . child_process.execsync mklink /j ${linkpath} ${targetpath} ; } ; }