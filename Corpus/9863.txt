 URL : "https://github.com/fengalin/media-toc/issues/56" TITLE : add media export BODY : export the media with its chapters. options: - export all / a selection of the available streams to a compatible container. the application proposes a set of containers that conform to the selection. - export all / a selection of the available streams to separate files in a compatible container. the application proposes a set of containers that conform to the selection.