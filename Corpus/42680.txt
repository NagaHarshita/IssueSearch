 URL : "https://github.com/ErikTillberg/Project-Schwarma/issues/17" TITLE : build cards generator BODY : build a generator for cards so that each time a new card is needed initial card handoff to new players, when new cards are used as reward , one is generated with random stats for attack, defence, mobility. once the card is generated, it gets added to the cards model, and a reference to it is given to the user.