 URL : "https://github.com/electron/rcedit/issues/36" TITLE : add appveyor support BODY : this repository needs ci, to ensure that it compiles correctly. a side effect would be that it can be set up to auto-deploy precompiled binaries in tagged releases. _ it also needs tests to make sure it changes an executable as intended, but perhaps that should be addressed in a separate issue. _