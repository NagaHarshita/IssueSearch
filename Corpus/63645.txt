 URL : "https://github.com/jasonmayes/Twitter-Post-Fetcher/issues/118" TITLE : error callback function BODY : hi, just a small suggestion here: specifying a custom error callback function in the config. something like that _ in the twitterfetcher.js _: if data.body === undefined { if customerrorcallbackfunction !== null { customerrorcallbackfunction ; } return; } instead of: if data.body === undefined { return; } it would be even better to have informations about the error. thank you and good job by the way.