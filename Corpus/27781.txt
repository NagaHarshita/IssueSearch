 URL : "https://github.com/nicolaiparlog/energy-model/issues/15" TITLE : enable maven builds BODY : to professionally process our project we want to build it with maven. the goal ist to be able to successfully build the project with mvn clean install and then execute it with java -jar <resulting-artifact> . travis was already activated https://travis-ci.org/nicolaiparlog/energy-model and the first build failed https://travis-ci.org/nicolaiparlog/energy-model/builds/230987235 because the project contains no pom.xml . travis will build all pr's and if as soon as maven is correctly configured, the build will pass.