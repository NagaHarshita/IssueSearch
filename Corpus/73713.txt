 URL : "https://github.com/Abjad/ide/issues/159" TITLE : debug empty-% navigation BODY : currently akasha > ss > % causes the ide to navigate into the build directory. and mraz > ss > % causes the ide to navigate into the distribution directory. mráz has no build directory. the pattern in both cases is that empty-% navigation from the main score package directory causes the ide to navigate into the first named directory. find out why this is. then block the behavior by causing the ide to do nothing or issue in a warning in response to empty-% navigation.