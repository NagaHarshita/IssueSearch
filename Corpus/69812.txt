 URL : "https://github.com/craigk5n/webcalendar/issues/63" TITLE : tz environment variable BODY : on page: http://php.net/%20date_default_timezone_set note: since php 5.1.0 when the date/time functions were rewritten , every call to a date/time function will generate a e_notice if the timezone isn't valid, and/or a e_warning message if using the system settings or the tz environment variable. so, maybe we should stop using set_env 'tz'