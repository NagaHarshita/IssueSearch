 URL : "https://github.com/dtschump/CImg/issues/138" TITLE : errors with saving BODY : attempts to save as a bmp result in the following error: cimg cimgioexception cimg::fopen : failed to open the file '~/tiles/0/0/0.bmp' with mode 'wb'. as a result of the following code to call the save function: std::string directorycommand = mkdir -m777 -p ~/tiles/ + boost::lexical_cast<std::string> zoom + / + boost::lexical_cast<std::string> x ; std::string filename = ~/tiles/ + boost::lexical_cast<std::string> zoom + / + boost::lexical_cast<std::string> x + / + boost::lexical_cast<std::string> y + .bmp ; const char file_o = cimg_option -o , filename.c_str , output file ; system directorycommand.c_str ; std::cout << made required dirs ; cimg<unsigned char> imageclone = image.get_resize 256, 256, -100, -100, 1 ; std::cout << scaled image ; imageclone.save file_o ;