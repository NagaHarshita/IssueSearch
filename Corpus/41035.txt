 URL : "https://github.com/swiftyspiffy/TwitchLib/issues/212" TITLE : whisper doesn't send BODY : i'm attempting to send a whisper in an onmessagereceived handler, but it's not sent. no error in the logs; the logs don't indicate anything related to the whisper at all. not sure what's going on. i enabled logging with client.logging = true; , not sure if there's another way to enable debug logging. async void onmessagereceivedwhisper object sender, onmessagereceivedargs e { var user = e.chatmessage.username.tolowerinvariant ; // do a bunch of filtering, i'm not whispering everyone on every message client.sendwhisper user, test ; }