 URL : "https://github.com/OpusCapita/onboarding/issues/159" TITLE : campaign launch failure BODY : 1 workflow.js l214 join campaign to campaigncontact and then inject 'c_' + campaign.customerid as tenantid into the email template 2 adjust email template to use tenantid in landingpage and transition api urls 3 /public/landingpage/:tenantid/:campaignid/:contactid strip the tenant prefix c_ from the tenantid in order to generate customerid edited 4 exactly as 3 , strip c_ and get customerid, error message if not c_ 5 /api/campaigns/start/:campaignid add customerid to the query as where condition, get customerid from current user req.opuscapita.userdata 'customerid'