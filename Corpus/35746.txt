 URL : "https://github.com/iCalcreator/iCalcreator/issues/32" TITLE : facebook events feed BODY : i'm trying to load a ical upcoming facebook events feed. i use icalcreator with composer, and this is my php file: require_once 'vendor/autoload.php' ; $v = new vcalendar ; $v->setconfig 'url', 'http://www.facebook.com/ical/u.php?uid=..&key=..' ; $v->parse ; $v->sort ; while $comp = $v->getcomponent { print_r $comp ; } i don't get any errors, but also no output. how can i retreive the events, so that i can save it in a mysql database. i also want to sync the events, like for instance every day.