 URL : "https://github.com/EddyVerbruggen/Calendar-PhoneGap-Plugin/issues/373" TITLE : ios status overlaps ui BODY : on ios 10 the statusbar overlaps the ui when the device's calendar is closed? the css is _position:fixed;_ for the header. the header is not overlapped by the status bar before window.plugins.calendar.createeventinteractively is called any ideas on how to solve this?