 URL : "https://github.com/sindresorhus/escape-goat/issues/4" TITLE : add template literal tags BODY : before: js const g = require 'escape-goat' ; const escaped = <html a=${g.escape a }><b>${g.escape b }</b> ${g.escape c }</html> ; after: js const g = require 'escape-goat' ; const escaped = g.escapetag <html a=${a}><b>${b}</b> ${c}</html> ; the code required is minimal and this can be improved further: js module.exports = function eskape strs, ...vals { var out = strs 0 ; for let i = 0; i < vals.length; i++ { out = out + escape vals i + strs i + 1 ; } return out; } > code from https://github.com/wbinnssmith/eskape/blob/master/src.js