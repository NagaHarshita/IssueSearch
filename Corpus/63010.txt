 URL : "https://github.com/Microsoft/sqlopsstudio/issues/244" TITLE : database load times out BODY : - sql operations studio version: 0.23.6 steps to reproduce: connected to azure database servers expanding the databases list almost always gives a 45 second timeout message and when it does load it is way slower than ssms. we have 40+ on one server and 100+ on another server. both timeout consistently.