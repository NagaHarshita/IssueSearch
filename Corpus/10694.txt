 URL : "https://github.com/chrmarti/testissues/issues/4675" TITLE : custom menu item BODY : is possible to make extension have custom menu item in the explorer？ here's i want to do in extension: when user right click a folder on explorer, show an custom menu item, then user click a item import template . pop out a dialog which shows a list of template html, js then user select a template to import the template code into that folder.