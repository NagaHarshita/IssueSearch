 URL : "https://github.com/Vayel/wello/issues/30" TITLE : cache de base de données BODY : actuellement, on lit en boucle dans la base de données pour récupérer le niveau d'eau par exemple : 1. le module io reçoit la valeur par série 2. le module model écrit la valeur en base 3. le module controller lit la valeur en base il est inutile en 3 de lire en base, on pourrait stocker la valeur en mémoire vive. les valeurs en base serviraient juste aux statistiques.