 URL : "https://github.com/mvdwg/ember-cli-updater/issues/16" TITLE : update ember-try config BODY : we can use the same four steps to update config/ember-try.js automatically 1. fetch original 2. calculate customizations 3. download new one 4. apply customizations we could use an ast differ.