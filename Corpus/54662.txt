 URL : "https://github.com/huridocs/uwazi/issues/845" TITLE : handle indexing errors BODY : if an errors happens during a new entity / document indexation, the document wont show up anywhere until a manual re-indexation is done. we need to handle this errors for a re-try.