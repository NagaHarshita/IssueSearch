 URL : "https://github.com/AndroidTamer/Tools_Repository/issues/234" TITLE : frida server push BODY : a small script which should do something like adb shell uname -a parse output get type of android device wget the latest version of frida-server same as the frida version we have installed. x86, arm, _64 push it to /data/local/tmp on adb connected device adb shell chmod 755 /data/local/tmp/frida-server adb shell /data/local/tmp/frida-server & additionally find the pid of frida-server store it somewhere and kill it when called for.