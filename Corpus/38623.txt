 URL : "https://github.com/ioam/holoviews/issues/1389" TITLE : problems with stream BODY : i installed via conda ananconda3-4.3.1-linux-x86_64 on ubuntu 17.04 with conda install -c ioam holoviews bokeh this installed holoviews 1.6.2-py36_0 ioam hoever, examples from holoviews-contrib will not run, since from holoviews.streams import stream will fail with a missing module streams. checking within site-packages/holoviews reveals, that the files streams.py und util.py as availaible on github are missing. if i copy them there manually from github, then stream fails with a missing disable_constant attribute in holoviews.core.util .