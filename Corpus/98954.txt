 URL : "https://github.com/twbs/bootstrap/issues/23999" TITLE : logic of grid gutter units BODY : what is the reason of using px in $grid-gutter-width? all other components are using rem units for horizontal spacing navbar, navs etc , so if i want to align padding in components: navbar + content container with nested grids + footer, i need to add left/right padding to my container and footer in rem value, but nested grids will have padding in px. what's the reason behind this?