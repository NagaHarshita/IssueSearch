 URL : "https://github.com/lmworster/minesweepr/issues/4" TITLE : board updating interface BODY : functions for updating a board display for: - a content update when something is learned about what is in a tile - a flagging action where the user flags or un-flags a tile these could be done a side-effect-bearing methods registered with each tile or as global functions which return an element when called which can then be assigned to the table cell which represents the tile which is being updates.