 URL : "https://github.com/justinethier/cyclone/issues/237" TITLE : macro hygiene issues BODY : two macros from r7rs with issues: this one works fine from eval and if if is renamed, but the following does not compile correctly: let-syntax given-that syntax-rules given-that test stmt1 stmt2 ... if test begin stmt1 stmt2 ... let if t given-that if set! if 'now if ;; => now the following returns inner instead of outer : write let x 'outer let-syntax m syntax-rules m x let x 'inner m ;; should be outer