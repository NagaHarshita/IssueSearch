 URL : "https://github.com/shpongle2634/JexcelUnit/issues/2" TITLE : excel parsing issue. BODY : some method may have custom parameter types. so if the cell in excel need to convert to custom type, we have to customizing the data. how can i handle them? 1. if parameter type is primitive type like int, double, char... including wrapper types convert them 2. if not, just hold them as string type. it may be mock name or custom types. it needs another idea.