 URL : "https://github.com/cznic/ql/issues/177" TITLE : composite unique index ? BODY : is it possible to create unique index on 2 columns ? for example create table test first_name string, last_name string ; create unique index if not exists uniquename on test first_name,last_name ;