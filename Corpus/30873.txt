 URL : "https://github.com/sholloway/graph-cockpit/issues/11" TITLE : register a new user BODY : when a user launches the application for the first time they should be prompted to create a new user profile. this should prompt them to specify a: - email address - first name - last name - username - password upon providing the required information: 1. the application should orchestrate registering the user in the property graph via the engine's identity service. 2. once the user is registered and session token should be gotten from the identity service. 3. using the session token, a web socket connection should be established.