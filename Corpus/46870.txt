 URL : "https://github.com/vjeeva/uphold/issues/5" TITLE : need a job dispatcher BODY : need to be able to schedule jobs, pipeline needed with a message queue a dispatcher service, etc. must create a json type job that can be sent from the ui, or by api, etc, into a message queue sqs or rabbitmq , which will send a message to a dispatcher service always listening and it will create the containers to test, just as the ui does right now.