 URL : "https://github.com/ivolimasilva/public-instagram/issues/6" TITLE : get username by id BODY : many endpoints that return posts, like tags.recent ... , have a property called owner that has another property called id . however all user's endpoints use username as an identifier. in order to use user's endpoints after retrieving posts, it's needed an endpoint to get username by id .