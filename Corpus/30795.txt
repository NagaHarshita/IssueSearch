 URL : "https://github.com/OWASP/Top10/issues/132" TITLE : rc2 - a1 injections BODY : https://github.com/owasp/top10/blob/master/2017/en/0xa1-injections.md under access lvl | exploitability , i think removing administrators would be better as they don't need to use injection to walk away with your database. also, someone new would start questioning your threat model they want us to protect against our own admins??? the best way to find out if an application is vulnerable to injection is to verify that all use of interpreters clearly separates untrusted data from the command or query i would drop the word untrusted in untrusted data as you may end up protecting against sql injection and then read back the data and use it in another interpreter where it can then abuse the application's trust in data coming from the database. how do i prevent i would add that you should be be aware when the control will require additional handling as well limit when using parameterized queries, etc.