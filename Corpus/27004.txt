 URL : "https://github.com/postmarketOS/pmbootstrap/issues/436" TITLE : clean up apkbuild s BODY : @pablocastellano wrote in https://github.com/postmarketos/pmbootstrap/pull/435 discussion_r134242611: > i've tested that you can just remove build and nothing bad happens. i don't know why a lot of aports are defining it as return 0 instead of just removing it 🤔 besides, some apkbuild s probably still have the || return 1 after the commands, which is obsolete since a few months or so upstream change in alpine's abuild . we should go through all apkbuild s and modernize them.