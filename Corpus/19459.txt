 URL : "https://github.com/isaac211/Marvin/issues/1" TITLE : add co sensor code BODY : add the co sensor code to main.py search for todo william to find where it needs to go. i have an event loop setup so the client asks for data. whenever it does, you compute the co percentage. choose a threshold when the light should be red if it is above. that should be everything. i have the server already set up to toggle the light if needed.