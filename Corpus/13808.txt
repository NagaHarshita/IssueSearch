 URL : "https://github.com/AMVSoftware/Cake.SqlServer/issues/39" TITLE : .net core support BODY : scripts that use cake.sqlserver cannot be run or debugged using coreclr. can we add a netstandard1.6 build to the package? it's okay if things like dacpak are missing initially; i don't use them. willing to help/pr.