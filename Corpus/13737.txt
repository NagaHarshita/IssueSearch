 URL : "https://github.com/NextLevel/NextLevel/issues/100" TITLE : xcode 9.0 issues BODY : xcode 9.0 i encounter type issues about 75 of them : swift avmediatype' aka 'nsstring' is not implicitly convertible to 'string!'; did you mean to use 'as' to explicitly convert? in : swift extension avcapturedeviceinput { /// returns the capture device input for the desired media type and capture session, otherwise nil. /// /// - parameters: /// - mediatype: specified media type. i.e. avmediatypevideo, avmediatypeaudio, etc. /// - capturesession: capture session for which to query /// - returns: desired capture device input for the associated media type, otherwise nil public class func deviceinput withmediatype mediatype: avmediatype, capturesession: avcapturesession -> avcapturedeviceinput? { if let inputs = capturesession.inputs as? avcapturedeviceinput { for deviceinput in inputs { if deviceinput.device.hasmediatype mediatype { return deviceinput } } } return nil } }