 URL : "https://github.com/isula/Home/issues/2" TITLE : go 2 rpm BODY : we need to have a project to a convert go application to a rpm. the reason is 'go' is much simpler than c/c++ in compiling and most of the projects are host remotely on github. it seems not necessary to have a local 'source' in rpm any more. we just need to have a spec file and do most of the job in the 'prep' session. several steps: 1. git clone 2. go build 3. the etc..