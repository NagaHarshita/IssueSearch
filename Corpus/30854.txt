 URL : "https://github.com/zaneriley/smartertype.com/issues/3" TITLE : refactor type utils BODY : functions should be split up so they do a single thing. functions should return consistent, immutable result. right now, multiple functions return px and rem values.