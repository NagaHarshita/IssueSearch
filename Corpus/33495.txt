 URL : "https://github.com/Sciss/Numbers/issues/5" TITLE : add sc-style modulus BODY : scala def mod a a: a, b: a implicit num: integral a : a = { import num._ if gteq a, zero a % b else { val c = -a % b if c == zero zero else b - c } } scala