 URL : "https://github.com/secure-software-engineering/rose/issues/285" TITLE : add engage interaction BODY : someone at cscw asked, if we can track if people read posts on facebook. should be straihgt forward to add an observer that tracks all links in a post's content. would be best to call it engage-post, instead of read because we can't really say how a user engages with the post. there are two options, how to implement this: track all clicks on posts track only outgoing links exclude finternal facebook links, like a photo, photo-album, or else we could easily hook up the status-update extractor as well.