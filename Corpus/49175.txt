 URL : "https://github.com/ga-wdi-boston/game-project/issues/639" TITLE : switching turns x & o BODY : i'm having a difficulties with switching between x & o. the bottom code is what i am using and i'm only receiving o's on the board. appfunction.js js let currentplayer = 'x' if currentplayer === 'x' { currentplayer = 'o' } else { currentplayer = 'x' } events.js js const cellclick = function event { // const $cell = $ event.currenttarget // $cell.addclass 'cell' + appfunction.currentplayer if appfunction.currentplayer === 'x' { return $ this .text 'x' } else { return $ this .text 'o' } }