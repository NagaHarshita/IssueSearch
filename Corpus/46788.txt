 URL : "https://github.com/hlissner/.emacs.d/issues/141" TITLE : moving to open popups BODY : i guess this is more a question than an issue. how do you move the cursor to an open popup? currently, when switching windows with c-x o other-window , buffers defined as unreal like popups are not considered as candidates for switching. for example, if i open the help buffer that describes de major and minor modes c-h m describe-mode , how would you switch between the help popup and the main buffer window?