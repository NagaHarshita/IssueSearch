 URL : "https://github.com/dominikh/go-tools/issues/2" TITLE : avoid vendor subdirectory BODY : i use go's vendoring with glide to retrieve version pinned package dependencies, into a vendor subdirectory. gosimple and staticcheck both require the vendor directory to be correct populated before i can successfully run them. i have project specific packages in subdirectories which i would like to include when i run gosimple. however, using gosimple ./... also includes the vendor subdirectory, so i get a lot of messages from 3rd party packages. is there an elegant way to run gosimple over ./... yet exclude the vendor directory? thanks.