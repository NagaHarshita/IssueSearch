 URL : "https://github.com/openshift/openshift-docs/issues/5704" TITLE : pv for openshift metrics BODY : according to https://docs.openshift.org/latest/install_config/cluster_metrics.html metrics-ansible-role there is a pv for persistent volumes, which need to be created before the installation , but there is no information on which parameters we need to setup for it: name, access mode, claimref etc. i have no idea what pv to make, so the ansible installation of cassandra would be able to mount it using it's pvc