 URL : "https://github.com/zenorocha/clipboard.js/issues/444" TITLE : copy issue! important!!!!! BODY : minimal example <button class= btn btn-xs default btn-copy type= button data-clipboard-text= test >copy key <i class= fa fa-key ></i></button> var clipboard = new clipboard '.btn-copy' ; clipboard.on 'success', function e { console.log e.text ; console.log e.action ; $ e.trigger .tooltip { title: 'copied!', trigger: 'manual', placement: 'bottom' } .tooltip 'show' ; } ; i follow the example to write the demo on jquery modal expected behaviour 1. i expected that the browser can print the 'test' value. 2. i expected that i can use the ctrl+v to paste the test on my computer. actual behaviour 1. it can print the test value on browser. 2. it failed to paste the test value on my computer. browsers affected it does not work on all of browser.