 URL : "https://github.com/PayFast/mod-wp_ecommerce_3/issues/3" TITLE : gateway update needed BODY : mihai here with wp ecommerce. i m just checking this cause we had a customer asking if this will work with wp ecommerce. took a quick look and it seems to work but it might need an update. at the moment to install it you have to copy the files in the core plugin and that in case of a plugin update will remove all the gateway files and will have to be reinstalled. it should be rewritten as a standalone plugin to avoid that.