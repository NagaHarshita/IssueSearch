 URL : "https://github.com/angular/angular/issues/15957" TITLE : pure components BODY : <!-- if you don't fill out the following information we might close your issue without investigating --> i'm submitting a ... check one with x bug report => search github for a similar issue or pr before submitting x feature request support request => please do not submit support request here, instead see https://github.com/angular/angular/blob/master/contributing.md question current behavior sorry if this request has already been asked, but i didn't find any similar topic. i have a parentcomponent and and a childcomponent . my parentcomponent hold an elementmodel , passed to my childcomponent using an input property. when i modify my elementmodel inside my childcomponent , it's modified inside my parentcomponent too expected behavior elementmodel should be modified only in the childcomponent . what is the motivation / use case for changing the behavior? - the components must be independents between each others, and stateless. we have to use properties for the inputs and events for outputs, but it's still possible to update an element directly. - if we want to modify our element inside our child without changing the parent element, we must clone it ourselves. - finally, if we have a list of elements and if an element is added to this list, the ngonchange or a setter is not called in our childcomponent angular version: 2.x, 4.x browser: all language: all