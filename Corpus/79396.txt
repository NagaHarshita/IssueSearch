 URL : "https://github.com/quilljs/quill/issues/1422" TITLE : setselection not working BODY : can anybody tell me why setselection does not select something? export class typeahead { constructor quill, options = {} { // save the quill reference this.quill = quill; this.triggert = false; this.searchword = ''; this.textchange = this.textchange.bind this ; this.quill.on 'text-change', this.textchange ; } textchange delta, olddelta, source { let insert = delta.ops.pop .insert; if insert && typeof insert != 'undefined' && source == 'user' { if insert == ' ' && this.triggert { this.triggert = false; let r = this.quill.getselection true ; let t = this.quill.getleaf r.index 0 .text; this.quill.setselection r.index + t.indexof ' ' , t.lastindexof ' ' - t.indexof ' ' return; } else if insert == ' ' { this.triggert = true; } } }