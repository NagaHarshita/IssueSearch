 URL : "https://github.com/beto-rodriguez/Live-Charts/issues/588" TITLE : axis setrange event args BODY : reading the code and cannot understand these lines: https://github.com/beto-rodriguez/live-charts/blob/master/wpfview/axis.cs l648-l652 the variables are the same and rangechangedeventargs will have zero values for limit changes. var bmax = double.isnan maxvalue ? model.toplimit : maxvalue; var nmax = double.isnan maxvalue ? model.toplimit : maxvalue; var bmin = double.isnan minvalue ? model.botlimit : minvalue; var nmin = double.isnan minvalue ? model.botlimit : minvalue; var e = new rangechangedeventargs { range = nmax - nmin, rightlimitchange = bmax - nmax, leftlimitchange = bmin - nmin, axis = this };