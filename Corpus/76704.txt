 URL : "https://github.com/djc/askama/issues/9" TITLE : loop index out of bounds BODY : using the fix from 8, something is wrong with the loop index tracking. {% for inner in nested %} outer = {{ loop.index0 }} {% for _ in inner %} inner = {{ loop.index0 }} {% endfor %} {% endfor %} thread 'main' panicked at 'index out of bounds: the len is 1 but the index is 1', /checkout/src/libcollections/vec.rs:1410 from a quick read, it looks like the _loop_cur variable is never decremented.