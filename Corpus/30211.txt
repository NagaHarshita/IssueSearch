 URL : "https://github.com/kelloggm/checker-framework/issues/113" TITLE : offset special case BODY : consider the following code: java public void test double a, double sub { int a_index_max = a.length - sub.length; // has type @ltl value={ a , sub }, offset={ -1 + sub.length , -1 + a.length } for int i = 0; i <= a_index_max; i++ { // i has the same type as a_index_max for int j = 0; j < sub.length; j++ { // j is @ltl sub double d = a i+j ; // i + j is safe here. because j is ltl sub , it should count as -1 + sub.length } } }