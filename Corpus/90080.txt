 URL : "https://github.com/Pomax/react-onclickoutside/issues/207" TITLE : rollup dependency cleanup BODY : its a followup for 203 >there are still rollup dependencies leftovers in the package.json . it should be decided if they should be removed or kept and being used for something. > it can still be valuable to leave it to: > - provide flat bundle single file bundle - keeping internals hidden > - build umd version of the lib, so its easy to include on the webpage and experiment