 URL : "https://github.com/pfnet/chainer/issues/2506" TITLE : chainer usage problem BODY : when i use chainer to train my data, it can run successfully. but i don't know if this usage is correct ? in my model.py, i define def __call__ self, normal_x, sub_x, mini_x, train=false : h1 = self.conv1_1 normal_x h1 = f.relu self.bnorm1_1 h1, test=not train h2 = self.conv2_1 sub_x h2 = f.relu self.bnorm2_1 h2, test=not train h3 = self.conv3_1 mini_x c = f.concat h1,h2 ,axis=1 c = f.concat c,h3 ,axis=1 in my train.py, i input 3 variable. is this step right for me? normal_train = variable xp.asarray normal_img sub_train = variable xp.asarray subway_img mini_train = variable xp.asarray mini_img y_train = variable xp.asarray label_img output = dmn normal_train, sub_train,mini_train, train=true loss = f.softmax_cross_entropy output, y_train