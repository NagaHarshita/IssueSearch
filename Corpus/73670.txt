 URL : "https://github.com/pacifica/pacifica-metadata/issues/4" TITLE : database creation script BODY : pacifica metadata version master pacifica core software versions master platform details any scenario: cherrypy does have a wsgi interface that can be loaded into other services. however, that interface doesn't provide any initialization. we should make an external script to allow administrators to build the schema outside of running cherrypy. steps to reproduce: 1. run metadata service in uwsgi 2. metadata doesn't build database schema expected result: the service should have run as designed actual result: service complains bitterly about not having elements of the schema