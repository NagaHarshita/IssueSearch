 URL : "https://github.com/strongloop/loopback-example-access-control/issues/119" TITLE : automigrate to mongodb BODY : i am in the middle of migrating this project from memory db to mongodb. i managed to autmigrate the user/account property into a mongo collection, as well as, the user, accesstoken and rolemapping , using this script: var path = require 'path' var app = require path.resolve __dirname, '../server' var account = app.models.account var role = app.models.role var rolemapping = app.models.rolemapping var team = app.models.team var ds = app.datasources.mongodb var accounts = {username: 'john', email: 'john@doe.com', password: 'opensesame'}, {username: 'jane', email: 'jane@doe.com', password: 'opensesame'}, {username: 'bob', email: 'bob@projects.com', password: 'opensesame'} ds.automigrate 'account', function err { if err throw err accounts.map function acc { account.create acc, function err, record { if err return console.log err console.log 'record created', record } } } ds.automigrate 'user', function err { if err throw err console.log 'user model migrated' //datasource.disconnect ; } ds.automigrate 'accesstoken', function err { if err throw err console.log 'accesstoken model migrated' //datasource.disconnect ; } ds.automigrate 'rolemapping', function err { if err throw err console.log 'rolemapping model migrated' } i am having problem defining the team and roles properties to automigrate. how do i associate the newly entered account mongodb ids with the teams and roles? i'd appreciate some explanation or point me to a tutorial that explains this problem.