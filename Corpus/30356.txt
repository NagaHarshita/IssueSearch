 URL : "https://github.com/udacity/isdc_issues_tracker/issues/38" TITLE : part 2, lesson 7.12 BODY : what if you wanted to change the size of the array? the code states for example, we can turn the 2d grid from above into a 1d array here, the -1 means fit all values into the length of this array however, .resize truncates the last item if you do np.reshape np_grid, 1, -1 .size i believe the author intended to use .reshape instead. the . reshape documentation and codebase specifically states you can feed -1 to fit all values in the new array.