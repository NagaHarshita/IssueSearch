 URL : "https://github.com/avajs/ava/issues/1497" TITLE : read snapshot content BODY : description it would be cool if the snapshot content corresponding to a snapshot-id could be retrieved via the api. then the following would be possible: - test the backend api via snapshot testing - test the front-end by using the snapshots of the api tests the api could be something like t.getsnapshot snapshotid and would retrieve the object which is usually compared to when using t.snapshot .