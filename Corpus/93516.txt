 URL : "https://github.com/openhab/openhab-cloud/issues/26" TITLE : adding items does not work BODY : per the subject, clicking on the button to add a new item the blue + under configuration > new item does not work when working through the cloud. doing the same operation directly on the oh2 instance works fine, so the setup of the snapshot should be ok. moreover, both instances oh2 and cloud are in this case on my laptop so this means localhost:8080 and localhost:3000, respectively .