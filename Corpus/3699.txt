 URL : "https://github.com/ValarDragon/Koch_Snowflake/issues/1" TITLE : things to add BODY : make configurations changeable via command line make configurations changeable via config file find better default color scheme delay all draw actions until end, with attention to depth order. then add an override for sidelet + not reverse so that will be fixed