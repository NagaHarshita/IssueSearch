 URL : "https://github.com/AlexaCRM/php-crm-toolkit/issues/46" TITLE : how to set lookup fields BODY : how can you set a lookup field value when creating a record? try creating a new entity record and insert the contact search value the record was created, but the lookup field is still empty where is my mistake $client = new client $clientsettings ; $metadata = metadatacollection::instance $client ; $new = $client->entity 'new_testentity' ; $new->new_testentity_name = 'testname'; $new->new_testentity_contact = $contactguid; $newdata = $new->create ;