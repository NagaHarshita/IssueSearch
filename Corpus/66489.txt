 URL : "https://github.com/learn-co-curriculum/react-router-params/issues/8" TITLE : movie show won't render BODY : this line of mapstatetoprops in movie show page const movie = state.movies.find movie => movie.id == ownprops.match.params.movieid won't render as is. since anything in params is a string and the id of a movie is an integer, you would first need to parseint ownprops.match.params.movieid, 10 in order to get the ids to match up.