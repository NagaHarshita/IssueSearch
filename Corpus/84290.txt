 URL : "https://github.com/ethanmoffat/EndlessClient/issues/68" TITLE : create strongly-typed ids BODY : currently, character/player ids and npc indexes are ints. it would be nice if they were strongly typed id objects that represented a legal range of ids. c public struct characterid { int id { get; } public characterid int id : this { if id <= 0 || id > short.maxvalue throw new argumentexception $ id must be between 1 and {short.maxvalue} ; id = id; } }