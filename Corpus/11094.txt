 URL : "https://github.com/Metaburn/doocrate/issues/4" TITLE : filter by labels BODY : ability to filter tasks by single, multiple labels so for example - filter all the art tasks bonus points - if filtering happens on the db end