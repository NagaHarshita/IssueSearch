 URL : "https://github.com/jweinst1/Wind/issues/2" TITLE : method of execution BODY : wind , a stream based language needs a method or algorithm to execute its code. such a method may include translation to a byte instruction set, then execution of that byte code, or incremental direct evaluation. the method of execution should take into account efficient stack vs memory. direct execution for wind , direct execution would consist of directly tokenizing portions or increments of code into windobject 's. this method would require the base object of the wind language to have a next pointer, such that arguments to a command can be accessed: c struct windobject { struct windobject next; };