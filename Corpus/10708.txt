 URL : "https://github.com/xephonhq/xephon-k/issues/46" TITLE : data file reader BODY : tracker 32 related 45 writer need to read what you write - header, actually we don't read header, we read backward to avoid seek, header data magic + version is duplicated in footer - footer - data block - decoding - cache - evict we can't cache everything from disk in memory unless our memory is much larger than disk, in fact, we can let user specify if they are going to query it again - push down aggregation? - x index - index of indexes - indexentries, all decoded using protobuf - indexentry - aggregated value, not stored yet - debug - print index of data - print decoded data