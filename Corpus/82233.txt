 URL : "https://github.com/CodeYellowBV/mobx-spine/issues/10" TITLE : add non-optimistic delete BODY : currently, model.delete immediately removes the model from the store if any , and then does a request to remove it. by default it should wait on the request and then remove it, as long as there is a primarykey set. with the option immediate: true it should not wait on the request.