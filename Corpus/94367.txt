 URL : "https://github.com/kelseyhightower/confd/issues/553" TITLE : can write to env variable? BODY : hi, i have a requirement that should use the data as env variable to start an application because some application only accept parameters as input. on other way, we can rewrite the application to accept the config file as parameter to solve it but sometimes unfortunately the application is not owned by us. so i need find another way to set env to accomplish. does confd support writing to env variable? i can't find any infos in docs.thanks.