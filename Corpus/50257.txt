 URL : "https://github.com/unosquare/embedio/issues/98" TITLE : wildcards not working BODY : i am using embedio 1.9.1 and have code similar to this: c _httpserver = webserver.create $ http://localhost:8080 ; _httpserver.registermodule new testmodule ; c class testmodule : webmodulebase { public testmodule { addhandler /settings , httpverbs.get, onfetchsettings ; addhandler /users/ , httpverbs.post, oncreateuser ; } // oncreateuserasync and other handlers } when run, the first handler is works correctly, but not the second. that is, the correct handler is called for /settings , but /users/37 results in a 404.