 URL : "https://github.com/crottinger/prj-rev-bwfs-dasmoto/issues/3" TITLE : setting page font BODY : what you have here works, but it's not as efficient as it could be. on larger projects you don't want to have to list every element that needs this font. instead you can use inheritance to set the default font-family for every element: html { font-family: helvetica; } https://github.com/crottinger/prj-rev-bwfs-dasmoto/blob/master/dasmoto's/resources/css/style.css l1-l3