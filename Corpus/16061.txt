 URL : "https://github.com/jbogard/MediatR/issues/157" TITLE : handler gets lost BODY : i'm using the latest mediatr with autofac and noticed some strange behaviours, after a couple of hours some of my handlers that worked before can not be found anymore. until i restart the iis process i'm getting the invalid operation exception from the requesthandler class: invalidoperationexception handler was not found for request of type + message.gettype + .\r container or service locator not configured properly or handlers not registered with your container. , innerexception ; i'm using the autofac wireup from the examples //builder.registersource new contravariantregistrationsource ; // does not work for me builder.registertype<mediatrr> .as<imediatr> .instanceperlifetimescope ; builder.register<singleinstancefactory> ctx => { var c = ctx.resolve<icomponentcontext> ; return t => { object o; return c.tryresolve t, out o ? o : null; }; } .instanceperlifetimescope ; builder.register<multiinstancefactory> ctx => { var c = ctx.resolve<icomponentcontext> ; return t => ienumerable<object> c.resolve typeof ienumerable<> .makegenerictype t ; } .instanceperlifetimescope ; i'm not sure if this belongs to the fixed handler caching and the usage of: lazyinitializer.ensureinitialized .. but there is no other error recorded in my logs. any ideas?