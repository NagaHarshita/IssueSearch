 URL : "https://github.com/varnish/libvmod-memcached/issues/16" TITLE : build issues on el6 BODY : input from @ingvarha: autoconf in el6 is too old. it is possible to do make dist in el7 for example, and then copy the result back to el6. lsof is missing in buildrequires in the spec file. it is used in the tests. src/tests/gen-vcl.sh needs to be part of the tarball produced by make dist .