 URL : "https://github.com/WildflowerSchools/sensei/issues/75" TITLE : image interaction tagger BODY : tool to generate ground truth interaction segments from classroom images stored on s3 select classroom, date hour? navigate through photos select entity pair, and mark start of interaction, using current photo timestamp as start time. some point later, that interaction can be ended, using current photo timestamp as end time. multiple interactions can be being tracked at the same time.