 URL : "https://github.com/vakata/jstree/issues/1885" TITLE : node number, undefined BODY : node number, undefined i need to render from a list where it can have multiple node, one inside the other. i can not know the exact node number, because the client can create multiple nodes. how to make this automatic, regardless of how many knots, my list process and show the automatic tree