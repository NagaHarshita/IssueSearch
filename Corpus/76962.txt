 URL : "https://github.com/HuszarPeter/Readables/issues/13" TITLE : merge readables into one BODY : there should be a function an easy invoked one where the user can merge two or more readable into one. this requires at least two readables. the first one is the main the other ones are the secondaries. the function moves the files from the secondary readables into the main, and deletes the secondary readables.