 URL : "https://github.com/smeighan/xLights/issues/978" TITLE : sup file import BODY : when importing a sup file in xlights sub models cannot be selected because they do not display. an example is if i create an arch model and select the number of arches as 4. once created i create submodels to represent each arch. when i import a sup file i can import onto the main arch model all 4 arches , but cannot import onto each submodel submodels do not display as available selections when importing .