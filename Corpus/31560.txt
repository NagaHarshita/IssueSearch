 URL : "https://github.com/bp-flugsimulator/server/issues/4" TITLE : speichern von daten BODY : intro wir hatten und auf konfigurations dateien festgelegt, um daten zu speichern. aber da die integration von sql in django sehr einfach ist sollten wir vielleicht in erwägung ziehen für alle konfigurationen sql zu benutzten. vorzugweise sqlite, da es sehr klein ist und auch sehr portabel sein sollte. vorteile man muss keine configfile klasse implentieren, die auch mit django funktioniert. großer aufwand? migrationen/etc. sind einfach einfacher umgang mit queries keine probleme im mehrnutzerbetrieb daten sind gesichert. stromausfall weniger fehleranfällig nachteile overhead? sqlite portable genug? fragen da das ausschließlich auf dem master läuft, sollte man sich überlegen ob das auch auf windowsxp laufen muss. wie komplex sind die konfigurationsdateien?