 URL : "https://github.com/mattphillips/jest-extended/issues/9" TITLE : new matcher: .tobeempty BODY : feature request description: .tobeempty use .tobeempty when checking if a string '' , array or object {} is empty. js test 'passes when given an empty string', => { expect '' .tobeempty ; expect 'hello' .not.tobeempty ; } ; test 'passes when given an empty array', => { expect .tobeempty ; expect 'hello' .not.tobeempty ; } ; test 'passes when given an empty object', => { expect {} .tobeempty ; expect { hello: 'world' } .not.tobeempty ; } ;