 URL : "https://github.com/relayjs/relay-examples/issues/15" TITLE : a bug in treasurehunt BODY : in database.js export function gethidingspot id { return hidingspots.find hs => hs.id === id } if i add log here: console.log id: ${id} ${hs.id} ${hs.id === id} ${hs.id == id} if the id is '2', it will print： 'id: 2 2 false true' am i making a mistake or maybe it shouldn't be === but == in the gethidingspot function?