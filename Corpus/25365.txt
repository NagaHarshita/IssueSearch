 URL : "https://github.com/SwiftKickMobile/SwiftMessages/issues/102" TITLE : wrong image bundle BODY : hi, i've noticed that every time i run 'pod install' the bundle of images.xcassets changes to swiftmessages-swiftmessages and no longer finds the error icon so the app crashes. while i was watching i found a ! in these lines of code:      public var image: uiimage {                 return uiimage named: rawvalue, in: bundle.sm_frameworkbundle , compatiblewith: nil !      } so in my case with the wrong bundle the app goes out. it's very annoying because as i said every time i run pod install i have to change the bundle. please solve it