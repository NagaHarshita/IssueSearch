 URL : "https://github.com/hejtmy/iEEG-python/issues/7" TITLE : wilcox average is wrong BODY : if average: tfr1.data = tfr1.data.mean -3, keepdims = true tfr2.data = tfr1.data.mean -3, keepdims = true this averages data, but it shoudl append instead - compare all epochs from all all electrodes afectively you then compare two matrices epoch x electrodes x freqs x time - when appending you are resizing the matrix in the epoch dimenstion - adding new epochs to the data from the other electrodes