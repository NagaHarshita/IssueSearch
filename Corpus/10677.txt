 URL : "https://github.com/Shopify/polaris/issues/9" TITLE : css class issue. BODY : issue summary the polaris-resourcelist__item--persistactions class suggested in the docs for the resourcelist component does not match the css class in the styles.scss file that's generated. expected behavior css should use polaris-resourcelist__item--persistactions actual behavior css is using polaris-resourcelist__item-persistactions . steps to reproduce the problem try to implement a resourcelist item with actions and it will not show the actions since the css to unhide the element never gets applied. specifications - polaris version: 1.0.0