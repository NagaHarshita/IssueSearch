 URL : "https://github.com/peragro/peragro-at/issues/44" TITLE : damnfs on windows BODY : i tried adding the following code but for some reason i cant get os.name to work in damnfs.py this causes the setup to fail however i can make it work with comments on the lines in question > def __init__ self, is_dir, size : fuse.stat.__init__ self if os.name == 'nt': windows doesnt support this stuff : print windows os doesnt support everything... else: if is_dir: print os.name self.st_mode = stat.s_ifdir | 0555 self.st_nlink = 2 else: self.st_mode = stat.s_ifreg | 0444 self.st_nlink = 1 self.st_size = size