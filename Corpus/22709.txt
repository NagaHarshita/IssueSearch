 URL : "https://github.com/joomla-projects/media-manager-improvement/issues/222" TITLE : media type plugins BODY : we need to decide what to do with the media-type plugins feature. we have basically 3 options 1 drop the feature and hard-core all supported types into the client 2 define interface and return meta data to the client something like preview_url, thumbnail_url, iconclass, edit_url 3 use native webcomponents and render them for unsupported plugin types. detailed description will follow