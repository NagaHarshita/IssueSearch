 URL : "https://github.com/quipucords/rho/issues/482" TITLE : support su not just sudo BODY : opening a new issue ___ specify type: - enhancement bug severity if applicable : - low ___ description: some users don't allow users to execute privileged commands even with sudo they must use su to become root. we should support become_method=su to allow those users to run privileged commands as root user. maybe we add an auth option that is become_method where default is sudo but it accepts su and then we may also need to support become_user default being root . ___