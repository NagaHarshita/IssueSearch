 URL : "https://github.com/golemfactory/golem/issues/1294" TITLE : fix adding a task BODY : drag&drop works fine, but adding task with a + button works horrible. it should somehow allow user to select file or directory. at this point it only allows to add a directory. first of all it's awful ux: we want to add a file, so we're going through some folders and realize, that we don't see a .blend file. and what is worst, we have a problem if we have more than one file in selected directory. so we have to change it to - allow user to add file or folder - make it more intuitive and clear for user