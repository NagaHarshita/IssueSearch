 URL : "https://github.com/serde-rs/bench/issues/1" TITLE : remove layers of &mut BODY : for example in this function https://github.com/serde-rs/bench/blob/0.0.4/src/ser.rs l230 we are dealing with a mut reference to a mut reference to a serializer which contains a mut reference to vec\<u8> which contains a pointer to data. unclear whether 4 layers of indirection affects performance but let's experiment.