 URL : "https://github.com/mysqljs/mysql/issues/1743" TITLE : pool block the hole game BODY : const mysql = require 'mysql' ; var pool = mysql.createpool { host: '127.0.0.1', user: 'root', password: '', database: 'test', charset: 'utf8mb4_general_ci' } ; pool.getconnection function err, connection { // use the connection connection.query 'show databases', function error, results, fields { // and done with the connection. connection.release ; // handle error after the release. if error throw error; console.log results ; // don't use the connection here, it has been returned to the pool. } ; } ;