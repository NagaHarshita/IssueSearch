 URL : "https://github.com/ckaestne/vbc/issues/5" TITLE : cache one ... constants BODY : instead of creating v.one null again and again, just cache those instructions and replace it by a static field access to the v library. the objects are immutable anyway. should work for all sequences of iconst_1 + tointeger + v.one as well. this won't have a big effect, but we repeat this over and over again when initializing fields and variables, such as $exceptionvar