 URL : "https://github.com/pixolith/fabricjs-customise-controls-extension/issues/53" TITLE : custom icon position BODY : hi, i have tried to change the default position of custom icons, so when i changed the position of one element, that's ok, but the mouse interaction area remains wrong. that area stay the same even changing the icon position. example: // top-left this._drawcontrol 'tl', ctx, methodname, left + margin_corner, top + margin_corner, this.tlicon, this.tlsettings ; how do you think that i can fix this ? thanks a lot.