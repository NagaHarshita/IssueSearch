 URL : "https://github.com/askarinaqvi/marketresearch/issues/4" TITLE : read from twitter BODY : algorithm to read tweets based on a keyword and place each into a separate json document 1. create bluemix web application with a runtime like liberty for java 2. add the insights for twitter service to the app 3. when the insights for twitter service is bound to the app, the service instance is provisioned with unique credentials. our app will use these credentials with rest apis to search and consume twitter content. 4. retrieve the credentials from vcap_services and integrate the service instance with the app.