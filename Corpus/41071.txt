 URL : "https://github.com/sussol/react-native-data-table/issues/8" TITLE : windows uwp support? BODY : hey i've been trying to get the datatable to work under react-native-windows but it fails with props datasource get row is not a function next to it i implemented a listview which works properly with the same datasource & renderrow values, so is it the platform which breaks it, and will there be a version for windows?