 URL : "https://github.com/riot/riot/issues/2477" TITLE : nested tags not working BODY : consider the following html markup: <div class= outer > outer model { outermodel } <div class= inner > inner model { innermodel } <div> <select onchange= { dochange } > <option value=0>0</option> <option value=1>1</option> </select> </div> </div> </div> and the following script: var outer = riot.tag 'outer', false, function opts { this.outermodel = 'outer'; this.dochange = function event { this.update { outermodel: 'updated' } } } ; var inner = riot.tag 'inner', false, function opts { this.innermodel = 'inner'; this.dochange = function event { this.update { innermodel: 'updated' } } } ; riot.mount '.outer', outer ; riot.mount '.inner', inner ; two nested tags, an outer one and an inner . nothing spectacular. for some reason, this does not work as intented: { innermodel } displays nothing event though innermodel is initialized with inner inner tag dochange handler is never called - but outer tag is! from my understanding of riotjs, inner tag dochange function should handle the onchange event of the select. and there is no reason for { innermodel } to display nothing. is this a but? or is riotjs not supposed to support nested tags? i set a jsfiddle if you want to test this online: https://jsfiddle.net/ericmorand/g72zrccj/34/