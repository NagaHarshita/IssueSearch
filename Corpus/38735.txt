 URL : "https://github.com/DeamonRealm/Age_of_Empires_II/issues/13" TITLE : quadtree game freeze BODY : build: v0.3 development description: seems that the quatree formation for the pathfinding causes some crashes due to replicating much more times than need it. type: code severity: x critical major minor priority: x urgent high normal low steps to reproduce: spawn a troop with m or n select the troop with mouse left click activate movement with f5 click somewhere in the map with the rightclick for the troop to move repeat the last step a few times until the screen freeze frequency: 1/1 screenshot optional :