 URL : "https://github.com/LeventErkok/sbv/issues/334" TITLE : yices algreal parse error BODY : hello, i get the following error when running yices 2.5.4 qf_nra logic, no options from sbv. data.sbv: unexpected response from the solver. context : getmodel sent : get-value s0 expected: a value binding for kind: sreal received: s0 1 the value 1 is correct, and is indeed what yices returns when i translate the problem into smtlib and run it directly. the solver also works correctly when i adjust my problems slightly a translation in a 2d geometry problem so that none of the resulting variables is an integer.