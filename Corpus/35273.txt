 URL : "https://github.com/svnpenn/bm/issues/25" TITLE : ditch format objecct BODY : ~~~js let ya = {}; for let eurl in durl { let nurl = new url durl eurl ; let nusp = nurl.searchparams; nusp.set 'ratebypass', 'yes' ; nusp.set 'signature', dsig eurl ; let xr = ~~ nusp.get 'clen' 8 / nusp.get 'dur' / 1000 ; ya xr = <p><a href= ${nurl.href} >${xr} kb/s ${nusp.get 'mime' }</a></p> ; } let fdiv = document.queryselector ' yt-dl' ; if !fdiv { fdiv = document.createelement 'div' ; fdiv.id = 'yt-dl'; object.assign fdiv.style, { right: 0, bottom: 0, zindex: 6, width: '12em', padding: '1em', borderradius: '1em', position: 'fixed', background: 'plum' } ; document.body.prepend fdiv ; } fdiv.innerhtml = ${new date .tolocaletimestring } click to copy the filename, then right click to download ; values ya .foreach xr => fdiv.innerhtml += xr ; ~~~