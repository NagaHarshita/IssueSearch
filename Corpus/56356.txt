 URL : "https://github.com/andsel/moquette/issues/279" TITLE : mqtt ping doesn't work BODY : hi andsel， we test the ping timeout of a client, but it seems no work properly. first, client connect to server with a will message, and the ping interval of 5min. then, we directly power of the client, after 8min, zhe client is still 'online', the will message doesn't trigger at all. util later the server detect a socket read error, the will message is published. it's a bit too later, seem that the ping timeout check doesn't work at all. we also noticed that but not sure if server publish message to this client after power off, the real timeout may be much more late.