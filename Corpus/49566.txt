 URL : "https://github.com/gracicot/kangaru/issues/45" TITLE : type_id is not stable BODY : kgr::type_id is not stable across compiler. for example, using clang and gcc for multiple tu will link, but will cause crashes because the id generated is different across compilers. we could see how ctti does it.