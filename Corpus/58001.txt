 URL : "https://github.com/iNZightVIT/iNZightTools/issues/6" TITLE : returning factor types BODY : in order to avoid specifying factor levels based on only a few lines of the data, we can read the preview in as a character. when reading the full data, we still don't want to specify all the levels if we don't know them all and we don't want to read the data twice to figure them out . so again, read factor columns as character, __and then__ coerce to factor. question: is there a better way of doing this than {r} df <- inzread ... invisible sapply which sapply df, class == character , function i df i <- factor df i also, we need to worry about converting numeric to factor - we should probably worry about the order! e.g., age; we want the levels to be {8, 9, 10, 11, 12}, but if we don't specify this, r will default to {10, 11, 12, 8, 9} since it only looks at the first number character .