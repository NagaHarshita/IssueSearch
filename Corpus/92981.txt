 URL : "https://github.com/tanguyantoine/react-native-music-control/issues/114" TITLE : add a stopcontrol function BODY : this is a request a feature currently musiccontrol.resetnowplaying is just disable the display of the control panel, but you can still interact with it. is it possible to add a musiccontrol.stopcontrol function that calls musiccontrol.resetnowplaying and remove every controls and if it is possible, that displays the previous control panel for instance, you were listening some music on spotify, when stopcontrol is called, the panel of spotify is redisplayed