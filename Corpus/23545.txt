 URL : "https://github.com/JohnLangford/vowpal_wabbit/issues/1343" TITLE : problem with space BODY : hello! i have a problem. i parse a data: https://www.kaggle.com/c/habr-num-bookmarks/data i do it such way: def to_vw_format document, label=none : time = datetime.strptime first_json 'published' '$date' , '%y-%m-%dt%h:%m:%s.000z' text = ' '.join re.findall ' а-яа-я_ {3,}', document 'content' .lower return str label + ' | text ' + text + ' | lengthtext ' + str len first_json 'content' + ' | h ' + str time.hour + ' | m ' + str time.month + ' ' with open '../data/habr_review/train.json', 'r' as inp_json, \ open '../data/habr_review/habr_train.vw', 'w' as out_vw: index = 0 for line in tqdm_notebook inp_json : data_json = json.loads line index += 1 out_vw.write to_vw_format data_json, train_target.loc train_target.iloc index .name, 'target' and after that i save it into file. and i have