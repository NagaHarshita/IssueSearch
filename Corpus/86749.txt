 URL : "https://github.com/osteele/gojekyll/issues/31" TITLE : implement windows support BODY : this library uses the liquid library. the liquid library uses github.com/jeffjen/datefmt , which depends on gnu strftime . find another way of doing strftime, or a way to link the gnu library into the executable on windows. the repo contains an appveyor.yml . appveyor is currently disabled for this project, since it runs the same error each time.