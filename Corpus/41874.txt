 URL : "https://github.com/nethunteros/rpi3-kalimon/issues/17" TITLE : add options in pi3.sh BODY : it is useful to create installer options. to be able to have an easy configuration and not have to edit the script every time and look for the specific lines example: bash usage: ./pi3.sh -arg -h <hostname> -m minimal -f full -t tft -d doom if it is possible to implement it someday -r <package> if u need remove a tool, example metasploit -a <package> if u like add a tool -w <version> i can not think what letter to assign -v verbose it is necessary?