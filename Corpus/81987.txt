 URL : "https://github.com/ccrisan/motioneyeos/issues/1011" TITLE : add an arm/disarm feature BODY : add a feature so the user can arm the camera and allow notifications and movies to be enable. and have a button so the user can disarm it. so the user is not always recording or getting notifications when they are home.