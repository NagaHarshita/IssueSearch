 URL : "https://github.com/loppure/theme/issues/5" TITLE : fonti needs a redesign BODY : there could be more than one fonte per post. as of now only one is allowed maybe we can automatically add the field on the backend as the user type util a max of 5?