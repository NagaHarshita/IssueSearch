 URL : "https://github.com/pug-php/pug/issues/131" TITLE : localisation of strings BODY : i need to run all strings in plain language in a php localisation function. e.g. __ 'some string', 'mytheme' or _e 'some string', 'mytheme' shortcuts to php gettext what is the best way to achieve this? i could build a data array and pre populate it with strings and pass it to pug: $data = 'string1' => __ 'some string', 'mytheme' ; and than interpolate string1 inside pug. but surely there is a better way :