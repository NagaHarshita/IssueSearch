 URL : "https://github.com/zwggit/Algorithms/issues/1" TITLE : longest valid parentheses BODY : //使用栈 / 定义个start变量来记录合法括号串的起始位置，我们遍历字符串，如果遇到左括号，则将当前下标压入栈，如果遇到右括号，如果当前栈为空，则将下一个坐标位置记录到start，如果栈不为空，则将栈顶元素取出，此时若栈为空，则更新结果和i - start + 1中的较大值，否则更新结果和i - 栈顶元素中的较大值 / int longestvalidparenthes string s { int res = 0, start = 0; stack<int> m; for int i = 0; i < s.size ; ++i { if s i == ' ' m.push i ; else if s i == ' ' { if m.empty start = i + 1; else { m.pop ; res = m.empty ? max res, i - start + 1 : max res, i - m.top ; } } } return res; }