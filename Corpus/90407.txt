 URL : "https://github.com/react-brasil/call4papers/issues/6" TITLE : setup inicial do projeto. BODY : algumas tecnologias pra discutirmos e iniciarmos um setup do projeto. react redux webpack immutable.js jest + enzyme storybook configurado pra gerar os snapshots do jest eslint-config-airbnb podemos criar um repo de componentes dummy e instalar via npm no projeto principal, com isso facilitamos a manutenabilidade quando for necessário mexer na ui. no projeto principal podemos organizar por features, algo semelhante a isso: - events/ -- components/ --- eventform.js -- actions.js -- action-types.js -- constants.js -- index.js -- reducer.js - index.js - rootreducer.js