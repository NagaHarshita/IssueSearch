 URL : "https://github.com/CrikeeIP/OPTICS-Clustering/issues/2" TITLE : image class needed BODY : hi. the dependency on cimg bugs me, since the only part needed is drawing a graph into an image and saving that image. could someone provide an image class simple rgb-image suffices with the following api: - create an image with a given size - set single pixels in an image - given two points, draw a line segment into that image - optionally: take a set of values a graph and draw these values into an image. - save image to disk ingo