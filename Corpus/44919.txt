 URL : "https://github.com/NSI-IT/CLG-DLHPORTL/issues/18" TITLE : generate partner code BODY : as an admin, i would like to generate a partner code so that the licensors can use this code while registering to the platform acceptance criteria verify that admin will select generate partner code option once all required details have been entered verify that partner code will not be generated until all required details have been provided verify that once partner code is generated, admin can view the generated code and share it with licensor to sign up verify that the partner code will be sent by email on the linked email address