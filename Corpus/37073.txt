 URL : "https://github.com/hyrise/sql-parser/issues/37" TITLE : join multiple tables BODY : this typical olap query join fact table with multiple dimensions is not supported sql select city.name, product.category, sum price from fact inner join city on fact.city_id = city.id inner join product on fact.product_id = product.id group by city.name, product.category it gives syntax error, unexpected inner, expecting $end , however it is a valid sql syntax.