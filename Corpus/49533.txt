 URL : "https://github.com/pat-laugh/websson-libraries/issues/70" TITLE : tuple bad constructor BODY : the constructor this data&& data : keys nullptr , data std::move data {} may cause problems if misused. keys should either be set to a structure ready to get keys, or else tuple should be made an immutable object. or may the constructor should be removed; it is possibly not even used.