 URL : "https://github.com/morassman/process-palette/issues/59" TITLE : output panel steals focus BODY : if running a process via a hotkey opens an output panel or writes into an existing one, the current editor panel loses focus and has to be clicked into with the mouse. this issue came in one of the recent updates. previously, the focus would return to the editor and one could code and compile conveniently using only the keyboard.