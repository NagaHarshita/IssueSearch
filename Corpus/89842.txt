 URL : "https://github.com/svenwiltink/go-MusicBot/issues/34" TITLE : remove the queue pointer. BODY : there are a couple of bugs that exist because of the queue pointer. we need to remove queue pointer and instead have a seperate list containing the history of the last x songs. this way the queue won't grow in size over time either.