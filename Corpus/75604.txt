 URL : "https://github.com/CVCalendar/CVCalendar/issues/498" TITLE : how to localize menuview BODY : i'm trying to localize the menuview, so the day labels will display the danish name instead: currently they show: m t w t f s s on danish that should be: m t o t f l s i've implementer calendar from cvcalendarviewdelegate , and it gets called! func calendar -> calendar? { let calendar = self.mycalendar print calendar.locale return calendar } when printing the locale of the calendar, i see da_dk fixed as expected, but the menuview seems to remain in english locale. how do i change the locale of the menuview ?