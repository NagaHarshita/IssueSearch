 URL : "https://github.com/neuecc/Utf8Json/issues/18" TITLE : support for $type BODY : is there a way to support something like $type ? this would allow the following scenario: public class a { } public class b :a{ } public class c : a { } public void method { var list = new list<a> { new b , new c }; var bytes = utf8json.jsonserializer.serialize list ; var dlist = utf8json.jsonserializer.deserialize<list<a>> bytes ; }