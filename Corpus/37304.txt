 URL : "https://github.com/openshmem-org/specification/issues/174" TITLE : lock completes what? BODY : this is need of clarifiction: the shmem_clear_lock routine releases a lock previously set by shmem_set_lock after ensuring that all local and remote stores initiated in the critical region are complete. does this include rma/amo operations? this also needs the standard threading disclaimer: that only the calling thread's action and those of other threads that are ordered beforehand can be ordered by this call.