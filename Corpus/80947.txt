 URL : "https://github.com/derUli/user_notice/issues/3" TITLE : user interface erstellen BODY : in main_class notiz wird auf dashboard wie folgt angezeigt: meine notizen: notiz oder keine notiz vorhanden bearbeiten bearbeiten verweist auf eine eigene action, die so ausschaut wie die darstellung auf dem dashboard aber mit ckeditor oder codemirror je nach einstellungen des aktuellen users und einem speichern button. der speichern button leitet nach dem speichern wieder aufs dashboard um und scrollt und klappt den meine notizen tab auf.