 URL : "https://github.com/Geeklog-Plugins/gus/issues/11" TITLE : update gus cache system BODY : should maybe use template cache or at least move the gus cache pages into the geeklog data directory so they are not accessible directly by the internet.