 URL : "https://github.com/n-sokolov/CoffeeShop/issues/1" TITLE : tests for paging BODY : _ context _: paging mechanism must be tested. _ problem _: tests use the same database as the main application, so there is no guarantee that tests will be run against the same environment over a collection. _ solution _: separate main database and test database; drop collection every time a test is run.