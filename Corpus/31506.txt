 URL : "https://github.com/XOOPS/XoopsCore/issues/521" TITLE : smarty tags problem BODY : notice the following code: <option value= {$id} {if $menu_id == $id} selected='selected'{/if}>{$title}</option> currently }> is being replaced by } and that behavior breaks the html output by removing the closing > see: <option value= 1 selected='selected'navbar</option> how should we handle this? adding extra spaces does not cut it :