 URL : "https://github.com/ipfs/js-ipfs-repo/issues/145" TITLE : does not work in firefox? BODY : openerror: the operation failed for reasons unrelated to the database itself and not covered by any other error code. it works fine on chrome can anyone reproduce this error? i used the basic start code: javascript const ipfsrepo = require 'ipfs-repo' const repo = new ipfsrepo '/tmp/ipfs-repo' repo.init { cool: 'config' }, err => { if err { throw err } repo.open err => { if err { throw err } console.log 'repo is ready' } }