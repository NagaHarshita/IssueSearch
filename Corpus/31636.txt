 URL : "https://github.com/este/este/issues/1396" TITLE : storybook and este? BODY : https://storybook.js.org/ what do you think about adding storybook to este? at the very least it documents the este components. but this could potentially become a ui for the admin/editor/designer type user that we would want to convert from wordpress/drupal/etc. this would not be the same type of ui as wp/drupal have -- it opens up a whole new realm of possibilities. anyway, here is what i came up with that allows este components inside of storybook it doesn't just work : . anyone, please suggest improvements : import react from 'react'; import { setaddon, storiesof } from '@storybook/react'; import { action } from '@storybook/addon-actions'; import { provider as felaprovider, themeprovider } from 'react-fela'; import propsaddon from 'storybook-addon-props-fela'; import felarenderer from '../lib/felarenderer'; import button from '../components/button'; import { browsertheme } from '../themes/browsertheme'; setaddon propsaddon ; export const createstoryrenderer = theme: any => component: any => { return <felaprovider renderer={felarenderer}> <themeprovider theme={theme}> <component /> </themeprovider> </felaprovider> ; }; export const createstoryrender = theme: any => { const render = createstoryrenderer theme ; return component: any => { return render component ; } }; const storiesrender = createstoryrender browsertheme ; storiesof 'button', module .adddecorator storiesrender .addwithprops 'with text', => <button primary >boo</button> .add 'with some emoji', => <button onclick={action 'clicked' }>😀 😎 👍 💯</button> ; ! screen shot 2017-09-07 at 9 28 29 am https://user-images.githubusercontent.com/83722/30174206-1e36ff2a-93af-11e7-8c58-e29800e6e10b.png