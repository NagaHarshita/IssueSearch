 URL : "https://github.com/octobercms/october/issues/3028" TITLE : 5.5 - auth in middleware BODY : expected behavior return the logged in user - exact same code on build 419 works fine. actual behavior auth::getuser returns null has no effect reproduce steps in plugin.php public function boot { $this->app 'illuminate\contracts\http\kernel' ->prependmiddleware 'myplugin\middlewares\mymiddleware' ; // or push } mymiddleware.php class mymiddleware { public function handle $request, closure $next { app::before function { if app::runninginbackend || app::runninginconsole { return; } $translator = translator::instance ; if !$translator->isconfigured { return; } if $user = auth::getuser { var_dump $user ; // returns null $translator->setlocale $user->getlocale ; } } ; return $next $request ; } } october build 420