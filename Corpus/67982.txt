 URL : "https://github.com/nmunro/azathoth/issues/24" TITLE : reconsider model layout BODY : i think i've designed the emotion system wrong, i'm thinking instead of having a general emotion class, each expected emotion has its own class and each diary has a mapping for each emotion which can be null, which would basically mean zero. this will save space for not creating unneeded records, in fact, only a handful of instances of each emotion would ever need to be created as one emotion can never have more than 100% and with 11 other emotions then a foreign key need only point to a specific instance of an specific emotion class.