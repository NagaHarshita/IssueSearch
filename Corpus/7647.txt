 URL : "https://github.com/reactstrap/reactstrap/issues/744" TITLE : how to set size BODY : how do i write the size property in modals which i see is a proptype of string datatype. i have a thumbnail which if the user clicks on it, a modal with the whole image is displayed. unfortunately the image is larger than the modal width and bleeds outs of the edges of the modal. how do i control the width of the modal to allow the whole image to fit?