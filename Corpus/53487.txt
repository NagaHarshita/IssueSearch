 URL : "https://github.com/Severaux/monowolf/issues/32" TITLE : dropped packets monome BODY : sending updating messages, sends the entire grid 256 messages with 3 variables each from matrixctrl to the monome, this sometimes causes dropped packets to the monome with unreliable interfacing as a result. solution: remove all matrixctrl's & send only changed or possibly changed information to the monome through row, key & map messages binary . messaging after switching between menu's & submenu's through map-messages 4 messages with 10 variables each .