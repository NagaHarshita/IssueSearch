 URL : "https://github.com/CESNET/libyang/issues/353" TITLE : config false flag BODY : i'm using the version 0.13.43 and i wrote the following simple function to check whether or not a leaf is config false . bool isconfig struct lys_node_leaf temp { printf flag: %x , temp->flags ; if temp->flags == lys_config_r return false; else return true; } my leaf i wanted to check it on: leaf noconf { type uint32; config false; } since config false has been set in the yang schema, the flag should be lys_config_r , so 0x02, but my function prints this: >flag: 6 but it should be 2, why is it like this?