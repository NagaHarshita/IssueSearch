 URL : "https://github.com/thomaskuestner/CNNArt/issues/13" TITLE : data splitting problem BODY : when performing training and testing data splitting, only normal mode is working while crossvalidation_data and crossvalidation_patient still have some bugs. the problem is x_train , y_train , x_test and y_tset should be returned as a list instead of dictionary to be written into h5 file via hf.create_dataset .