 URL : "https://github.com/uChip/MCP342X/issues/5" TITLE : mcp3421 with 18bit issue BODY : hi, while experimenting i've noticed that i'm not able to use the 18bit mode. i tried to debug this but no luck: uint8_t mcp342x::getresult int32_t dataptr { uint8_t adcstatus; // this thing prevents to enter the while loop! if configregshdw & mcp342x_size_mask != mcp342x_size_18bit { return 0xff; } // even after removing the adcstatus from the while loop, the next line can't be executed while adcstatus & mcp342x_rdy != 0x00 { if wire.requestfrom uint8_t devaddr, uint8_t 4 == 4 { // this one char dataptr 3 = wire.read ; char dataptr 2 = wire.read ; char dataptr 1 = wire.read ; } else { return 0xff; } }; dataptr = dataptr >> 8; return adcstatus; } what's wrong here?