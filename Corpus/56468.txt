 URL : "https://github.com/ljessendk/easybinder/issues/4" TITLE : nested properties help BODY : hi, i'v got problems with binding nested properties to the corresponding fields. could you help me to assign the following nested property to the field? here is my exmaple: form to bind public class form { private mtextfield name = new mtextfield name ; } entity to bind public class myentity { private nestedentity nested; //getter & setter } public class nestedentity { private string name; //getter & setter } view public class myview { form form = new form ; ... public void build { autobinder<myentity> binder = new autobinder<myentity> myentity.class ; // binding to fields :: here missing nested? binder.bindinstancefields form ; mgridlayout grid = new mgridlayout 3, 1 .withfullsize ; // binding to fields :: here missing nested? how can i assign this nested.name to the field // in form : private mtextfield name = new mtextfield name ; grid.addcomponents binder.buildandbind nested.name ; } }