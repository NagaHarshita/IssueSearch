 URL : "https://github.com/fawda123/NeuralNetTools/issues/17" TITLE : error when plotting BODY : hello, i have a strange error when trying to use plotnet . let's assume i have two models, called m1 and m2 , which are trained using the exact same formula on two different datasets. if i do r > plotnet m1,alpha=0.6 everything works fine. but if run r > plotnet m2,alpha=0.6 what i get is r error in as.character forms : cannot coerce type 'closure' to vector of type 'character' do you have any idea of what could cause this problem?