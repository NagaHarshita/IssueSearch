 URL : "https://github.com/vmware/vic/issues/6455" TITLE : set up ci/cd pipeline BODY : _from @jooskim on september 14, 2017 19:17_ user statement as a developer i would want a working ci/cd pipeline set up for vic engine ui code so that once a commit/pr to the master branch is merged it should kick off a build and unit tests. integration tests are run nightly. acceptance criteria - set up secrets - set up a task to build angular - set up a task to java service - set up a task to package angular & java service artifacts - set up a task to publish the packages to gcp - set up a task to notify slack channel _copied from original issue: jooskim/vic 79_