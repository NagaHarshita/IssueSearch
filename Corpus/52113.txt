 URL : "https://github.com/BennyKok/OpenLauncher/issues/31" TITLE : cancel does not cancel BODY : android 7.1.1 lineageos openlauncher alpha2_patch2 f-droid what is happening: in the settings, any option that has an ok / cancel button, the cancel option saves the settings instead of reverting to original settings - open launcher settings - select app drawer -> size defaults to columns 4 / rows 5 - move the slider to change the size, e.g. rows to 6 - touch cancel - open the setting again - rows is still 6 instead of being set back to 5 what should happen the use of the cancel button should revert any changes to settings instead of making them active.