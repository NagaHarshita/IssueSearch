 URL : "https://github.com/fchollet/keras/issues/6511" TITLE : loading memory memory leak BODY : i am running the following simple code on the cpu with tensorflow backend. k.clear_session does not clear the memory completely and there is a serious memory leak. any ideas how to solve it ? import keras.models import keras.backend as k for i in range 300 : try: model = keras.models.load_model 'my_model.hdf5' k.clear_session except exception as e: print e break