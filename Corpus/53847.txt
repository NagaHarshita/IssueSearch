 URL : "https://github.com/amida-tech/ration/issues/46" TITLE : excel dashboard export BODY : steps to success: 1. add project manager as a field 2. deliverable and contract end date could also be project fields 3. calculations are just another view we probably can't get our team a full spreadsheet, but we can normalize the data and get a better export that is easier to export. check out https://www.npmjs.com/package/xlsx