 URL : "https://github.com/catalyst/moodle-auth_saml2/issues/159" TITLE : ispassive saml idp test BODY : what happened? we want a way to test that the idp and sp connection is all correct, but without needing credentials. a way to get this is by attempting ispassive authentication. what you expected: have a simple url which can be visited in a browser, which will redirect through to the idp, and then bounce back so the sp with some identifiable text that it worked.