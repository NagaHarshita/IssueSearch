 URL : "https://github.com/goldhat/QuizMaster/issues/77" TITLE : quiz email tab BODY : quiz emails tab should show checkboxes for each registered email. global emails are automatically checked, which means adding a new global email should work need to test this automatically without having to add it to every existing quiz. non-global emails, are unchecked, admin checks them allowing to make emails for specific quizzes.