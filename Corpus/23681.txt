 URL : "https://github.com/jqw-chang/algorithmO/issues/4" TITLE : random side notes: BODY : - to calculate if there's a faster solution and using a count variable to count the of times the program runs. machine learning - deterministic test cases --- keeping seed in memory and changes it if need be ? - ask what user thinks the big-o notation is first before calculating approximation!!!! - console.trace - call stack but prints in string/text - stack trace - new console -> fs.writefilesync