 URL : "https://github.com/FCO/Test-Fuzz/issues/8" TITLE : hangs on some parameters BODY : not sure why, but run-tests will hang on certain parameters. for example, the following will hang because of the num type. from playing with it a bit, it seems that the problem may occur in the .generator method of test::fuzz::generator . use test::fuzz; sub foo num $a is fuzzed { true; } run-tests;