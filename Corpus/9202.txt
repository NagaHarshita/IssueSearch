 URL : "https://github.com/SiggiGue/pyfilterbank/issues/6" TITLE : two small issus BODY : splweighting.weight_signal : argument in function definition ist _data_, but in return value _x_ is used. simple fix would be changing _x_ to _data_ in call of _lfilter _ octbank multiple functions : i assume there were some code changes in numpy 1.12 or 1.13 that start checking for integers as argument of _np.zeros _ , _np.abs _ and others. so it is necassary to cast the divisions, at least in my setup python 3.5.3