 URL : "https://github.com/lharzenetter/winery/issues/38" TITLE : backend should json BODY : hints: org.eclipse.winery.topologymodeler.addons.topologycompleter.helper.jaxbhelper getxmlasstring shows how to generate xml from an object org.eclipse.winery.repository.export.csarexporter addselfservicemetadataasjson shows how to generate a json based on an object: objectmapper om = new objectmapper ; // using om.writevalue zos, application causes trouble with the zos, so we write it into a byte array first byte bytes = om.writevalueasbytes application ; zos.write bytes ; thus, there should be a method .jaxbhelper getjsonasstring be created. this should - be tested - be used whereever possible