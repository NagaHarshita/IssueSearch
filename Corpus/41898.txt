 URL : "https://github.com/factoriommo/factorio-multienv-ctl/issues/12" TITLE : add pre-start command BODY : in our case we reeeeeeeaaaaaaly need to automate our banlist updated from our api with every start/restart of the service. i usually just run this command: wget https://aerith.ovh/factoriommo/banlist/api/bans.php -o /var/factorio/instances/instance_name/server-banlist.json probably should result in something like bellow in the config wget https://aerith.ovh/factoriommo/banlist/api/bans.php -o ${instance_path}/server-banlist.json