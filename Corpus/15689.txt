 URL : "https://github.com/GoblinDBRocks/GoblinDB/issues/22" TITLE : file update error BODY : sometimes, the goblin_db.json gets corrupted on update. this happens when there is data in the file i.e. last execution stored stuff and open again the database. on doing a .set, it store it as a new json at the end of the actual one, instead of load the previous data, add the new value, and store it all together. i'm now trying to create some code snippet to reproduce it, but the ressult is this: json { user_5 :{ name : paco , id :5}} user_8 :{ name : paco , id :8}} instead of this: json { user_5 :{ name : paco , id :5}, user_8 :{ name : paco , id :8}}