 URL : "https://github.com/petermeissner/wikipediatrend/issues/28" TITLE : stats.grok.se server down BODY : at the moment maybe forever the stats.grok.se server which provides the data for the tool is down, so the will not work.