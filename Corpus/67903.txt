 URL : "https://github.com/teamevolve/EVOLVE/issues/42" TITLE : random number seed bugs BODY : • repeated experiments from the same input window appear to use different seeds. • the seed in exported data is the initial seed actually used for the 1st experiment, not the value actually used in a subsequent experiment. desired sequence: • open program and randomly set initial seed —what is done now. • display same seed after first experiment. be sure this value is exported. • <more>