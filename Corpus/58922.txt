 URL : "https://github.com/hadley/svglite/issues/84" TITLE : support multiple pages BODY : is there some way we can make svglite or svgstring or a wrapper support multiple pages? the png device supports sprintf syntax to denote the page number in the filename: r svglite::svglite rplot%03d.svg ggplot2::qplot iris$species ggplot2::qplot iris$sepal.length dev.off also useful would be if svgstring returns a character vector of length > 1 for multiple pages: r s <- svgstring ggplot2::qplot iris$species ggplot2::qplot iris$sepal.length dev.off svgdata <- s length svgdata this would allow us to use svglite with evaluate/knitr. currently this fails: r options device = svglite::svglite out <- evaluate::evaluate 'ggplot2::qplot iris$species ; ggplot2::qplot iris$sepal.length '