 URL : "https://github.com/BryceGuilmette/prj-rev-bwfs-dasmoto/issues/2" TITLE : ids have to be unique BODY : <p>imported paint from finland. over 256 colors available in-store, varying in quantity 1 oz. to 8 oz. . clean finnish paint microbinds to canvas, increasing the finish and longevity of any artwork.<div id= price > starting at $5.00 / tube.</div></p> you can't reuse id, it will break on browsers. if you are using any anchor links with ids as targets, the browser will pick one and ignore the others. jquery uses id for manipulation, it will ignore the other ids with the same name also and just pick one to work with. in this case we should be using classes. classes are reusable and repeatable. you would write it like this class= price and then change the in your css to a period instead .price{ font-family: helvetica; font-weight: bold; color: blue; }