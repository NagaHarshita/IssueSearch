 URL : "https://github.com/cmarchand/gaulois-pipe/issues/31" TITLE : multiple <xslt> in <when> BODY : hello, i tried to run 2 xsl inside <when> : <pipe mutithreadmaxsourcesize= 24349456 nbthreads= 4 traceoutput= logger > <xslt href= cp:/common/xsl/identity.xsl /> <choose> <when test= /toto' > <xslt href= cp:/titi.xsl /> <xslt href= cp:/tata.xsl /> </when> <otherwise> <xslt href= cp:/common/xsl/identity.xsl /> </otherwise> </choose> <xslt href= cp:/common/xsl/identity.xsl /> <output id= main method= xml encoding= utf-8 indent= no > <folder absolute= $ context.uri /$ resstep /> <filename name= $ input-basename .xml /> </output> </pipe> but it seems that only the first one is executed. i made my pipe worked, by using 2 <choose> with only 1 <xslt> inside <when> can you check this point, to see you're having the same issue ? thanks