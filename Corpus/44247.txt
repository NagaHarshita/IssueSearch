 URL : "https://github.com/oxfordcontrol/CDCS/issues/6" TITLE : error if k.q is empty BODY : i get the following error message if k.q = : operands to the || and && operators must be convertible to logical scalar values. error in cdcs_utils.checkinputs line 40 if isfield k,'q' && max k.q > 0 error in cdcs line 104 at,b,c,k,opts = checkinputs at,b,c,k,opts ; of course i can get around that issue by deleting the field q from k before calling cdcs . however, it may be more user friendly to check that k.q is nonempty in checkinputs : isfield k,'q' && ~isempty k.q && max k.q > 0