 URL : "https://github.com/ponylang/rfcs/issues/109" TITLE : improve range usability BODY : current issues with collections.range that hurt its usability: negative ranges where min > max is only expressable for signed integers: pony // this is not possible, as the inc parameter is also u8 range u8 10, 0, -1 the default inc should only be 1 if min <= max , otherwise it should be -1 to ensure that we iterate into the expected direction: pony // this should infer a default step of -1 range u8 10, 0 // this should infer a default step of 1 range u64 0, 10