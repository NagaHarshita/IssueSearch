 URL : "https://github.com/NethServer/dev/issues/5267" TITLE : asterisk wrong gid / uid: BODY : steps to reproduce - make sure gid 995 is already in use getent group | grep 995 chrony : x : 995: - install nethserver-freepbx - asterisk package fails install expected behavior asterisk sould be installed also if gid 995 and uid 995 are already in use actual behavior asterisk package isn't installed components asterisk13-core-13.15.0-1.sng7.x86_64 wrong lines in .spec are: getent group asterisk >/dev/null || groupadd -g 995 -r asterisk getent passwd asterisk >/dev/null || useradd -r -u 995 -s /bin/bash -d /home/asterisk asterisk suggested solution: change the previous lines with %{_sbindir}/groupadd -r asterisk &>/dev/null || : %{_sbindir}/useradd -r -s /sbin/nologin -d /var/lib/asterisk -m -c 'asterisk user' -g asterisk asterisk &>/dev/null || : workaround before installation, launch groupadd -r asterisk useradd -r -s /sbin/nologin -d /var/lib/asterisk -m -c 'asterisk user' -g asterisk asterisk