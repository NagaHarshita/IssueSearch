 URL : "https://github.com/elanthia-online/forums/issues/1" TITLE : add restore script BODY : should add a restore script that works like this: bash restore --app=<container> --db=<container> --rdb=<db tarball> --public=<public tarballs> so it's easy to create a container that can be used for mocking & restore from problems.