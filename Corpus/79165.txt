 URL : "https://github.com/GitHubAcc12/Sudoku/issues/1" TITLE : run-time check failure 2 BODY : das beispielprogramm wirft zufällig eine von zwei exceptions zum ende des debuggens: 1. run-time check failure 2 - stack around the variable 's' was corrupted. 2. run-time check failure 2 - stack around the variable 'w' was corrupted. die funktionalität des beispielprogrammes bleibt erhalten und der ablauf wird bis zum ende des programmes nicht gestört. beim starten der executable ohne debugger und ohne inputfile in der laufzeitumgebung tritt kein fehler auf. sobald sich aber ein inputfile in der laufzeitumgebung befindet tritt eine exception auf und führt zum absturz des programms. allerdings wieder erst nachdem alle funktionen erfolgreich durchgelaufen sind. der fehler tritt sowohl auf dem master branch als auf meinem branch auf.