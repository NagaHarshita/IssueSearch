 URL : "https://github.com/Microsoft/BashOnWindows/issues/2373" TITLE : can not rails s -b 0.0.0.0 BODY : your windows build number: microsoft windows version 10.0.14393 what you're doing and what's happening: i ran rails s -b 0.0.0.0 to run rails project for another computer via lan using ipv4. if i type ipv4 from ipconfig in this computer i can join, but another computer can not; althought join same network what's wrong / what should be happening instead: i think the problem is wsl can not support ip for another computer join this. or something like that