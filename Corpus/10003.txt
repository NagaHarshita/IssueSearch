 URL : "https://github.com/openvstorage/integrationtests/issues/484" TITLE : add vdbench test BODY : investigate if this could trigger addtional issues. also take data validation into account when testing with vdbench: e.g. -vr -j -v activate data validation. -vr activate data validation, immediately re-read after each write. -vw activate data validation, but don’t read before write. -vt activate data validation, keep track of each write timestamp memory intensive -j activate data validation