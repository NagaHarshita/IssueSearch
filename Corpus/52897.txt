 URL : "https://github.com/disastersystems/disaster-recovery/issues/33" TITLE : create updating script BODY : to also not just import config but also run composer install to make sure whenever the script runs both new modules or configuration updates happen fluently.