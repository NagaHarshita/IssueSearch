 URL : "https://github.com/wordpress-mobile/WordPress-iOS/issues/8037" TITLE : limit 2fa codes to numbers BODY : currently the 2fa entry screen isn't limited to just numbers. it should be 1. the text field ought to only accept numbers as input 2. the submit button should only be enabled when the text field contains numbers 3. only codes of length 6,7, or 8 numbers should be accepted. 4. all of these guidelines should apply when pasting as well.