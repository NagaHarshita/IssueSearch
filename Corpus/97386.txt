 URL : "https://github.com/ocaml/opam/issues/3038" TITLE : github ssh clone times out BODY : when running opam pin --yes add --no-action --dev-repo ocp-ocamlres , things hang and eventually timeout. the same command works when cloning a number of other packages that use https github links. i can clone the package using the dev-repo specified in the package's opam file. the same problem exists if i specify the url directly: opam pin --yes add --no-action ocp-ocamlres git://github.com/ocamlpro/ocp-ocamlres but not if i use the https version: opam pin --yes add --no-action ocp-ocamlres https://github.com/ocamlpro/ocp-ocamlres here's the error: ~/tezos/repo $ opam pin --yes add --no-action ocp-ocamlres git://github.com/ocamlpro/ocp-ocamlres opam pin --yes add --no-action ocp-ocamlres git://github.com/ocamlpro/ocp-ocamlres note package ocp-ocamlres is currently pinned to https://github.com/ocamlpro/ocp-ocamlres version 0.3 . proceed and change pinning target ? y/n y error could not synchronize /users/milodavis/.opam/default/.opam-switch/sources/ocp-ocamlres from git://github.com/ocamlpro/ocp-ocamlres : git fetch -q git://github.com/ocamlpro/ocp-ocamlres exited with code 128 error error getting source from git://github.com/ocamlpro/ocp-ocamlres this issue was also present in opam 1. opam config report opam-version 2.0.0~beta3 self-upgrade no os darwin external-solver aspcud $in $out $criteria criteria -count removed ,-notuptodate request ,-sum request,version-lag ,-count down ,-notuptodate changed ,-count changed ,-notuptodate solution ,-sum solution,version-lag jobs 3 repositories 1 http pinned 1 git current-switch default