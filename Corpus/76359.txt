 URL : "https://github.com/keyboardio/Akela-OneShot/issues/4" TITLE : issues with interruption BODY : when a one-shot mod or layer is interrupted it is more visible with layers, though , and the interrupting key is held longer than a cycle or two, then it gets registered again. so if on the base layer, there's a , and on a one-shot layer it becomes b , and i tap the layer key, then tap this key slowly, then both b and a will register. there should be a short timeout where the interrupting key is masked out after an interruption, to avoid these issues. or perhaps a mask until release... but that may interfere with holding. but perhaps that's an easier compromise?