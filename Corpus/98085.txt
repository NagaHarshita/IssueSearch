 URL : "https://github.com/ericagol/celerite.jl/issues/6" TITLE : implementing lapack solver BODY : when j > 4, should be faster to use lapack solver. there are the gbtrf & gbtrs functions that are built into julia, but also the mks solver that may require recompiling julia.