 URL : "https://github.com/phoenixframework/phoenix/issues/2651" TITLE : test --no-start problem BODY : actual behavior just run these 3 steps: $ mix phx.new tmp $ cd tmp $ mix test --no-start argumenterror argument error stdlib :ets.lookup_element ecto.registry, nil, 3 ecto lib/ecto/registry.ex:18: ecto.registry.lookup/1 ecto lib/ecto/adapters/sql/sandbox.ex:426: ecto.adapters.sql.sandbox.mode/2 elixir lib/code.ex:376: code.require_file/2 elixir lib/enum.ex:675: enum. -each/2-lists^foreach/1-0- /2 so it seems the --no-start option indeed prevents the application and its children tmp.repo especially to be launched, but ecto seems so be needed somehow, crashing the test suite. expected behavior possibility to use the --no-start option with exunit environment elixir version elixir -v : 1.5.1 phoenix version mix deps : 1.3.0 nodejs version node -v : v7.10.0 npm version npm -v : 4.2.0 operating system: macos