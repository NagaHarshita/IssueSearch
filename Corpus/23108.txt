 URL : "https://github.com/openwrt/mt76/issues/99" TITLE : no tx on antenna 2 BODY : hi, currently it seems, that the driver doesn't utilize the second antenna properly. we currently have a scenario, where we have one fixed antenna connected to ant2 and one screw-in antenna connected to ant1 is connected to the mt7628an as it turns out, wifi only works properly if ant1 that is optional is connected and not when only ant2 is there. we did some measurements and it turned out, that we see no data transmissions on ant2, so ant2 only seems to receive data, but not transmit. is this a known limitation? can anyone image what the problem might be? greets bwa