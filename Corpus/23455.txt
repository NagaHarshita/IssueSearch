 URL : "https://github.com/Unitech/pm2/issues/2968" TITLE : pm2 attach <pm_id> BODY : allow to attach to a process stdin and stdout in order to send commands. this is very useful when you have implemented a kind of cli in an application iot, electronic... . how we fork a process: javascript var cspr = spawn command, args, { env : pm2_env, detached : true, cwd : pm2_env.pm_cwd || process.cwd , stdio : 'pipe', 'pipe', 'pipe', 'ipc' //same as fork in node core } ; https://github.com/unitech/pm2/blob/master/lib/god/forkmode.js l94 potential solution use node.js readline via pm2 attach <proc_id> and at every new command separated by a , send to pm2 daemon method that will do a god.clusters_db pm_id .stdin.write cmd @vmarchaud thoughts?