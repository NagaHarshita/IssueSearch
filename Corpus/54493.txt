 URL : "https://github.com/SortableJS/Vue.Draggable/issues/97" TITLE : get the parent element BODY : hi, i have following scenario. i have multiple lists. now i want to move elements from one list to another. how do i get the parent element not the dom, but the js ? i couldn't find any property in the move or the add event. for example if i move the children element with id 432 to another target group , how could i get the id of the new container? this is the simplified template: html <div v-for= category in targetgroups > <draggable element= ul v-model= category.groups :options= dragoptions > <li v-for= group in category.groups class= list-group-item >{{group.name}}</li> </draggable> </div> the data looks like this: js targetgroups = { id: 12, name: foo , children: { id: 432, name: baz } // , ... //, ... }