 URL : "https://github.com/GoogleChrome/sw-helpers/issues/421" TITLE : defaults in sw-build BODY : library affected : sw-build browser & platform : n/a issue or feature request description : issue so sw-build should have a defined set of defaults to it, which it can use if the developer passes nothing. this will also avoid various build step plugins to define these defaults randomly on their own will.