 URL : "https://github.com/khaosdoctor/setup/issues/1" TITLE : force lf endings BODY : crlf line endings are causing a lot of problems in unix like systems such as mac , because vim cannot read the .vimrc file, bash cannot read the .bash_profile file and so on. things start to get a bit messy when you download it