 URL : "https://github.com/felixSchl/purescript-redux-saga/issues/6" TITLE : avoid queuing of inputs BODY : compare redux-saga's behavior: javascript function test { yield fork function { console.log 1, yield take ' ' ; console.log 2, yield take ' ' ; } ; yield put { type: 'foo' } ; yield call delay, 100 ; yield put { type: 'bar' } ; } // yields: // > 1 { type: 'foo' } whereas in purescript-redux-saga: purescript test = do yield fork do traceanya =<< take pure <<< id yield $ liftaff $ delay $ 100.0 milliseconds traceanya =<< take pure <<< id yield $ put 1 yield $ put 2 -- yields: -- > 1 -- > 2 the reason is that avar stores the value even if there's no receiver yet.