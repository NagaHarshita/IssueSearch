 URL : "https://github.com/our-city-app/oca-backend/issues/484" TITLE : clean up index.yaml BODY : there's a limit of max 200 indexes in index.yaml . see if we can get rid of some indexes by removing some deleted properties by creating xxxarchive models.