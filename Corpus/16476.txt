 URL : "https://github.com/egoist/vbuild/issues/126" TITLE : introduce presets BODY : i want the config files to be even more simple, which mean you can use a .vbuildrc or directly configure vbuild in package.json or cli option. in most case you can do that if you don't use functions like webpack and run in config file, so i think the presets is worth adding here. js // vbuild-preset-jest module.exports = function vbuild { vbuild.setoption 'vendor', false .setoption 'minimize', false vbuild.on 'test', => { vbuild.run runjest webpack2jest vbuild.config.value } } - the preset can update cli options - only executes in specific command, use .on - run custom build process or default one, vbuild.run - run with vbuild test --presets jest