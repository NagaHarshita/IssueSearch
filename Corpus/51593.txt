 URL : "https://github.com/deployphp/deployer/issues/1193" TITLE : auto unlock on failure BODY : | q | a | ----------------- | --- | issue type | feature request | deployer version | 4 description consider this laravel deploy script as an example: task 'deploy', 'deploy:prepare', 'deploy:lock', 'deploy:release', 'deploy:update_code', 'deploy:shared', 'deploy:vendors', 'deploy:writable', 'artisan:view:clear', 'artisan:cache:clear', 'artisan:config:cache', 'artisan:optimize', 'deploy:symlink', 'deploy:unlock', 'cleanup', if any of these step fails we need to manually unlock deployment. i believe logically when script exits ether with failure or success the unblock recipe should be executed.