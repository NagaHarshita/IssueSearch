 URL : "https://github.com/underscoreio/essential-scala/issues/40" TITLE : code mistake c.5.40 BODY : c.5.40 solution to: union of maps scala def union a map1: map a, int , map2: map a, int : map a, int = { map1.foldleft map2 { map, elt => val k, v = elt val newv = map.getorelse k, v //mistake map + k -> newv } } this code returns map a -> 2, b -> 4 instead of desired map a -> 3, b -> 6 val newv = v + map.getorelse k, 0 - that will fix the problem