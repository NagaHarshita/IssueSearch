 URL : "https://github.com/davidtom/pintrips/issues/12" TITLE : re: code review BODY : in the controllers, you have some methods which should probably reference ar/methods in the models. eg. tripscontroller get_orphan_events friends index commentscontroller   | current_user && @comment.user.is? current_user   instead of is?, can just use == directly