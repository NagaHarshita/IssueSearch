 URL : "https://github.com/niclabs/net-viz/issues/55" TITLE : create rollback method BODY : it should take care of: - deleting cables and then nodes from the live tables when their valid_from-to range doesnt include the rollback timestamp point - reviving nodes and then cables from their particular history tables to the active ones - while doing so, conflicting nodes and cables should be deleted