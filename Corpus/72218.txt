 URL : "https://github.com/conveyal/analysis-ui/issues/494" TITLE : better choropleth breaks BODY : now that we're using a new gridualizer version, we can make use of the ckmeans classifier. current code appears to still be using a diverging quantile-based classifier. it looks like the ckmeans classifier can cooperate with the diverging one in the same way. the improvement in display quality should be huge. note that computing the breaks is slower though, so we have to make sure they aren't computed at each render.