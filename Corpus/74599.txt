 URL : "https://github.com/HPCE/hpce-2017-cw6/issues/17" TITLE : external library support BODY : hi, is it the case that we can use external libraries? for instance, in our case, we would like to use boost library, however, this requires it being installed on the machine the code is being run on, because we need to link against it. is it possible to ask for the library to be installed prior to running the tests? many thanks.