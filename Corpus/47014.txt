 URL : "https://github.com/nZEDb/nZEDb/issues/2558" TITLE : no connection defined BODY : expected behaviour expected web pages to load correctly and update scripts to update correctly actual behaviour get no connection defined errors for both update scripts and web pages to load correctly. steps to reproduce the behaviour clean install on ubuntu 16.04 lts with php 7.0.22 and mariadb. installed pre-reqs, ran the install script in _install and checked to make composer was correctly installed. permissions are correct for the whole folder. ./zed update all also errors with no connection defined also happens after running ./zed update all on a working site as well.