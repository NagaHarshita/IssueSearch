 URL : "https://github.com/fchollet/keras/issues/8644" TITLE : custom loss with arguments BODY : suppose i have a loss function that takes arguments which values can changes accordingly with inputs. basically, my inputs are two images and an info file. the info file is used only in the loss function but is loaded in the batch. how can i do this? actually i see how to pass parameters like a normal function but since these values changes, and i load in generator and put like inputs, i should embed them in output otherwise keras refuse to create the graph.