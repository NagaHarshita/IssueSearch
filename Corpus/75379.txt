 URL : "https://github.com/gnunn1/tilix/issues/1006" TITLE : harden compilation flags BODY : since some distros use the prebuilt binaries like arch's aur , it makes sense to enable all hardening flags to raise the security level. as you do many sensitive operations in the terminal. current checksec output: partial relro no canary found nx enabled no pie no rpath no runpath no	fortify ideally we'd enable them all.