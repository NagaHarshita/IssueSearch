 URL : "https://github.com/HendrikNoeller/Writer/issues/15" TITLE : .fdx import crash BODY : some appstore reviews mention crashes on .fdx import. > this is a basic, workable app, which i have written a few short scripts with… recently, however, it has began crashing only moments after opening > don't work don’t open the fdx format.