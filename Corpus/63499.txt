 URL : "https://github.com/Zonama/ZonamaDev/issues/21" TITLE : easy box upgrade script BODY : need something easy like: bash cd ~/zonamadev/fasttrack ./upgrade.sh - check host disk free for enough to do the upgrade - option to skip saving the vm if the user is low on disk? - ssh in to vm and run swgemu backup - scp the backup out to the host - tar $home.tar to file on host some config files can't be copied - maybe save the virtualbox image ? export or detach from vagrant - ./setup.sh pass args to customize the box at load - scp backup and $home.tar to box and extract - reboot the vm after user is happy: bash cd ~/zonamadev/fasttrack ./upgrade.sh --cleanup - remove the orphaned vm box