 URL : "https://github.com/airliti/estructura/issues/1" TITLE : queue.toarray ; BODY : the queue.toarray ; doesn't take the .itemoffset into account resulting in null values at the beginning to the returned array. const aqueue = queue.from 'a', 'b', 'c', 'd', 'e', 'f' aqueue.dequeue aqueue.dequeue const toarray = aqueue.toarray toarray 0 .should.equal 'c'