 URL : "https://github.com/GoogleChrome/sw-toolbox/issues/249" TITLE : toolbox rule problem BODY : i created the following rule using toolbox with a custom handler. toolbox.router.get '/user/test', function request { return new response 'it works! ' + request.url ; },{ origin: 'http://local.rebelchatserver:10000' } ; it only works after i reload the page twice. the first time, it send the request to the network , but if i reload the page, i get the correct response from service worker