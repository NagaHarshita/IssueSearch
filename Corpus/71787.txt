 URL : "https://github.com/augustoccesar/QueryBuilder/issues/15" TITLE : aggregation and group by BODY : create a way to make group by aggregations on the selectbuilder with something like: java new selectbuilder .select aggregation.count {u}id .groupby {u}city ; or create it outside and append to selectbuilder . java aggregation agg = aggregation.count {u}id .groupby {u}city ; new selectbuilder .aggregation agg ;