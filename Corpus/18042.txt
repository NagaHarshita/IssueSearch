 URL : "https://github.com/j-nguyen/FreshPlan/issues/1" TITLE : set-up moya swift BODY : description moya is a network abstraction layer, helped to create network requests. it uses simplistic enums to make network requests a lot easier, than the normal urlsession setup. obviously you can set-up urlsessions with this, but this is a lot more manageable for us, and we don't have to set-up the boilerplate code.