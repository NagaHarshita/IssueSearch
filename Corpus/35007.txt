 URL : "https://github.com/sisoputnfrba/foro/issues/731" TITLE : cursor de un archivo BODY : buenas, leyendo el issue 684 me surgió una duda: el kernel debe conocer el valor del cursor para poder enviarle al file system en una petición de lectura o escritura el offset a partir del cual leer o escribir. mi pregunta es si debo guardar el valor de cada cursor de cada archivo para cada proceso en el kernel, por ejemplo agregando una columna cursor en la tabla de archivos de proceso. gracias.