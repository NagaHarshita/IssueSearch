 URL : "https://github.com/lucasjkr/logstash-filters-for-siemonster/issues/2" TITLE : step 1002 needs refactor BODY : 1 - whole filter should be enveloped with if bro in tags , to prevent other logs getting processed in that pipeline 2 - the if's for identifying log type should be else ifs, and if the type is not identified, simply call it bro-other- so the data can still travel through the pipeline.