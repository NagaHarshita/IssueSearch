 URL : "https://github.com/angular/angular/issues/16544" TITLE : wrong email validation BODY : <!-- if you don't fill out the following information we might close your issue without investigating --> i'm submitting a ... check one with x x bug report feature request support request current behavior <!-- describe how the bug manifests. --> email validator does not work properly. it validates emails without host domain. currently me@host is validated to true expected behavior <!-- describe what the behavior would be without the bug. --> me@host should not validate, me@host.domain should validate. minimal reproduction of the problem with instructions <!-- if the current behavior is a bug or you can illustrate your feature request better with an example, please provide the steps to reproduce and if possible a minimal demo of the problem via https://plnkr.co or similar you can use this template as a starting point: http://plnkr.co/edit/tpl:avjomerrnz94ekvua0u5 . --> issue appears to be in the email_regexp , at packages/forms/src/validators.ts line 45 const email_regexp = /^ ?=.{1,254}$ ?=.{1,64}@ -! $%&' +/0-9=?a-z^_ a-z{|}~ + \. -! $%&' +/0-9=?a-z^_ a-z{|}~ + @ a-za-z0-9 a-za-z0-9- {0,61} a-za-z0-9 ? \. a-za-z0-9 a-za-z0-9- {0,61} a-za-z0-9 ? $/; angular version: 4.0.x <!-- check whether this is still an issue in the most recent angular version --> browser: all | chrome xx | firefox xx | ie xx | safari xx | mobile chrome xx | android x.x web browser | ios xx safari | ios xx uiwebview | ios xx wkwebview <!-- all browsers where this could be reproduced --> all language: all | typescript x.x | es6/7 | es5 typescript