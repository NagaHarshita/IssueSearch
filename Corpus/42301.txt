 URL : "https://github.com/adonisjs/adonis-middleware/issues/8" TITLE : array of inputs/files BODY : _from @andrasf on january 30, 2017 10:5_ hi, i also got issues like issue 303. <input type= text name= datas value= one /> <input type= text name= datas value= two /> with {{ form.open {action: 'testcontroller.index', files: false} }} console.log request.input 'datas' outputs 'one', 'two' with {{ form.open {action: 'testcontroller.index', files: true} }} console.log request.input 'datas' outputs null with {{ form.open {action: 'testcontroller.index', files: true} }} when uploading array of files request.file 'files' returns null but request.file 'files ' works, returns array. handling text values the same way: request.input 'texts' returns null and request.input 'texts ' only returns the last value. it seems like you can have multiple text values with files: false or you can upload multiple files with files: true but you cannot have both in one form. so it has to do with multipart/form-data params. is this considered as a bug? also looks inconsistent. _copied from original issue: adonisjs/adonis-framework 439_