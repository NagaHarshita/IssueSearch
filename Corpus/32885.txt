 URL : "https://github.com/carlos8f/zenbot/issues/817" TITLE : zenbot | quadrigacx BODY : can someone help shed some light on setting this up.. i'm a bit confused on the instruction i've tried multiple variations.. don't get what as a string not numbers means // to enable quadrigacx tranding, enter your api credentials: c.quadriga = {} c.quadriga.key = 'your-api-key' // this is the manual secret key entered by editing the api access // and not the md5 hash you see in the summary c.quadriga.secret = 'your-secret' // replace with the client id used at login, as a string, not number c.quadriga.client_id = 'your-client-id'