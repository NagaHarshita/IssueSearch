 URL : "https://github.com/dennyhollick/localplaylist/issues/2" TITLE : user can enter a location BODY : user story “as an anon user, i want to enter a location because i want discover concerts there.”  scenario given i am on the homepage, when i type a city name, then show it in the input box. acceptance criteria the user sees their text in the input box and it can be passed to a form other info optional predictive text: possible to pre-populate the list from https://www.npmjs.com/package/all-the-cities