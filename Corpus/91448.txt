 URL : "https://github.com/angular/zone.js/issues/739" TITLE : tostring for node objects BODY : currently lib/zone.ts on line 1166 states: typescript public tostring { if this.data && typeof this.data.handleid !== 'undefined' { return this.data.handleid; } else { return object.prototype.tostring.call this ; } } shouldn't this be typescript if this.data && typeof this.data.handleid === 'number' { ? because otherwise it returns node timer objects, which are not strings.