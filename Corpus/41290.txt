 URL : "https://github.com/videojs/video.js/issues/4822" TITLE : .srt to .vtt function BODY : i'm using videojs 6.5.1 i always had to convert my .srt files to .vtt for videojs to pick it up, is it possible to use my small function in the videojs library to convert srt to vtt syntax? does anyone know where i can use my function before videojs parse the subtitle? function srt2vtt srt { var vtt = '' srt = srt.replace /\r+/g, '' ; var list = srt.split ' ' ; for var i = 0; i < list.length; i++ { var m = list i .match / \d+ : \d+ : \d+ ?:, \d+ ?\s --?>\s \d+ : \d+ : \d+ ?:, \d+ ?/ if m { vtt += m 1 +':'+m 2 +':'+m 3 +'.'+m 4 +' --> '+m 5 +':'+m 6 +':'+m 7 +'.'+m 8 +' '; } else { vtt += list i + ' ' } } vtt = webvtt + vtt vtt = vtt.replace /^\s+|\s+$/g, '' ; return vtt }