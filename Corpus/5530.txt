 URL : "https://github.com/datahq/datahub/issues/1" TITLE : publish to npm BODY : we want this library to be published in npm so we can install and use it from other libraries. blocker : there is already published package, which belongs to @rufuspollock and probably is outdated so we need an access to publish there - https://www.npmjs.com/package/datahub acceptance criteria this lib is published here - https://www.npmjs.com/package/datahub tasks get access yarn publish