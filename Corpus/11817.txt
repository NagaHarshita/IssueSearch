 URL : "https://github.com/ZeroPhone/pyLCI/issues/7" TITLE : legacy: cpu load BODY : while ui elements run in background, they basically run an idle loop which sleep 's. this might not be the best use of cpu time and could probably be done with something less cpu-consuming. there aren't useful things that ui elements could run while in background, but i think there could still be a way to decrease cpu consumption - it might very well be that time.sleep s could be replaced by something like select .