 URL : "https://github.com/stellarbit/aioping/issues/4" TITLE : send lot of pings at ones BODY : if i try to ping a lot of pings like in the code below , i get such message: valueerror: too many file descriptors in select what do you suggest, to surpass this limitation? import asyncio import aioping amount_of_pings = 1000 async def do_ping host, n=0 : try: delay = await aioping.ping host 1000 print f {n} ping response in {delay} ms except timeouterror: print timed out loop = asyncio.new_event_loop asyncio.set_event_loop asyncio.new_event_loop set as global loop = asyncio.get_event_loop tasklist = for i in range amount_of_pings : tasklist.append loop.create_task do_ping google.com ,n=i wait_tasks = asyncio.wait tasklist loop.run_until_complete wait_tasks loop.close