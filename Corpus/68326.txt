 URL : "https://github.com/krisselden/chrome-debugging-client/issues/5" TITLE : resuming node execution BODY : hey, i'm trying to attach to a node process started with --inspect-brk . afaik that should launch the process and break on the first line pausing the debugger. so i assume i'd need to run debugger.resume however it never seems to return and just hangs. the connection is ok, tabs list fine. can anyone tell me what i'm doing wrong? import { createsession } from chrome-debugging-client ; import { debugger } from chrome-debugging-client/dist/protocol/v8 ; createsession async session => { // spawn node const child_process = require 'child_process' ; child_process.exec 'node --inspect-brk test.js' ; // open the rest api for tabs const client = session.createapiclient 127.0.0.1 , 9229 ; // get the active tab const tabs = await client.listtabs ; const tab = tabs 0 ; await client.activatetab tab.id ; // open the debugger protocol // https://chromedevtools.github.io/devtools-protocol/ const debuggerclient = await session.opendebuggingprotocol tab.websocketdebuggerurl ; // get debugger interface const dbg = new debugger debuggerclient ; console.log 'aaa' ; //await dbg.enable ; //console.log 'bbb' ; // <-- never happens? await dbg.resume ; console.log 'ccc' ; // <-- never happens? return 'the end'; } .then data => { console.dir data ; } .catch err => { console.error err ; } ;