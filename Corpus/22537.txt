 URL : "https://github.com/chrislusf/gleam/issues/12" TITLE : local sort broken? BODY : the code below should be equivalent to sort | uniq -c golang flow.new . strings string{ a , a , b , a } . map function x return x, 1 end . reduceby function i1, i2 return i1 + i2 end . fprintf os.stdout, %s %d . run the expected output should be something like a 3 b 1 but when i run it, i get: a 2 b 1 a 1 it looks like the local sort is not working. below, i have implemented the example in a test: golang func testcountstringoccurances t testing.t { var s string var count int flow.new . strings string{ a , a , b , a } . map function x return x, 1 end . reduceby function i1, i2 return i1 + i2 end . savefirstrowto &s, &count . run assert.equal t, a , s, we expect the first row to be 'a' assert.equal t, 3, count, the count should be 3!