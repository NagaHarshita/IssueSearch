 URL : "https://github.com/ipfs/interface-datastore/issues/10" TITLE : remove libp2p-crypto dep BODY : currently, libp2p is a dep just for the randombytes function. this is a bit of overkill. we can reduce the size of the module if we do that.