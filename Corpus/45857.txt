 URL : "https://github.com/mafintosh/hyperdb/issues/20" TITLE : is browser supported? BODY : hi, i just tried to build a browser app with the following code and i have an issue concerning blake2b-wasm wich uses node fs module. var hyperdb = require 'hyperdb' var ram = require 'random-access-memory' var db = hyperdb function filename { return ram } db.put '/hello', 'world', function err { if err throw err db.get '/hello', function err, nodes { if err throw err console.log '/hello --> ' + nodes 0 .value } } is hyperdb working on browser?