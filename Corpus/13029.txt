 URL : "https://github.com/lugolabs/tutorials/issues/9" TITLE : startday of week BODY : the first day of the week can be changed to monday in the creator by $ ' weekpicker' .weekpicker { firstday: 1, ... now the grid adapts, but the selected week doesn't adapt - it will always select the first sunday. i solved it in my local copy by adding to the _create function: if typeof this.options.firstday == 'undefined' { this.options.firstday = 0; } then you can safely change the last lines in _setweek function to: this._startdate = new date year, month, day + this.options.firstday ; this._enddate = new date year, month, day + this.options.firstday + 6 ;