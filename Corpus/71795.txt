 URL : "https://github.com/epicforce/AnthillProToJenkinsMigrator/issues/21" TITLE : ahp common step features BODY : as part of jenkinsstep, there's a run ... shell that handles some default parameters. there's a few we don't support that we should: preconditions ignore failures post process timeout ignore failures and timeout are trivial to support and at the very least we should support those. pre and post, i'm not sure what all we can do, but we can support it if we can.