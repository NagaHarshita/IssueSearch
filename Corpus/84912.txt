 URL : "https://github.com/ReactiveKit/Bond/issues/366" TITLE : drag & drop in table view BODY : hi, i was trying to implement a simple table view with simple capability to reorder items in the table view by drag & drop. this feature is available in the very basic uitableviewcontroller implementation out of the box. however, when i bind my tableview to a datasource for simplicity, just an observablearray<string> when i set self.tableview.setediting true, animated: false , i only get a delete button but not the reorder control with drag and drop feature. when i delete the binding and let it work by the delegate methods of feeding data, it appears and works all well. my question is - is this intentional? is drag and drop somehow turned off while using bindings? how should i approach this in order to make it work with bond? thank you!