 URL : "https://github.com/crawfoal/greensteps/issues/52" TITLE : add a pr checklist BODY : some things i'd like on the list: i18n is used for user-facing strings as well as in specs. each chunk of code changes matches the style of similar files, specs, classes, etc. step through the changes by hand in the browser or wherever makes sense - typically can do this in a heroku review app.