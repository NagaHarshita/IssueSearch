 URL : "https://github.com/dgraph-io/dgraph/issues/533" TITLE : backup returns empty reply BODY : backup seems to complete successfully but the request to backup returns an empty reply. ➜ verify git: master ✗ curl localhost:8080/admin/backup curl: 52 empty reply from server i was able to run the verify script and confirm that the source matches the destination which means the backup completed successfully. tried this after loading the 21m rdf but should be reproducible on a smaller data set too.