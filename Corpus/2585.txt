 URL : "https://github.com/conversant/rtree/issues/3" TITLE : bug in branch BODY : hi.i test the code and run some data and find some bug. in branch.java function add should return this not child i .return child i will miss some leaf. here is code: if size < mmin { for int i=0; i<size; i++ { if child i .getrect .contains trect { child i = child i .add t ; mbr = mbr.getmbr child i .getrect ; return child i ;//here should return this } }