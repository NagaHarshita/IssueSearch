 URL : "https://github.com/invisibleroads/socketIO-client/issues/169" TITLE : when should i call wait BODY : in the examples wait is called basically after every method that communicates with the server, but i noticed that: - wait seconds = 1 waits for one second even if a response is received before then - everything works fine without calling wait when is it necessary/recommended to call wait ?