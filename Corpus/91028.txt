 URL : "https://github.com/binlecode/swagger/issues/8" TITLE : support multiple api docs BODY : swagger api document will be long and slow in swagger-ui if when a lot of endpoints are included in single swagger bean generation and json building. it is desired to support splitting the annotation scan scope to break into multiple endpoints groups that make separate json for modularity and performance.