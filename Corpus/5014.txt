 URL : "https://github.com/appcelerator/amp/issues/1221" TITLE : issues on 1209 BODY : gtw.yml + io.amp.role: infrastructure + environment: + - service_ports=80 + - virtual_host=https://gw. you don't need redirection from http to https? gtw.yml + - service_ports=80 + - virtual_host=https://gw. + ports: + - 8080:80 do you need to expose on port 8080? gtw.yml + - virtual_host=https://gw. + ports: + - 8080:80 + deploy: we should add a placement constraint to restrict it on the worker nodes see other stack files api/rpc/stats/stats.proto - metricsioentry io = 14; - metricsmementry mem = 15; - metricsnetentry net = 16; + string sgroup = 13; shouldn't we add new fields at the end, instead of breaking the ids? pkg/docker/docker.go @@ -155,6 +158,7 @@ func d docker stacklist ctx context.context output string, err error { return string output , nil } +// stackremove remoce a stack s/remoce/remove/ platform/stacks/portal.stack.yml +services: + + portal: + image: appcelerator/amp-ui:1.0.0 we started aligning the versions of the artifacts. please use the ${tag:-0.10.1} version as for other core services. platform/stacks/portal.stack.yml + image: appcelerator/amp-ui:1.0.0 + networks: + - default + deploy: should contain a placement constraint too please also remove portal/.ds_store, it contains a binary