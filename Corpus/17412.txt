 URL : "https://github.com/ImOutOfAlex/dotfiles/issues/7" TITLE : pathing is broken BODY : the pathing stuff is a mishmash that's piled up in every file to avoid environment modification. i'd rather not rely on it being in a .dotfiles directory as it is in my environment, but if it is it can be completely pathed using ~/.dotfiles. i've used multiple solutions which are listed here: script=$ readlink -f $0 echo $_ > /dev/null script=$ readlink -f $_