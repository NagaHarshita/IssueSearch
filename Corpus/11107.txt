 URL : "https://github.com/pubkey/rxdb/issues/60" TITLE : find not working BODY : can't get rxdb to find data in a collection. i have tried numerous variations of the find. cachedb.collections.documents.find .where 'cache.name' .eq pathdata.base .where 'cache.path' .eq pathdata.dir .exec .then document => { resolve document ; } ; i have created an index on the database collection col.pouch.createindex { index: { fields: 'cache.name', 'cache.path' , name: 'cachenamepath', ddoc: 'cachenamepath', type: 'json' } i know there is data that matches the query.