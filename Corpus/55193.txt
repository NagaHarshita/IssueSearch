 URL : "https://github.com/stats4sd/SSD-Resources-Demo/issues/53" TITLE : improve image choosing BODY : currently, resource-small chooses which image by checking for every standard resource type id within the resource.rtype . if it finds one, it stops looking and returns the correct image. this works, but i can see it being a pain to update with new images / types in the future, and seems to bring my chrome to the edge of crashing when running it locally. no idea why - i don't think 10 x 10 sequential if-statements should break a browser...