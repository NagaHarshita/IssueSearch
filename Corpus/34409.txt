 URL : "https://github.com/kyuni22/pybbg/issues/21" TITLE : change columns order BODY : there is a bug in bdh sometimes the order of the columns is not the same as your entered it in the ticker list. so you should change the function as : data = dataframe data data.columns = pd.multiindex.from_tuples data, names= 'ticker', 'field' data.index = pd.to_datetime data.index data = data ticker_list return data