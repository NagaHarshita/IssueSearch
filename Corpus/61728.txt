 URL : "https://github.com/arendst/Sonoff-Tasmota/issues/1002" TITLE : blocked loop on reset 2 BODY : on the nodemcu and wemos the watchdog bites on erasing the flash 3mb . there is an additional line required in settings.ino line 312 to update the osw_last_loop . for uint32_t _sector = _sectorstart; _sector < _sectorend; _sector++ { nointerrupts ; result = spi_flash_erase_sector _sector ; interrupts ; if _serialoutput { serial.print f d_log_application d_erased_sector ; serial.print _sector ; if spi_flash_result_ok == result { serial.println f d_ok ; } else { serial.println f d_error ; } delay 10 ; } osw_last_loop = millis ; }