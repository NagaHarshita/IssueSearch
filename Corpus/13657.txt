 URL : "https://github.com/TestRoots/travistorrent-tools/issues/6" TITLE : why count > 50 ? BODY : > select u.login, p.name, p.language, count > from projects p, users u, watchers w > where > p.forked_from is null and > p.deleted is false and > w.repo_id = p.id and > u.id = p.owner_id > group by p.id > having count > 50 > order by count desc in the query you have used to fetch data from ghtorrent, what's the use of having count > 50?