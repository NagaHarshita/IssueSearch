 URL : "https://github.com/nnarain/dotrix/issues/36" TITLE : move from qt to sdl+imgui? BODY : would require entire rewrite... the build would be smaller, and more flexible in terms of what is in a single window display. local multiplayer -> boost.asio scripting -> boost.python need a replacement for http request for dotrix-server though that isn't implemented at the moment anyways