 URL : "https://github.com/vin0110/haas/issues/54" TITLE : refactor haas actions BODY : there are actions that are executed on the master node to support haas operations. for example, when client executes haas data save it creates a process on the master node that does the save. currently, this code is in haascli but should be in a separate module that is copied to the master node. for example. the client runs haas stack save ... this remotely executes python /opt/haas/bin/server-side-of-save-command.py ... the name of the py module above should be something else. in addition to refactoring, we need a reliable way to