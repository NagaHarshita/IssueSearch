 URL : "https://github.com/mangojuicejs/mangojuice/issues/11" TITLE : add dev version of process BODY : when process.env.node_env !== 'production' export dev version of the process instead of the original one. for now, the most important thing that devprocess should do is to deny change of the model and meta directly from commands and tasks. make it so using proxy if available in the environment