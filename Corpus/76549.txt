 URL : "https://github.com/openSUSE/kernel/issues/2" TITLE : libelf-devel not present BODY : the kernel 4.11.rc8 is built with config_stack_validation, which now seems to require the presence of libelf-devel. however, since this new requirement is not listed as a require: in kernel-default-devel/kernel-devel, building kmps always fails with a message cannot use config_stack_validation, please install libelf-dev, …