 URL : "https://github.com/trimox/angular-mdc-web/issues/416" TITLE : bundle size is too big BODY : when used with angular cli, the prod build would include the material.es5.js 580.67k , which inclued all subcomponents, no mater what subcommponent modules you are really used. it's too large! we need to makes the below code really meaningful: @ngmodule { imports: mdccoremodule, // required mdcfabmodule, mdcmenumodule, mdciconmodule, mdcbuttonmodule, mdccardmodule , ...