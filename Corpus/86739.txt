 URL : "https://github.com/Hoshiningen/BudgetTracker/issues/21" TITLE : implement the events tool BODY : the main window should allow the user to manipulate reoccurring events for the selected account. reoccurring events are defined as fixed-amount transactions that occur at regular intervals. requirements 1. the user should be able to create, delete, and edit account events, where each event type is limited appropriately. 1. expense events should be limited by the money that's available for the month.