 URL : "https://github.com/Yomaris/Introduce-to-Computer-Science-and-Programming-Using-Python-/issues/3" TITLE : calculate the square BODY : 例一 python x = 25 epsilon = 0.01 step = 0.1 guess = 0.0 while guess <= x: if abs guess 2 -x < epsilon: break else: guess += step if abs guess 2 - x >= epsilon: print 'failed' else: print 'succeeded: ' + str guess it will print succeeded: 4.9999999999998 例二 python x = 25 epsilon = 0.01 step = 0.1 guess = 0.0 while guess <= x: if abs guess 2 -x >= epsilon: guess += step if abs guess 2 - x >= epsilon: print 'failed' else: print 'succeeded: ' + str guess script enters an infinite loop and never terminates 运行结果： 无限循环加了 break 以后 是failed 因为其首先满足的条件为 guess <=x 没有加break的时候 ，满足 guess <=x 这个条件的数有无数个，结果为无限循环 加了break以后，循环只执行一次就结束，所以结果为failed 例三 python x = 25 epsilon = 0.01 step = 0.1 guess = 0.0 while abs guess 2-x >= epsilon: if guess <= x: guess += step else: break if abs guess 2 - x >= epsilon: print 'failed' else: print 'succeeded: ' + str guess 运行结果： succeeded: 4.9999999999998