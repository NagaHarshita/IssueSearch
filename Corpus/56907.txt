 URL : "https://github.com/PeterMosmans/security-scripts/issues/38" TITLE : output breaks terminal BODY : running scan with the following command: ./analyze_hosts.py 10.0.0.0/24 --compact --threads 2 -o scan leaves the terminal no echoing typed characters after all, bash's reset is necessary to get it back to previous state.