 URL : "https://github.com/colmap/colmap/issues/66" TITLE : accessing raw depth data BODY : hi johannes, after the running the dense mapper, how can one access the pixel-wise depth estimates per image. i imaging they are found in the 0000xxx.depth_map.txt files in the dense folder, but when i cat the file, i see gibberish. i suppose they are stored as uchars, no? assuming i can read the raw files into my program, how is the depth info stored in them? does it contain the image dimensions and such as well? thanks! marc