 URL : "https://github.com/dotCMS/core/issues/11107" TITLE : new site browser portlet BODY : in this repo https://github.com/dotcms/dotjs/tree/master/components jt start to build a new site angular site browser component that allows drag and drop from the file system to dotcms among other things, we need to implement this. expected behavior in general make sure that the new site browser component works and that we don't lose any of the key functionality of the old jsp portlet. need to define what key functionality means possible solution 1. install the component using npm 2. import it into the project __this will give us the basic functionality but we'll lost actiosn over items like: add new page, copy and paste etc...__