 URL : "https://github.com/LaravelRUS/SleepingOwlAdmin/issues/508" TITLE : model events doesn't work BODY : собственно, в initialize секции не работают ивенты. пытаюсь при создании манипулировать данными, например: php $this->creating function $model { $model->owner_id = auth ->user ->id; $model->staff ->attach auth ->user ->id, 'group' => 1 ; } ; и ничего не происходит... owner_id - обязательное поле, но получаю ошибку, так как ивент не срабатывает...