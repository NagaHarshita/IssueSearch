 URL : "https://github.com/mbdavid/LiteDB/issues/467" TITLE : null reference on update BODY : hello, i have a simple class serializable public class userprofile { bsonindex public guid userid { get; set; } bsonref devices public list<string> devices { get; set; } } which i would like to retrieve, change and save. retrieving is ok: var profiles = database.getcollection<userprofile> profiles .include p => p.devices ; var profile = profiles.find p => p.userid.equals profileid .firstordefault ; if !profile.devices.contains deviceid profile.devices.add deviceid ; //-- up to here everything is ok, object has a guid and one string profiles.update profile ; // <----- this fails with a null ref exception why does the update fail? thank you