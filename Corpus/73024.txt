 URL : "https://github.com/fesnt/clean-code-javascript/issues/4" TITLE : code não otimize demais BODY : neste trecho, o exemplo não está invertido? imagino que o correto seja o abaixo. outro detalhe é sobre o argumento que foi feito em cima de browsers antigos, porém a maioria dos códigos estão em es2015, o que perde um pouco o sentido do argumento. talvez seja melhor escrever de outra forma... ruim: javascript // em navegadores antigos, cada iteração de list.length não cacheada seria custosa // devido a recomputação de list.length . em navegadores modernos, isto é otimizado. for let i = 0; i < list.length; i++ { // ... } bom: javascript for let i = 0, len = list.length; i < len; i++ { // ... }