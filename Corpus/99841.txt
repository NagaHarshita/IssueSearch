 URL : "https://github.com/realm/realm-js/issues/1107" TITLE : no logging on ubuntu 16.04 BODY : the following program logs nothing on ubuntu 16.04 with realm js 1.8.2: ~~~ const realm = require 'realm' ; // realm.sync.setsynclogger => { console.log click ; } ; const foo = { name: 'foo', properties: { alpha: 'int' } }; realm.sync.user.login 'http://localhost:9080', 'ks@realm.io', 'foo', error, user => { if error { console.log error ; } else { let realm = new realm { schema: foo , sync: { user: user, url: 'realm://localhost:9080/~/foo' }, } ; } } ; ~~~ also, click is not printed if i comment in the custom logger. this was the case when i installed realm js on clean ubuntu 16.04 using npm install --save realm . if i instead build the binding from the github repo, then default and custom logging works as expected. cc @fealebenpae