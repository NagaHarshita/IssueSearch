 URL : "https://github.com/rstudio/shiny-server/issues/272" TITLE : shiny-server on debian BODY : i've installed the ubuntu binary and works fine but... i can't start/stop/reload shiny using systemctl i tried to build from source, everything works fine and server starts from command line but again can't do that with systemd. i copied the unit files - both shiny-server.service and shiny-server.debian.service - in /lib/systemd/system and created the symlinks in /etc/systemd/systems but: systemctl start shiny-server failed to start shiny-server.service: unknown unit: shiny-server.service systemctl status shiny-server -l failed to get properties: no such interface '' am i missing something ? any hints? regards