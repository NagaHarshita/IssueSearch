 URL : "https://github.com/OpenGamePanel/OGP-Website/issues/198" TITLE : error on installation BODY : i'm guessing this isn't the place to post this, but i need help. i'm running centos 6.9 and when trying to restart proftpd service via your given commands in the tutorial sudo sed -i s/^loadmodule\ \ mod_auth_file.c/ loadmodule mod_auth_file.c/g /etc/proftpd.conf sudo service proftpd restart it gives me a fail notice root@80 ~ sudo service proftpd restart shutting down proftpd: failed starting proftpd: failed i tried reinstalling the service and then re-running the 2 given commands but it didn't work. would really appreciate some help. thanks.