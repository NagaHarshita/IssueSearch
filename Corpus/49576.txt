 URL : "https://github.com/mobxjs/serializr/issues/40" TITLE : typing of deserialize BODY : i think the typing of deserialize typescript function deserialize<t> modelschema: clazzormodelschema<t>, jsonarray: any , ... : t ; would be more straightforward if it takes the type of the array itself, instead of its element. typescript function deserialize<t extends any > modelschema: clazzormodelschema<t 0 >, jsonarray: any , ... : t;