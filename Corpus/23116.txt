 URL : "https://github.com/JaysGitLab/cs5666-spr17-javatrix-ccubed/issues/75" TITLE : no zero dimensions BODY : go through the constructors and make sure that none of them can store an array with a zero dimension in a . go through the constructor tests. - don't allow creation of zero dimension arrays in tests that are not expecting exceptions. - optional? construct tests that expect an exception for an array with a zero dimension.