 URL : "https://github.com/finchalyzer/slinky/issues/10" TITLE : generate responsive code BODY : need to make it so that slinky generates responsive code. i think as long as it made it so that the everything stacked on top of each other in mobile clients in the order groups were created in sketch it wouldn't be too difficult. note: since this is email, it'll need to stack without media queries. perhaps exporting it to inky foundation for emails code would work well for that.