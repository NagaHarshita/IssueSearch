 URL : "https://github.com/NVIDIA/DIGITS/issues/1605" TITLE : segmentation png labels BODY : hello, i see in the new digits that it is possible to use rgb png labels by defining rgb color values of each class in a text file. like this for example 0, 0, 0 128, 0, 0 and so on ... if we decided to not to use this, and provide labels previous digits versions like the one used to make segmentation examples , then how the labels should be? does in the new digits version, a typical conversion performs on the png labels?