 URL : "https://github.com/nerrad/wp-cli-event-espresso/issues/19" TITLE : tweaks to loader BODY : right now the loader::addcommands at a minimum needs: use instanceof check instead of using getcommandparent . use something like https://github.com/brightnucleus/invoker/blob/master/src/instantiatortrait.php for loading the classes so we don't end up with a big switch as time goes on.