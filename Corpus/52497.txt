 URL : "https://github.com/lablup/sorna-client/issues/4" TITLE : command line interface BODY : make it available via python -m sorna or python -m sorna.client provide batch-mode access via cli. - --build , --exec , --lang , --mount , --limit params - if some params are not omitted, infer them with some heuristics: e.g., choose the only script file in the arguments as --exec target provide list/control of active kernel sessions for the configured keypair. show resource statistics of the configured keypair.