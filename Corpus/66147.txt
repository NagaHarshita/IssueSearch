 URL : "https://github.com/ElemeFE/react-amap/issues/4" TITLE : map: key is not a prop. BODY : 地图是渲染出来了，但是浏览器报如下错误： warning: map: key is not a prop. trying to access it will result in undefined being returned. if you need to access the same value within the child component, you should pass it as a different prop. 我的代码如下 key -在这里我随便写了 ： js import react, { component } from 'react'; import { map } from 'react-amap'; class app extends component { render { return <div style={{width: '600px',height: '500px'}}> <map key={ dsafs34132424 } /> </div> ; } } export default app;