 URL : "https://github.com/pods-framework/pods/issues/4445" TITLE : select2 set delay option BODY : i need a way to set the delay option, because now its 100 ms by default and my wordpress almost die using the select2 search because it send several ajax request to get the response. i have a large posts table