 URL : "https://github.com/linuxserver/docker-kodi-headless/issues/56" TITLE : webserver not starting BODY : running this container on latest synology dsm. webserver is not starting on port 8282 anymore after updating to krypton. i have copied the .kodi directory from an existing openelec krypton install where guisettings.xml contains below and the webserver works properly looks like krypton default is chorus2 interface, which looks slick but is definitely new... <services> <webserver default= true >true</webserver> <webserverpassword>xxxxx</webserverpassword> <webserverport>8282</webserverport> <webserverusername default= true >kodi</webserverusername> <webskin default= true >webinterface.default</webskin> <zeroconf default= true >true</zeroconf> </services> when i launch the container, the web interface page fails to load and the <webserver> setting in guisettings.xml gets automatically set from true to false. this will continue to happen on each boot. any one else having luck with webserver on latest container?