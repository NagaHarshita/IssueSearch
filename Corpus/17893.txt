 URL : "https://github.com/juliangut/slim-routing/issues/5" TITLE : route name prefix BODY : add to group annotation a prefix or nameprefix or routenameprefix key to be prefixed to route annotation's name key so routes from the same group can share a common prefix prefixes can be nested with parent groups's add global route prefix strategy, can be snake_case, camelcase or whatever to join group prefixes and route names php / @jsr\router @jsr\group prefix= user , pattern= /user / class user { / @jsr\route name= login , pattern= /login / public function hello { } / @jsr\route name= logout , pattern= /logout / public function hello { } } resulting route names depending on strategy: snake_case user_login user_logout camelcase userlogin userlogout other user.login user.logout ...