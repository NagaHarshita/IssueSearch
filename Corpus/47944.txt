 URL : "https://github.com/ualbertalib/pushmi_pullyu/issues/24" TITLE : read from queue redis BODY : if we are custom implementing pushmipullyu see 17 , we will need to implement the communication of pushmipullyu to talk to our queue redis . pushmipullyu will sit and monitor this queue. once an element on the queue is ready passes a certain window of wait period , pushmipullyu will pop this element off the queue and start the preservation event.