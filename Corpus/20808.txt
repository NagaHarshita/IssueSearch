 URL : "https://github.com/codemirror/CodeMirror/issues/4491" TITLE : ctrl+f not working BODY : if i try and search for a phrase that exists file i have loaded in code mirror i can only find the phrases visible to the portion of code i can see in the browser window. the browser search only finds the other instances if i scroll further down so that the phrases and show in the visible portion of the editor.