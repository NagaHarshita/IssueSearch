 URL : "https://github.com/mapbox/assembly/issues/751" TITLE : easy equal-height grid row BODY : we should make it easy to create a grid row where every item has equal height. right now you can do it by adding additional flex classes; but i think this use-case is common enough that we want to make an easy and bug-free implementation. i think adding a modifier or two for grid classes might do the trick.