 URL : "https://github.com/upstage-org/limelight/issues/69" TITLE : rails 5.1 default id type BODY : in rails 5.1, the default column type for id fields is :bigint rather than :integer we will need to adapt as this becomes an issue