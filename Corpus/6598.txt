 URL : "https://github.com/Hagbuck/imagetools/issues/41" TITLE : ajout des const BODY : il faut ajouter des const dans les paramètres surtout dans le pgm mais aussi dans le bmp. rappel: type ptr : le pointeur peut venir pointer autre chose type const ptr : le pointeur ne peut pas pointer autre chose, mais ce qu'il pointe peut être modifier const type const ptr : le pointeur ne peut pas être modifier ainsi que ce qu'il pointe