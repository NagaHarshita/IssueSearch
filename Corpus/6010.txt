 URL : "https://github.com/textlint/textlint/issues/405" TITLE : upgrade moch 4 BODY : textlint project use almost mocha as testing library. v3 and v4 is mixed in a project. we want to use mocha v4. mocha v4 deprecated --compilers option. instead of, use --require . and, use glob insteadof --recursive options - release v4.0.0 · mochajs/mocha https://github.com/mochajs/mocha/releases/tag/v4.0.0 release v4.0.0 · mochajs/mocha - compilers deprecation https://github.com/mochajs/mocha/wiki/compilers-deprecation compilers deprecation tasks - upgrade mocha v4 - rewrite mocha.opts if needed - use --require instead of --compilers - use glob pattern instead of --recursive - check npm test for each packages