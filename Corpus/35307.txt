 URL : "https://github.com/jorge07/redis-cluster-demo/issues/1" TITLE : docker swarm example BODY : would like to see a docker-compose.yml example to run redis cluster on 3 servers in docker swarm, with replication between each redis on the 3 servers, while enabling read and write access to each redis on the 3 servers.