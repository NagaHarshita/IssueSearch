 URL : "https://github.com/cjkent/osiris/issues/39" TITLE : document known limitations BODY : cloudformation gets confused if you remove staticfiles from an api because it tries to remove the s3 bucket and fails if it contains any files. cloudformation gets confused if you switch from using root.template to using the generated template. it tries to create the static files bucket in the main template without removing it from the nested template. it fails because the bucket already exists.