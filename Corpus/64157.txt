 URL : "https://github.com/zerodayz/citellus/issues/255" TITLE : fix packstack detection BODY : actual behavior packstack=$ is_rpm openstack-packstack-. if ! -z $packstack ; then echo $ packstack detected >&2 exit $rc_failed is_rpm { if x$citellus_live = x1 ; then rpm -qa $1 |egrep ^ $1 - 0-9 elif x$citellus_live = x0 ; then awk '{print $1}' ${citellus_root}/installed-rpms |egrep ^ $1 - 0-9 fi } expected behavior packstack=$ is_rpm openstack-packstack if ! -z $packstack ; then echo $ packstack detected >&2 exit $rc_failed steps to reproduce the behavior