 URL : "https://github.com/golemfactory/golem/issues/975" TITLE : ecdsa signing is slow BODY : after code profiling we can see that a lot of time is signing and veryfing messages. potentional optimization steps: - switch to devp2p preferable, but time-consuming - check if we're using natively compiled secp256k1 - limit number of messages that we're sending - sign only hellomessage should limit the number of messages that we're sending