 URL : "https://github.com/facebook201/HTTP/issues/3" TITLE : tcp 和 udp BODY : tcp构成 传输层协议 tcp负责在不可靠的传输信道上提供可靠的抽象层, 向应用层隐藏了大多数网络通信的复杂细节, 比如丢包重发、按序发送、拥塞控制以及避免 数据完整。采用tcp确保发送的所有字节能够完整地被接收到,而且到达客户端的顺序也是一样。也就是说 tcp专门为精确传送做了优化。