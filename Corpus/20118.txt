 URL : "https://github.com/akintner/RailsEngine/issues/42" TITLE : boss mode merchant BODY : get /api/v1/merchants/:id/customers_with_pending_invoices returns a collection of customers which have pending unpaid invoices. a pending invoice has no transactions with a result of success. this means all transactions are failed. postgres has an except operator that might be useful. activerecord also has a find_by_sql that might help.