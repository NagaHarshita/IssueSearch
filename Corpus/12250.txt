 URL : "https://github.com/pholls/do-you-see-what-issee/issues/5" TITLE : no known address BODY : when no address is retrieved e.g. when the iss is over an ocean , instead of hiding the address div from the view entirely, it is blank, so the page reads: the iss is currently directly above: with nothing there. a javascript error appears in the dev tools console, saying it can't read property formatted_address of undefined . it appears that, instead of removing the address section div because no results are found, the object returned does not exist and the call fails