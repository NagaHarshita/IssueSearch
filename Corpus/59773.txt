 URL : "https://github.com/carstn/chocolatey-packages/issues/18" TITLE : use msi instead of zip BODY : currently the chocolatey package uses zip package for installation of the golang environment. is it possible to use the msi package instead of the zip package?