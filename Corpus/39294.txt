 URL : "https://github.com/CodeYellowBV/mobx-spine/issues/28" TITLE : saving related store BODY : i want to save a ride which has activities : const ride = new ride {}, { relations: 'activities' } ; ride.activities.add { ride } ; ride.activities.add { ride } ; ride.activities.add { ride } ; ride.saveall the request becomes something like this: { data : { id : -98, } , with : { ride : { id : -104 }, { id : -105 }, { id : -106 } , activity : { id : -101, ride : -104 }, { id : -102, ride : -105 }, { id : -103, ride : -106 } } } it doesn't relate the activities with the single ride, but creates a ride per activity.