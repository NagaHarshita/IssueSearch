 URL : "https://github.com/webino/WebinoImageThumb/issues/23" TITLE : problem with https images BODY : hi, i am working with zend 2 and webinoimagethumb, and i realize there is a bug with https image. if i put an https image, i get a image file not found: error from this code: protected function fileexistsandreadable { if $this->isdatastream === true { return; } if stristr $this->filename, 'http://' !== false { $this->remoteimage = true; return; } if !file_exists $this->filename { $this->triggererror 'image file not found: ' . $this->filename ; } elseif !is_readable $this->filename { $this->triggererror 'image file not readable: ' . $this->filename ; } } so i changed it: if stristr $this->filename, 'http://' !== false || stristr $this->filename, 'https://' !== false { $this->remoteimage = true; return; } i wonder if there is any reason that https wasn't considered here? thanks.