 URL : "https://github.com/poppu-mtg/StackIt/issues/17" TITLE : handling split cards BODY : e.g. beck // call : first modification is to change get_card_info in scraper.py so that it reads: >if line.find ' / ' != -1: the current version does not have the spaces around the separator, forcing split cards to be treated as if they were pointing to a specific art. i tested it real quick and adding the spaces makes sure that at least the card is recognized. after that is the treatment of the two cmc's as found in the database. i have a couple of ideas but need to see sample html info from cmcweb first...