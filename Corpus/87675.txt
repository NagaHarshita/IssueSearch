 URL : "https://github.com/NamelessMC/Nameless/issues/856" TITLE : maintenance mode error v2 BODY : template: what kind of issue is it question, bug, etc. ? bug in which version of namelessmc did it occur 1.x.x ? 2.0.0-pr3 how would you describe the issue? you cannot log into your site once using maintenance mode, do you have relevant resources about this issue logs, pictures, etc. ? http://prntscr.com/h9n1k4 database shows the setting to be off do you have an idea how or where the issue occurs? when using the maintenance mode setting in core > debugging and maintenance. when using maintenance mode and closing the admin cp browser tab, you cannot get back into the site after doing so. even going to the admin directory /admin redirects to the maintenance page.