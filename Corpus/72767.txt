 URL : "https://github.com/WasabiFan/adafruit-win10-iot/issues/2" TITLE : change bno055 power mode BODY : hi, i can't seem to get the power mode to change after i initialized the imu. here's the code i am using: bno055 imu = new bno055imu ; await imu.initialize ; await imu.setpowermode powermode.suspend ; now when i call imu.readbyte bno055register.bno055_pwr_mode_addr ; it returns 0, so the power mode isn't being changed to suspend mode. is there something else that i need to do to get the power mode to change?