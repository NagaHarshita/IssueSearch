 URL : "https://github.com/nextcloud/news-android/issues/600" TITLE : sync fails after update BODY : hi, i've just updated the app from version 0.9.9.5 to 0.9.9.11 on my galaxy s7 running android 7.0. unfortunately, the update seems to break the sync. hitting the sync button has no effect. then, i re-installed the app and now i get the following error on sync: attempt to invoke virtual method 'java.lang.string com.google.gson.jsonelement.getasstring ' on a null object reference before the app update, everything worked fine. what can i do?