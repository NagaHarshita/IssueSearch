 URL : "https://github.com/marekma/DotNetMigrator/issues/6" TITLE : do we need the backup? BODY : i can see that you have branches that saying that you want to fix cannot-create-backup-when-backup-exists imho people after successful migration will not want a backup. and if the migration fails, they always have it in source control. i used this tool couple times and never created backups.