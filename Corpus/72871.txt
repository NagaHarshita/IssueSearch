 URL : "https://github.com/conda-forge/conda-forge-webservices/issues/113" TITLE : check user's permissions BODY : thinking about whether we should be checking user's permissions before running certain commands. for example, should one be able to ask for a re-rendering to a pr that they don't have push permissions to otherwise i.e. a feedstock they cannot make changes to ? any thoughts on this @isuruf?