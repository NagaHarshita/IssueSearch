 URL : "https://github.com/scg-unibe-ch/ese2017-team8/issues/21" TITLE : crash you opponent 1 BODY : wenn man beim erstellen eines paketes beim kommentar einen zu langen string über 256 zeichen eingibt stürzt die seite ab und erstellt kein paket, wenn man dann aber zurück geht und es korrigiert wird dann ein paket erstellt es wird aber eine id übersprungen. deutet darauf hin, dass wenn ein fehler beim erstellen eines paketes passiert trotzdem irgendwas geändert wird.