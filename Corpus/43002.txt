 URL : "https://github.com/joshghent/NodeChat/issues/5" TITLE : change way chat works BODY : on login ask the user if they want to join a room or create a room if they join a room, handle the api request for the user to join the room. if they create a room, generate a new room code which should be a random string that should be unguessable not necessarily secure like a password but password-like .