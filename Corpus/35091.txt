 URL : "https://github.com/wp-cli/ideas/issues/14" TITLE : db & media migration BODY : import the database from a remote installation into a local one and replace the url correctly as wp-search-replace does it ie: wp remote pull staging wp remote push prodcution the environments would be defined in wp-cli config file. media should be handled the same ie compare which media is not used anymore and which media is missing and download them. tldr; a wp cli alternate to wp migrate db pro plugin