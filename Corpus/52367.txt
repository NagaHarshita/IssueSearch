 URL : "https://github.com/PDOK/LinkDaLe/issues/112" TITLE : classify literal types BODY : description currently, literals types are guessed based on each literal. we want the user to create type their literals. the following literal types need to be there - string - language-tagged string by default look at their locale --> dialog where they are able to specify the tag - integer - float - double - long - date - datetime - other --> dialog where they are able to fill in their own uri and help the user by selecting the closest match of the data first todo - write description - add corrosponding labels