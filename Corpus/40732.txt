 URL : "https://github.com/prestodb/presto/issues/8577" TITLE : use ssl for port 443 BODY : since presto 0.180, presto-jdbc requires explicit ssl option like jdbc:presto://api-presto.treasuredata.com:443/td-presto?ssl=true for using ssl. i still don't figure it out what kind of users want to disable ssl for port 443 access. if there is no specific reason, i'd like create a pr to turn on ssl=true by default when port 443 is used, so that we can always use the simplest url like: jdbc:presto://api-presto.treasuredata.com:443/td-presto any opinion?