 URL : "https://github.com/KunYi/DumpSMBIOS/issues/5" TITLE : read access violation BODY : hi ! void dumpsmbiosstruct void addr, uint len and void smbios::parsesmbiosstruct void addr, uint len should be fixed to avoid read access violation with certain motherboard: ... for ;; { pheader = psmbiosheader p; dispatchstructtype void p ; lpbyte nt = p + pheader->length; // point to struct end if dword nt + 1 >= lastaddress break; // this can happened on x99 motherboard with 3302 bios version. while 0 != nt | nt + 1 nt++; // skip string area nt += 2; if dword nt >= lastaddress break; p = nt; } ... hope this can helps