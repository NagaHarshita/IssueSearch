 URL : "https://github.com/openconfig/reference/issues/75" TITLE : encoding of leaf values BODY : there seems there is a discrepancy between the specification text in 2.2.3 node values and the examples in 2.3.1 of the gnmi specification https://github.com/openconfig/reference/blob/master/rpc/gnmi/gnmi-specification.md 223-node-values : in the example tree, we have: /a/b name=1 /c/e uint32 if we follow the text in 2.2.3, the server should return a typedvalue with uint_val : 123 but the example text in 2.3.1 shows: json_val: 10042 // decoded byte array