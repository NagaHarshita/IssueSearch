 URL : "https://github.com/laurelherting/portfolio/issues/83" TITLE : fix linting errors BODY : eslint / prettier should be working now. i would create a feature branch fix/linting-errors and fix these errors then pr. i like to keep these types of fixes apart from new features / changes. react and nodejs projects have started using a combo of eslint and prettier across the board. with the prettier plugin for our editors, all our code looks the same and has a standard. it makes code reviews and stepping in to other repos much easier. : some examples from app.js : component isn't used since you're using a stateless component - this should just be an import for react import react, { component } from 'react'; indenting const app = => <router> <div> <header id= home classname= height: 753px; > ...