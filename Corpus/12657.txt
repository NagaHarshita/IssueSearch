 URL : "https://github.com/Capharas/BsCaching/issues/3" TITLE : read cache block BODY : int readcacheblock int blockid, char buffer ; liest 1024 byte aus dem block mit der id blockid in den bereitgestellten puffer buffer ein. liefert 0 im erfolgs- und -1 im fehlerfall d.h. blockid existiert weder im hauptspeicher noch auf festplatte zurück. sollte der entsprechende block sich nicht im hauptspeicher sondern auf der festplatte befinden, muss für diesen block platz im hauptspeicher durch auslagerung geschaffen werden