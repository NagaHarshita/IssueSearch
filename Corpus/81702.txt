 URL : "https://github.com/monica001/leetcode/issues/41" TITLE : 414. third maximum number BODY : 第一個版本 public int thirdmax int nums { // 先排序 array.sort nums ; list<int> list = new list<int> ; list.add nums nums.length - 1 ; // i-1 防超出索引範圍 for int i = nums.length - 1; i - 1 >= 0; i-- { if nums i != nums i - 1 { list.add nums i - 1 ; } if list.count==3 { return list 2 ; } } // 迴圈跑完，沒有第三個最大數字，回傳最大數字 return list 0 ; } 第二個版本參考 reference: http://www.cnblogs.com/grandyang/p/5983113.html public static int thirdmax { // 用long宣告，避免陣列裡面有int範圍的最小值 long first = long.minvalue, second = long.minvalue, third = long.minvalue; for int i = 0; i < nums.length; i++ { int num = nums i ; if num > first { third = second; second = first; first = num; } else if num > second && num < first { third = second; second = num; } else if num > third && num < second { third = num; } } // thrid等於原本數字或等於second，傳回first最大數，否則表示有第三個最大數，回傳thrid long t = third == long.minvalue || third == second ? first : third; return int t; }