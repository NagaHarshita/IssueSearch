 URL : "https://github.com/twbs/bootstrap/issues/24129" TITLE : carsole very fast this way BODY : append // to every line greatly improving runtime performance var start = performance.now ; $ mycarousel .carousel ; var stop = performance.now ; console.log carousel took + stop - start + milliseconds. ; // 5 milliseconds compared to this with 5 milliseconds improvement. var start = performance.now ; //$ mycarousel .carousel ; var stop = performance.now ; console.log carousel took + stop - start + milliseconds. ; // 0 milliseconds