 URL : "https://github.com/bengranett/minimask/issues/5" TITLE : logging settings rewritten BODY : @bengranett: i think that putting statements like this logging.basicconfig level=logging.debug into the module files overwrites the logging settings of any code that uses the module. anyway in pypelid the logging settings seem to have been broken by switching to minimask happily this seems to be the only thing that went wrong . :