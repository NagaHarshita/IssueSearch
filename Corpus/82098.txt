 URL : "https://github.com/StarcounterApps/Timeline/issues/24" TITLE : add filtering to timeline BODY : the timeline should be able to filter different event types. by following the latest design for the timeline, there should be a filter button, which opens a new menu where the user can: 1. see all the types in a paginated list 2. enable/disable every event type. > if enabled it should be visible under the all tab. 3. if an event type is enabled, it should also be possible to add this event type as a favorite in order to create a shortcut for sorting and displaying only events from that event type. 4. add a search field , to sort the paginated list, which live updates the list based on the input