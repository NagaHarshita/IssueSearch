 URL : "https://github.com/elpez/scam/issues/13" TITLE : add asserts to verify code BODY : it can be written like this c ifndef debug define assert n else define assert n \ ... endif so that the asserts can be turned off when debugging is turned off.