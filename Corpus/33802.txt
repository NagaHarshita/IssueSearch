 URL : "https://github.com/damln/localtower/issues/12" TITLE : array migration fail BODY : when adding a new model , and choosing array as one of the types for a new column, the migration fails. when using localtower to add a new migration the choice for array works perfectly. i noticed that new migration adds two lines: add_column :target_model, :column_name, :text, default: , array: true add_index :target_model, :column_name, using: :gin however, adding a new model and choosing array, only adds this one line inside the create_table action: t.array :column_name