 URL : "https://github.com/waterlink/mocks.cr/issues/28" TITLE : does not work BODY : crystal require spec require mocks class example def say_hello name hey, {name} end end mocks.create_mock example do mock say_hello name end example = example.new describe example do example.say_hello hello it do example.should have_received say_hello hello passes end it do example.should have_received say_hello hi fails end end in spec/mocks_spec.cr:19: undefined method 'say_hello' example.should have_received say_hello hello passes