 URL : "https://github.com/davidbanham/express-async-errors/issues/1" TITLE : simpler wrapper fn BODY : first off: nice little library, thanks! would it be simpler to do something like this for the wrapper function? js const wrap = fn => req, res, next => { promise.resolve fn req, res, next .catch next } if you're using async you _should_ also have support for promise . and this gets around any issues with the wrapped function _not being async/promise-based_, by wrapping it directly in a promise, which also avoids the need to check for catch as a function on the function return. what do you think about this?