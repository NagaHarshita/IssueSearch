 URL : "https://github.com/RossThorn/VizTest/issues/1" TITLE : d3 radar plot not working BODY : so i've found that the .enter is what is messing up the code. upon removal, the code block that triggers on map zoom/load/move works. however, the radar chart and the g element where the radar plot normally lies, is empty. any knowledge on this? // attempt to get the radar chart on the map feature = g.selectall plots .data sites.objects .enter .append function { var width = 100, height = 100; // config for the radar chart var config = { w: width, h: height, maxvalue: 100, levels: 5, extrawidthx: 300 }; //call function to draw the radar chart d3.json radardata2.json , function error, data { if error throw error; radarchart.draw g , data, config ; } ; }