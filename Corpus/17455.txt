 URL : "https://github.com/glebdmitriew/node-unzip-2/issues/19" TITLE : pipe from request BODY : cool library. it works fine with files, but i can't get it to work with requests: js const request = require 'request' ; const unzip = require 'unzip2' ; const run = => { let stream = request.get sdk_url .pipe unzip.parse .on 'entry', entry => { entry.autodrain ; } ; return new promise resolve, reject => { stream.on 'finish', resolve ; stream.on 'error', reject ; } ; }; this gives me: > { error: invalid distance too far back > at zlib._handle.onerror zlib.js:355:17 errno: -3, code: 'z_data_error' } any ideas?