 URL : "https://github.com/ssire/axel/issues/60" TITLE : date filter default value BODY : when using a default value with the date filter using the filter date format, the date is correctly printed with the screen date format when displaying the editor, however if clearing the date in the field delete all , then the default date value is printed on screen using the date filter date format and does not seem to be correctly recognized by the jquery widget. it seems the default date value is not converted using the date format when resetting the field.