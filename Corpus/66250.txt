 URL : "https://github.com/ngageoint/scale/issues/959" TITLE : message handler service BODY : create a message handler service that launches long running message handlers to process the backend messages. 1. create the concept of system services that launch long running system tasks 2. create the message handler service and add ability to scheduler rest api to control the number of message handlers 3. add node tasks and system tasks to node status json 4. add services section to status json