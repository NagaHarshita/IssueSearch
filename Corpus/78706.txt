 URL : "https://github.com/sohlex/botkit-rasa/issues/2" TITLE : recognize bot's messages BODY : hi! in order to not sent to rasa the bot reply, this could be added just after if !message.text || message.is_echo { next return } we can simply identify whether the message comes from a bot: if message.bot_id != null { console.log '\t\t own_debug bot response, not processing' next return } hope it helps. regards