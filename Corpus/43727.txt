 URL : "https://github.com/influxdata/kapacitor/issues/1192" TITLE : default node in batch BODY : hi, in the following code, i expected the default to fill the gap of the missing value, but somehow it remains blank. what am i missing? i get an alert with id 'kapacitor//...' batch |query 'select mean used_percent from influxdb . default . mem ' .groupby 'tag1','missing-tag' .period 15m .every 15m |eval lambda: trunc mean .as 'mean' |default .tag 'missing-tag', 'unknown' |alert .id 'kapacitor/{{ index .tags missing-tag }}/...