 URL : "https://github.com/dbrgn/gerber-types-rs/issues/14" TITLE : higher level api BODY : some brainstorming about a higher level api. what do we need for pcb creation? the best approach would probably to have a context object, and then to add helper methods on top of that builder style . some ideas: rust use gerber::layer; use gerber::{fileattribute, part, filefunction, extendedposition, coppertype}; let mut layer = layer::new ; // set properties layer.set_file_attribute part::single ; layer.set_file_attribute filefunction filefunction::copper { layer: 0, pos: extendedposition::top, copper_type: some coppertype::signal , } ; // create apertures for pads layer.create_aperture / todo: what does this look like? / todo: continue this brainstorming : cc @rnestler @ubruhin