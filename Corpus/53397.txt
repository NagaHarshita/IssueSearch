 URL : "https://github.com/gabrielrcouto/php-convnet/issues/1" TITLE : does not work on mnist BODY : does not work on mnist. there are two examples in the archive. one loads a working javascript version of the neural network, the other creates its own in php. in getprediction, it always returns the same value on a trained neural network. during training, a bug with softmax: in softmaxlayer::backward, in return -log $this->es $y ; -- $y instance of convnet\vol instead single value. demo https://github.com/gabrielrcouto/php-convnet/files/1240578/phpconvnet.zip