 URL : "https://github.com/helospark/telnet-snake/issues/1" TITLE : optimize toplist creation BODY : background it is slow to calculate the toplist after the game ends, taking several seconds. it also does not scale well as the number of data increases, because db continuously has to do ordered queries. ac - create toplist table, which is kept up-to-date, so it is fast - make backward compatible, if the table does not exist yet, create it