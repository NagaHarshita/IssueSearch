 URL : "https://github.com/xluffy/til/issues/66" TITLE : list index in bash BODY : ví dụ ta có array như sau > linux= 'debian' 'red hat' 'suse' 'fedora' ta thường loop qua từng phần tử như sau ${linux @ } > for i in ${linux @ } ; do echo $i ; done nếu muốn list index tương ứng với từng phần tử thì > for index in ${!linux @ } ; do echo $index ; done điểm khác nhau là dấu ! trước array