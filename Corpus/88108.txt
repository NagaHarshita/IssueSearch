 URL : "https://github.com/pkra/MathJax-single-file/issues/24" TITLE : modify menu configuration BODY : the preconfigured builds and possibly all builds should modify the menu configuration to prevent users from activating non-existing features. remove renderer submenu this library only support one renderer right now remove localizations submenu this build does not include locales though they/some could now be specified in the build configuration