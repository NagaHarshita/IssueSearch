 URL : "https://github.com/JoshGlazebrook/smart-buffer/issues/14" TITLE : mark for smart-buffer v4 BODY : > going forward for version 4 since these will be major breaking changes , i'm looking at adding: > > changing .writexxx value, offset to overwrite in place instead of inserting values. if an offset is not given, then it will just write at the currently tracked write offset position. > adding .insertxxx value, offset to function how .writexxx value, offset currently does. > adding import { buffer }... > allow more manipulation of the internally tracked automatic read/write buffers. ref: https://github.com/joshglazebrook/smart-buffer/pull/13