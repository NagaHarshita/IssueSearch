 URL : "https://github.com/freeCodeCamp/freeCodeCamp/issues/15015" TITLE : counting cards bug BODY : hi, i've done everything correctly, getting correct answer but still task doesn't completes. help me please. code: var count = 0; function cc card { // only change code below this line switch card { case 2: case 3: case 4: case 5: case 6: count = ++count; } switch card { case 7: case 8: case 9: count = count; } switch card { case 10: case j : case q : case k : case a : count = --count; } if count>0 { return \ + count + bet\ ; } else { return \ + count + hold\ ; } // only change code above this line } // add/remove calls to test your function. // note: only the last will display cc 10 ; cc j ; cc q ; cc k ; cc a ;