 URL : "https://github.com/jagi/meteor-astronomy/issues/621" TITLE : map custom type problem BODY : hi again! i tried to implement a new type in astronomy, for js maps: type.create { name: 'map', class: map, validate args { return true; } } ; if i use the cast function to display the field value in my document, it display map {} . no problem here. but when on clientside i call findone on a document, and then display the type of the field, it displays object{} . that's a real problem, because of that, i can't iterate correctly on it, nor call get, set, delete. i can't seem to find what's going on, or what i missed. any idea or valid custom type implementation for maps?