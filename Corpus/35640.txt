 URL : "https://github.com/caolan/async/issues/1374" TITLE : es7 await with async BODY : can i use this following syntax if final callback return a promise? let result = await async.waterfall async.apply fs.readfile, filename, utf8 , async.asyncify function contents { return db.model.create contents ; } , function model, next { // model is the instantiated model object. // if there was an error, this function would be skipped. } , callback ;