 URL : "https://github.com/apache/incubator-mxnet/issues/7961" TITLE : fixed_param in module BODY : hi, the current fixed_param is only compatible with params fixed at early stage. for example. fixing a here. input -> a -> b but if i have another network like following one. the fixed_param is not working as i want. here i want to train c and d while fixing a and b. so i will need the gradient from , which is blocked if i use fix param. input -> a ----------------> b |---->c| |-> d