 URL : "https://github.com/contribsys/faktory/issues/42" TITLE : add noop command BODY : if a worker has a concurrency of 10, then it may have 10 or 11 connections open. if the beat is once per 15s per worker not per connection is it worth adding a ping / pong command that a thread tied up with a long running job can send to the server to keep the connection alive? the worker could send a periodic beat but that updates the db i think which incurs some cost. i’m happy to submit a pr