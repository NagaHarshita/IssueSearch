 URL : "https://github.com/SheetJS/js-xlsx/issues/726" TITLE : set number format to text BODY : text number format is represented with string '@'. here is the code i have been trying function _jsontoxlsx data, options { var defaults = { start: { cell : 0, row : 0 } }; var options = options || defaults; var ws_name = sheetname ; / set up workbook objects -- some of these will not be required in the future / var wb = {} wb.sheets = {}; wb.props = {}; wb.ssf = {}; wb.sheetnames = ; / create worksheet: / var ws = {}; / the range object is used to keep track of the range of the sheet / var range = {s: {c:options.start.cell, r:options.start.row}, e: {c:0, r:0 }}; / iterate through each element in the structure / for var r = 0; r != data.length; ++r { if range.e.r < r range.e.r = r; for var c = 0; c != data r .length; ++c { if range.e.c < c range.e.c = c; / create cell object: .v is the actual data / var cell = { v: data r c }; if cell.v == null continue; / create the correct cell reference / var cell_ref = xlsx.utils.encode_cell {c:c,r:r} ; / determine the cell type / cell.t = 's'; / set the format / cell.z = '@'; / add to structure / ws cell_ref = cell; / this call updates the formatted text field .w / xlsx.utils.format_cell ws cell_ref ; console.log ws cell_ref ; } } ws '!ref' = xlsx.utils.encode_range range ; / add worksheet to workbook / wb.sheetnames.push ws_name ; wb.sheets ws_name = ws; return wb; } the console log outputs : { v: '00:04:44.17', t: 's', z: '@', w: '00:04:44.17' } but the sheet still shows the cell to have _general_ number format instead of _text_. i am using version 0.8.7 at node. you can find the sheet in the attachment. audio_segments_template.xlsx https://github.com/sheetjs/js-xlsx/files/1138043/audio_segments_template.xlsx