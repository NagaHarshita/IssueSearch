 URL : "https://github.com/radare/r2hate/issues/10" TITLE : cve as a hate machinery BODY : lately in r2land, some cve peeps have started to report null derefs, undefined behaviour as cves right after doing the release. things that can be fixed in 1 line for stuff that can't be exploited in any way is used to proof that c is a bad language or the code reviews are not nazi enough. this thing can be fixed by: empowering the code reviews with even more strict rules moving non-core things out of core setup 24/7 fuzzing machinery in our farm full refactoring of everything on every release enlarge from 1 week to 2 the stall time for new features