 URL : "https://github.com/graphcool/api-bugs/issues/186" TITLE : one to one self relation BODY : what is the current behavior? there is a contact model and self relate through relation spouse one-to-one . it can setspouse spouse1contactid, spouse2contactid . for example, joe and mary are couple. it can query mary from joe; however, it cannot do the reverse to query joe from mary with the same query. please share the relevant part of your graphql schema and all functions, permissions or other project settings for easier reproduction type contact implements node { id: id! @isunique name: string! spouse: contact @relation name: spouse } if applicable, share the query, mutation or subscription for reproduction query { contact id: $id { name spouse {name} }} what is the expected behavior? it should query spouse from a contact vice versa.