 URL : "https://github.com/adrienkohlbecker/vagrant-fsnotify/issues/19" TITLE : missing 'include' option BODY : in some cases, the activity that's interesting for fsnotify happens in specific subdirectories. an include regex would support this, and mirror exclude . linked to 18 in the interim, i am using a fugly regex with negative look-ahead assertions to ab use the 'exclude' option to make it work as an 'include'...