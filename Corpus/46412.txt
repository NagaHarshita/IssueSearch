 URL : "https://github.com/mantidproject/mantid/issues/18733" TITLE : make vis popups modal BODY : spotted by jon taylor expected behavior dialogs on the vsi should be able to come in front of the main vsi window actual behavior they are hidden behind steps to reproduce the behavior open vsi and click something that generates a dialog. for example, setting the rotation point. platforms affected osx