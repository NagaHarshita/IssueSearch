 URL : "https://github.com/screwdriver-cd/screwdriver/issues/652" TITLE : vm in k8s is slow in aws BODY : problem vm builds are 5 times slower than non-vm builds on aws for cpu intensive commands, e.g. hab install, docker build etc. investigation i exec into the vm and non-vm builds and do a top : - hab is taking up almost 100% of cpu in both builds when it tries to install pkgs during the beginning - but it is much faster in the non-vm builds possible cause - ec2 is also virtualization. having nested virtualization may affect the performance of the cpu in vm.