 URL : "https://github.com/pixelated/pixelated-user-agent/issues/934" TITLE : validate the password BODY : the user must inform and confirm the password, so we should validate that the passwords match and any other necessary validations for password. acceptance criteria when i type a password that is different than the confirmation, i should get a message informing me that the passwords don't match. when i don't type a password, i should get an error that i need to type a password. when i don't type a confirmation for my password, i should get an error that i need to type a confirmation. when my password is invalid too short, too long?? , i should get an error that i need to type a valid password. in scope implementing the validations showing the messages translating the messages out of scope changing password depends on 933