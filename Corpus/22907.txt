 URL : "https://github.com/mailgun/mailgun-go/issues/100" TITLE : multiple domains ? BODY : currently mailgun supports multiple domains, but the api is not. what's the best way to add this option ? add domain string in the message struct and change generateapiurl method , like: go func generateapiurl m mailgun, domain, endpoint string string{ if domain == { domain = m.domain } return fmt.sprintf %s/%s/%s , m.apibase , domain, endpoint } so: go func m mailgunimpl send message message mes string, id string, err error { .... r := newhttprequest m, message.domain, message.specific.endpoint .... } it would be interesting select a specific domain per each email send ! what's the best solution ?