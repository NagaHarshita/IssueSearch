 URL : "https://github.com/elwayman02/ember-user-activity/issues/115" TITLE : manually trigger event BODY : is there a way to manually trigger the idlechanged event? i have the listener but i would want to trigger the event manually, so i force the 'user-idle' to be checked. this is my code currently, standard procedure: this.get 'useridle' .on 'idlechanged', isidle => { if isidle { if this.get 'useridle' .enabled { // do stuff } } } ; but can i trigger it like or example: _this.get 'useridle' .trigger ;_ ??