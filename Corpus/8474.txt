 URL : "https://github.com/devopsgroup-io/siteshooter/issues/60" TITLE : opening a modal BODY : i'm having some issues getting a modal to open and then screenshot it. i'm using the following... siteshooter v1.5.2 node v7.9.0 i'm trying to screenshot a site that's at website.com/hcp on the page it has a background video and a play button over the background video. when the play button is pressed a modal opens and another video plays. i would like to get a screenshot of the modal open with the poster image of the video. basically i took your example code and changed it slightly. maybe i'm missing something... function getqueryvariable variable { var query = window.location.search.substring 1 ; var vars = query.split '/' ; for var i = 0; i < vars.length; i++ { var pair = vars i .split '=' ; if decodeuricomponent pair 0 == variable { return decodeuricomponent pair 1 ; } } } if typeof jquery !== undefined { jquery document .ready function { var pagename = getqueryvariable 'name' , pageevent = getqueryvariable 'prevent' ; console.log 'document ready.' ; console.log 'useragent', navigator.useragent ; console.log 'page: ', pagename ; console.log 'event: ', pageevent ; switch pagename { // home case '': switch pageevent { case 'hcp': jquery ' btn-play a' .click ; break; } break; } } ; }