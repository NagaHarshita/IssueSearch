 URL : "https://github.com/MagneticParticleImaging/MDF/issues/51" TITLE : storing background data BODY : when we convert a brukerfile into an mdf its not clear if we should store the bg subtracted system matrix or not. in brukerfiles on can open both which is in my point of view to much data. i think we should store the bg corrected data but should add another optional field containing a mean bg vector. this could then be used for reconstructing data where no bg measurement is available.