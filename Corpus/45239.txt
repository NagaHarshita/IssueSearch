 URL : "https://github.com/flathub/com.visualstudio.code/issues/4" TITLE : host runtime binaries BODY : is there some way to access system binaries like /usr/bin/go inside sandbox? many plugins do need system binaries to work properly.