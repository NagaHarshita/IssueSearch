 URL : "https://github.com/kubernetes/kubernetes/issues/47574" TITLE : validation of rbac rules BODY : is this a request for help? if yes, you should use our troubleshooting guide and community support channels, see https://kubernetes.io/docs/tasks/debug-application-cluster/troubleshooting/. : no what keywords did you search in kubernetes issues before filing this one? if you have found any duplicates, you should instead reply there. : rbac validation --- is this a bug report or feature request? choose one : feature request it would be helpful if the api server could validate rbac rules upon creation. right now it's possible to create rules targeting resources which don't exist. for example, i just ran into a case where port forwarding was being denied for one of our developers, and the problem turned out to be that portforward was misspelled in our rbac rule that was should have allowed it. it would have been great to have the api server catch this when the resources were first created. something like: $ kubectl apply -f rbac-rules.yml error: no resource pods/portfoward in api group core .