 URL : "https://github.com/missioncommand/emp3-android/issues/109" TITLE : mgrs grid lines BODY : to test use test basic. via the menu / settings / map grid. select mgrs. the mgrs grid should show up on the map. pan / zoom the map. the grid should correctly redraw. alter the camera heading, title and roll the map slows down for few seconds and stops after waiting few seconds it starts working again. this issue only appear in nexus tablet. same thing happens when selected utm grid