 URL : "https://github.com/mcallegari/qlcplus/issues/1002" TITLE : chaser scene shuffling BODY : problem description : i often create chasers that randomly turn some leds on/off. but i prefer having an unordered scene list over using the random mode as i want to control the exact play order e.g. to avoid the same scene twice . therefore, i manually resort the scenes so that they seem random. desired solution : a possibility to shuffle the scenes give each scene a new random position possible implementation : add a shuffle button right below the move updwards/downwards buttons. orient on the functionality of these buttons on how to swap scenes and then implement the fisher-yates shuffling algorithm. i know this issue has a quite low importance, but as it also doesn't seem that difficult, it might be a good first code contribution for me.