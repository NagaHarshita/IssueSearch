 URL : "https://github.com/kobakei/Android-RateThisApp/issues/83" TITLE : unable to add library BODY : i tried to add library but it gives error. have a look at the build gradle build.gralde project-level // top-level build file where you can add configuration options common to all sub-projects/modules. buildscript { repositories { jcenter mavencentral } dependencies { classpath 'com.android.tools.build:gradle:2.3.0' classpath 'com.novoda:bintray-release:0.3.4' } } allprojects { repositories { jcenter mavencentral } }