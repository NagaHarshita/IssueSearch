 URL : "https://github.com/angr/angr/issues/443" TITLE : get_any_node returns none BODY : hello again, i managed to obtain a path using a path_group and now i would like to get the node associated with it and after the finalllll_states . i have seen that in few cases if i do something like cfg.get_any_node addr where addr was returned via a pathhistory i receive none as return value and i cannot understand why. if i try to get the block at the same addr all works fine. typically the block start with something like add esp,0x10 . is there any reason why a node cannot be found? i have generatd a cfgaccurate . thanks