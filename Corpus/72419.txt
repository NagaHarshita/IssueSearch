 URL : "https://github.com/superposition-dev/lol-rm-migrations/issues/3" TITLE : build cli for migrations BODY : we need to write either several small cli's for migrations for example... create_table --name=blah etc. create_database --env=local alter_table --name=blah etc. or if we have one class to rule them all... like: migrations --create-table add in other variables need to grok usability for the cli