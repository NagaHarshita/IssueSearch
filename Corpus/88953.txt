 URL : "https://github.com/CSC470-Alphatronics/CSC470-Alphatronics/issues/4" TITLE : patron_login is spaghetti BODY : currently, the patron login is pretty nasty to look at around 150 lines of code . i added to that tonight by adding the suspension functionality, but i really think we should get this code pristine in our next sprint. we'll just want to think about ways we can optimize the code possibly using the read function and clean up the rest of it, possibly moving some of it to other functions if possible. also, i added in a variable called suspcolnum which is tracking the exact column number of the suspension column in the patron table. i'd like to find a better way of doing that than having a magic number assigned to it, if possible.