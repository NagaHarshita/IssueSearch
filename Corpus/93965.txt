 URL : "https://github.com/jupyter/notebook/issues/2722" TITLE : backend server log is lost BODY : i have a piece of code which run a backend server. when the code is executed, the backend server would be launched as a separated thread running in backend and the cell is mark as finished. but in this scenario, the backend server log is lost. i don't where to find it. could anyone tell me how to find the log ? not sure whether this is a bug.