 URL : "https://github.com/karel-brinda/prophyle/issues/190" TITLE : dealing with memory errors BODY : since we aim at computers with small ram, we should detect command errors caused by lack of memory and print more informative message with a possible solution. propagation => use less cores klcp & sa construction => don't do it in parallel @salikhov-kamil, would it reduce memory of prophyle_index ? these errors can easily appear when too many other apps are opened. we should also document it in faqs in documentation.