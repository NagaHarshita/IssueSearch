 URL : "https://github.com/IceflowRE/MR-eBook-Downloader/issues/27" TITLE : add custom file checking BODY : need custom checking if a good file is downloaded. or add after and before methodes? with filename.open mode='r', encoding= utf-8 as reader: check = reader.readline 14 if check == <!doctype html : if only the html file invalid attachment specified. was downloaded download_failed_list.append at_id else: download_succeed_list.append at_id