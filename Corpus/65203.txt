 URL : "https://github.com/chapel-lang/chapel/issues/7846" TITLE : improve channel formats BODY : today, channel formats are handled through type specifiers in writef calls or the style format argument: chpl writef '%...', foo ; // or var c = open 'foo.txt', iomode.r .reader style=... ; there are 2 dimensions to what we might want a channel io to support: formats and types . formats json, toml, yaml, ... ^ | | | ------> types array, int, string, ... here are some sketches of possible directions to move towards: chpl // e.g. json format json.writeln r, f ; f.writeln r, formatter=json ; writef '%c', json, r ;