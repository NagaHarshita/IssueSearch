 URL : "https://github.com/frictionlessdata/dpr-api/issues/330" TITLE : ui improvements and fixes BODY : as an outcome from 298 tasks - credits are missing: oki, datopian, sloan, etc. - swagger / api spec - recommend we don't consider this v1 yet i recommend we follow semantic versioning, so we should keep this below v1 for a couple of months at least - page on logout needs a little work - metadata button - triggers a download for the dp descriptor - why call it metadata this is a dp registry, let's keep the terminology consistent