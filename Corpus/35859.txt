 URL : "https://github.com/qvantel/jsonapi-client/issues/11" TITLE : filter values quoted BODY : thanks for the library! in https://github.com/qvantel/jsonapi-client/blob/master/src/jsonapi_client/filter.py l74, the value is being explicitly quoted. this results in double quoted values upon deserialization in crnk a fork of katharsis, a java jsonapi server-side library . here is the non-working filter setup: python filter = filter myuuuid=uuidstr if i explicitly set the filter string as below, it works correctly. python filter = filter filter myuuid ={} .format uuidstr i don't believe the values should be explicitly quoted. if you agree, i can submit a patch.