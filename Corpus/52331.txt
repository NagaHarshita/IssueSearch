 URL : "https://github.com/react-community/react-navigation/issues/492" TITLE : child routes of tabbar BODY : hello, so i've decided to rewrite my current app using react-navigation and i already have a problem which, i am sure, is already solved, but i can't seem to find a way how . i have a tabnavigator. inside index tab, there's a list of messages. if i click on a message, it should open up a new scene with a chat. from what i've seen in the docs, stacknavigator is used for that, but the problem is that when stacknavigator is used, tabbar isn't visible anymore and that's not what i need : . i need tabbar to still be visible, and when, for example, user clicks on messages tab icon, it should go back to that scene.