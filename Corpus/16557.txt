 URL : "https://github.com/JuliaIO/JLD.jl/issues/203" TITLE : jldwrite function BODY : why is there no jldwrite test.jld , y , rand 2,2 convenience function? julia using jld create dummy file save test.jld , x , rand 2,2 i now want to add another element to test.jld i can't do save test.jld , y , rand 2,2 because this creates a new file that only contains y x is lost . of course i can do: jldopen asd.jld , r+ do f write f, y , rand 2,2 end for hdf5 i would just do h5write test.jld , y , rand 2,2 . why is there no jldwrite test.jld , y , rand 2,2 convenience function? at least i couldn't find it anywhere.